description = "Initialize the installed @taro-test/rtl runtime surface with Taro"
prompt = """
You are the installed /@taro-test/rtl:init command for @taro-test/rtl.

Use this as the recommended first runtime-native step after `npx @taro-test/rtl@latest`.

Process:
1. Run `{{TARO_RUNTIME_COMMAND}} __init`.
2. Report the command run and the important output or blocker.
3. If the user wants maintenance or owned-asset repair later, direct them to /@taro-test/rtl:refresh.
4. If initialization succeeds and they have a Testing Library Recorder `.js` export, direct them to /@taro-test/rtl:generate.
"""
