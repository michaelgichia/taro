---
phase: 03-resume-retry-and-failure-semantics
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/cli/commands/regrade.ts
  - src/cli/commands/tests/regrade.test.ts
autonomous: true
requirements:
  - RGEX-02
user_setup: []
must_haves:
  truths:
    - If a regrade run fails mid-loop, the current test remains `in-progress` and later tests remain `pending`.
    - The command exits non-zero for loop execution failure without falsely marking the current row `completed`.
    - A rerun after failure retries the current in-progress test first and can continue through the queue once that retry succeeds.
  artifacts:
    - `src/cli/commands/regrade.ts` contains an explicit failure boundary around per-test runner execution.
    - `src/cli/commands/tests/regrade.test.ts` proves failure-stop and resume-after-failure behavior.
  key_links:
    - Regrade failure handling mirrors the target directory-loop contract in `src/cli/commands/target.ts`.
    - Failure-state tracker persistence builds directly on the resume semantics locked down in Plan `03-01`.
---

<objective>
Harden the failure path for `regrade --directory-loop` so interrupted runs stop cleanly, preserve the active tracker row, and resume safely on the next invocation.

Purpose: The loop now finishes successfully, but it still needs an intentional contract for runner errors and mid-loop interruption. Output: Explicit stop-on-failure behavior, corrected exit/logging semantics, and integration tests proving retry-after-failure works. </objective>

<execution_context> @$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md </execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-resume-retry-and-failure-semantics/03-RESEARCH.md
@.planning/phases/03-resume-retry-and-failure-semantics/03-VALIDATION.md
@.planning/phases/03-resume-retry-and-failure-semantics/03-01-PLAN.md
@src/cli/commands/regrade.ts
@src/cli/commands/regrade-runner.ts
@src/cli/commands/target.ts
@src/cli/commands/tests/regrade.test.ts
@src/cli/commands/tests/target.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add an explicit failure boundary around per-test regrade execution</name>
  <files>src/cli/commands/regrade.ts</files>
  <read_first>src/cli/commands/regrade.ts, src/cli/commands/target.ts, src/cli/commands/regrade-runner.ts</read_first>
  <action>Wrap the per-entry runner call in `src/cli/commands/regrade.ts` with deliberate failure handling so a thrown regrade error stops the loop immediately, leaves the current row `in-progress`, does not touch later pending rows, and exits with an execution-failure code instead of falling through the generic usage/configuration error path. Reuse the target directory-loop failure model rather than introducing bespoke semantics.</action>
  <verify>
    <automated>pnpm exec vitest run src/cli/commands/tests/regrade.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Runner failure stops the loop immediately.
    - The current row remains in-progress after failure.
    - Later rows remain pending after failure.
  </acceptance_criteria>
  <done>The regrade loop has an explicit, safe stop-on-failure contract.</done>
</task>

<task type="auto">
  <name>Task 2: Align regrade failure logging and exit codes with directory-loop execution failures</name>
  <files>src/cli/commands/regrade.ts</files>
  <read_first>src/cli/commands/regrade.ts, src/cli/commands/target.ts</read_first>
  <action>Separate invalid-command usage errors from mid-loop execution failures in `src/cli/commands/regrade.ts`. Preserve the existing code path for bad input, but ensure actual loop execution failures use a non-zero execution exit code and contextual logging that makes it clear the batch stopped on the active test rather than failing argument validation.</action>
  <verify>
    <automated>pnpm exec vitest run src/cli/commands/tests/regrade.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Usage errors still exit through the existing invalid-input path.
    - Mid-loop execution failures exit through a distinct non-zero failure path.
    - Logs make it clear which test stopped the loop.
  </acceptance_criteria>
  <done>Failures are diagnosable and no longer look like command-usage errors.</done>
</task>

<task type="auto">
  <name>Task 3: Add failure-stop and retry-after-failure integration tests</name>
  <files>src/cli/commands/tests/regrade.test.ts</files>
  <read_first>src/cli/commands/tests/regrade.test.ts, src/cli/commands/tests/target.test.ts, src/cli/commands/regrade.ts</read_first>
  <action>Add integration tests that prove: a runner failure leaves the current test in-progress and later tests pending, a rerun retries that same test first, and once the retried test succeeds the loop can continue to the remaining queued work. Keep the assertions focused on tracker state, call order, and exit codes rather than runtime docs or Phase 4 concerns.</action>
  <verify>
    <automated>pnpm exec vitest run src/cli/commands/tests/regrade.test.ts src/cli/commands/tests/target.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/cli/commands/tests/regrade.test.ts` contains a failure-stop assertion.
    - `src/cli/commands/tests/regrade.test.ts` contains a retry-after-failure assertion.
    - The tests prove later rows are not processed before the failed in-progress row succeeds.
  </acceptance_criteria>
  <done>The failure path is protected by the same kind of integration tests already used for target directory loops.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm exec vitest run src/cli/commands/tests/regrade.test.ts` exits 0
- [ ] A command-level test proves a failure leaves the current row in-progress and later rows pending
- [ ] A command-level test proves rerun retries the failed row before continuing to the rest of the queue
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The regrade directory loop stops safely on failure and resumes from the active row on rerun </success_criteria>

<output>
After completion, create `.planning/phases/03-resume-retry-and-failure-semantics/03-02-SUMMARY.md`
</output>
