---
phase: 03-resume-retry-and-failure-semantics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/commands/regrade.ts
  - src/cli/commands/tests/regrade.test.ts
autonomous: true
requirements:
  - RGEX-03
user_setup: []
must_haves:
  truths:
    - A rerun of `regrade --directory-loop` must not reprocess rows already marked `completed`.
    - An existing `in-progress` regrade row must be retried before any later `pending` row.
    - Tracker rebuilds for discovered test files must preserve prior `completed` and `in-progress` states unless a future documented gating rule explicitly requeues them.
  artifacts:
    - `src/cli/commands/regrade.ts` contains an explicit resume-selection path for existing tracker rows.
    - `src/cli/commands/tests/regrade.test.ts` proves completed rows are skipped and in-progress rows are retried first on rerun.
  key_links:
    - Resume selection in `regrade.ts` mirrors the existing directory-loop semantics already exercised by `src/cli/commands/target.ts`.
    - Regrade tracker rebuilds preserve prior row state instead of resetting completed work back to pending.
---

<objective>
Lock down resume semantics for `regrade --directory-loop` so existing tracker state survives reruns and already-completed rows are not reprocessed.

Purpose: Phase 2 delivered the happy path, but Phase 3 needs an explicit restart contract before failure handling is hardened. Output: Deterministic regrade resume behavior plus command-level tests that prove completed rows are skipped and the current in-progress row is retried first. </objective>

<execution_context> @$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md </execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-resume-retry-and-failure-semantics/03-RESEARCH.md
@.planning/phases/03-resume-retry-and-failure-semantics/03-VALIDATION.md
@.planning/phases/02-sequential-regrade-loop-and-history-persistence/02-02-SUMMARY.md
@src/cli/commands/regrade.ts
@src/cli/commands/regrade-runner.ts
@src/cli/commands/target.ts
@src/cli/commands/tests/regrade.test.ts
@src/cli/commands/tests/target.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract or clarify resume-selection rules for existing regrade tracker rows</name>
  <files>src/cli/commands/regrade.ts</files>
  <read_first>src/cli/commands/regrade.ts, src/cli/commands/target.ts, .planning/phases/03-resume-retry-and-failure-semantics/03-RESEARCH.md</read_first>
  <action>Refactor `src/cli/commands/regrade.ts` so the resume-selection rule is explicit rather than implicit: prefer the existing `in-progress` row, otherwise choose the next `pending` row, and never select `completed` rows during normal reruns. Keep the implementation aligned with the target directory-loop control flow instead of introducing a regrade-only policy surface.</action>
  <verify>
    <automated>pnpm exec vitest run src/cli/commands/tests/regrade.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The loop-selection logic in `src/cli/commands/regrade.ts` makes skip-completed and retry-current behavior obvious.
    - The command does not requeue completed rows unless their status is changed by some other explicit rule.
    - The refactor does not change Phase 2 happy-path behavior.
  </acceptance_criteria>
  <done>The regrade loop has a clear, reviewable resume contract.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve prior row state when rebuilding the regrade tracker</name>
  <files>src/cli/commands/regrade.ts</files>
  <read_first>src/cli/commands/regrade.ts, src/cli/commands/target-directory-tracker.ts</read_first>
  <action>Harden the tracker rebuild path in `buildRegradeDirectoryLoopTracker()` so existing `completed` and `in-progress` rows for currently discovered tests are preserved across reruns, while newly discovered tests still enter as `pending`. Keep Phase 3 scoped to preservation semantics only; do not introduce new score-threshold-based requeue rules here.</action>
  <verify>
    <automated>pnpm exec vitest run src/cli/commands/tests/regrade.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Existing completed regrade rows remain completed after tracker rebuild.
    - Existing in-progress regrade rows remain in-progress after tracker rebuild.
    - Newly discovered tests without prior tracker rows still start as pending.
  </acceptance_criteria>
  <done>Reruns can safely rebuild the tracker without losing place.</done>
</task>

<task type="auto">
  <name>Task 3: Add resume tests for skip-completed and retry-current behavior</name>
  <files>src/cli/commands/tests/regrade.test.ts</files>
  <read_first>src/cli/commands/tests/regrade.test.ts, src/cli/commands/tests/target.test.ts, src/cli/commands/regrade.ts</read_first>
  <action>Add command-level tests proving that reruns of `regrade --directory-loop` skip rows already marked `completed`, retry an existing `in-progress` row before later `pending` work, and continue to leave completed rows untouched unless a future explicit gating rule changes them. Mirror the target command’s existing resume assertions where possible to keep the two flows behaviorally aligned.</action>
  <verify>
    <automated>pnpm exec vitest run src/cli/commands/tests/regrade.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/cli/commands/tests/regrade.test.ts` contains a completed-row skip assertion.
    - `src/cli/commands/tests/regrade.test.ts` contains an in-progress-row retry-first assertion.
    - The new tests do not rely on Phase 4 runtime docs or future requeue rules.
  </acceptance_criteria>
  <done>Phase 3 has direct regression coverage for the resume behavior it promises.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm exec vitest run src/cli/commands/tests/regrade.test.ts` exits 0
- [ ] A command-level test proves completed rows are skipped on rerun
- [ ] A command-level test proves an existing in-progress row is retried before later pending work
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The regrade directory loop can resume from existing tracker state without reprocessing completed work </success_criteria>

<output>
After completion, create `.planning/phases/03-resume-retry-and-failure-semantics/03-01-SUMMARY.md`
</output>
