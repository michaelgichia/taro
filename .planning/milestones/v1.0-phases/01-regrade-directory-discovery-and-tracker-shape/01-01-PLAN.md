---
phase: 01-regrade-directory-discovery-and-tracker-shape
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/commands/target-directory-tracker.ts
  - src/cli/commands/tests/target-directory-tracker.test.ts
  - src/cli/commands/tests/target.test.ts
autonomous: true
requirements:
  - RGTRK-01
  - RGTRK-02
  - RGTRK-03
user_setup: []
must_haves:
  truths:
    - A canonical tracker file under `.taro/directory-loop/` can represent regrade-directory entries as well as current target-directory entries.
    - Tracker status updates still allow only one `in-progress` entry at a time after the schema changes.
    - Tracker Markdown round-trips preserve the per-entry fields needed for Phase 1 discovery work, including the current stored score threshold when available, without breaking the existing target tracker contract.
  artifacts:
    - src/cli/commands/target-directory-tracker.ts contains the generalized or regrade-capable tracker implementation.
    - src/cli/commands/tests/target-directory-tracker.test.ts covers the updated tracker shape and status semantics.
  key_links:
    - The tracker reader and writer agree on the same Markdown columns and metadata fields for regrade directory entries.
    - Existing target directory-loop tests continue to pass against the updated tracker surface.
---

<objective>
Create the tracker foundation that Phase 1 and later regrade directory-loop work will rely on.

Purpose: Reuse the repo’s canonical `.taro/directory-loop/` tracker conventions instead of inventing a separate batch-progress format for regrade. Output: A tracker module and tests that can represent test-oriented directory-loop entries while preserving the current single-active-entry invariant. </objective>

<execution_context> @$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md </execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-regrade-directory-discovery-and-tracker-shape/01-RESEARCH.md
@.planning/phases/01-regrade-directory-discovery-and-tracker-shape/01-VALIDATION.md
@src/cli/commands/target-directory-tracker.ts
@src/cli/commands/tests/target-directory-tracker.test.ts
@src/cli/commands/tests/target.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce a regrade-capable tracker entry shape</name>
  <files>src/cli/commands/target-directory-tracker.ts</files>
  <read_first>src/cli/commands/target-directory-tracker.ts, src/cli/commands/target.ts, .planning/phases/01-regrade-directory-discovery-and-tracker-shape/01-RESEARCH.md</read_first>
  <action>Refactor `src/cli/commands/target-directory-tracker.ts` so the tracker module can represent regrade directory-loop entries without losing support for the existing target directory-loop entries. Keep `getDirectoryLoopTrackerPath()`, repo-relative display paths, atomic temp-file writes, and the single `in-progress` entry reset behavior. Introduce explicit regrade-entry fields for the discovered test path and the current stored score threshold when available, but do not add Phase 2 fields such as updated score history writes or retry heuristics. Preserve a stable path for the existing target tracker output under `.taro/directory-loop/`.</action>
  <verify>
    <automated>pnpm test -- --run src/cli/commands/tests/target-directory-tracker.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/cli/commands/target-directory-tracker.ts` contains tracker entry fields that can represent a regrade test path in addition to current target-loop data.
    - `src/cli/commands/target-directory-tracker.ts` contains a field for the current stored score threshold when available on regrade tracker entries.
    - `src/cli/commands/target-directory-tracker.ts` still contains `getProjectStatePath(projectRoot, "directory-loop"` and the temp-file rename write pattern.
    - `src/cli/commands/target-directory-tracker.ts` still resets an existing `in-progress` entry to `pending` when a new entry is marked `in-progress`.
  </acceptance_criteria>
  <done>The tracker module supports Phase 1 regrade metadata without regressing the existing tracker invariants.</done>
</task>

<task type="auto">
  <name>Task 2: Update tracker Markdown rendering and parsing for test-oriented rows</name>
  <files>src/cli/commands/target-directory-tracker.ts</files>
  <read_first>src/cli/commands/target-directory-tracker.ts, src/cli/commands/tests/target-directory-tracker.test.ts, .planning/REQUIREMENTS.md</read_first>
  <action>Update `renderDirectoryLoopTrackerMarkdown()` and `readDirectoryLoopTracker()` so regrade directory-loop rows can round-trip through Markdown with explicit status, tracked test path, and the current stored score threshold when available while retaining the existing target-loop columns or an intentionally generalized column layout. Make the rendered heading and summary block still identify the file as a Taro directory-loop tracker and keep counts for `pending`, `in-progress`, and `completed` entries.</action>
  <verify>
    <automated>pnpm test -- --run src/cli/commands/tests/target-directory-tracker.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/cli/commands/target-directory-tracker.ts` renders a Markdown table that includes a test-oriented path column for regrade entries.
    - `src/cli/commands/target-directory-tracker.ts` renders the current stored score threshold for a regrade entry when that value exists.
    - `src/cli/commands/target-directory-tracker.ts` parses the same rendered table back into tracker entries via `readDirectoryLoopTracker`.
    - `src/cli/commands/target-directory-tracker.ts` still renders summary lines containing `- Pending:`, `- In progress:`, and `- Completed:`.
  </acceptance_criteria>
  <done>The tracker’s Markdown format can be used as the canonical persisted surface for both current target-loop rows and upcoming regrade-loop rows, including the current stored score threshold required by RGTRK-02.</done>
</task>

<task type="auto">
  <name>Task 3: Expand tracker regression coverage without breaking target loop behavior</name>
  <files>src/cli/commands/tests/target-directory-tracker.test.ts, src/cli/commands/tests/target.test.ts</files>
  <read_first>src/cli/commands/tests/target-directory-tracker.test.ts, src/cli/commands/tests/target.test.ts, src/cli/commands/target-directory-tracker.ts</read_first>
  <action>Add focused Vitest coverage for the updated tracker schema: include at least one test that renders and reads back a regrade-oriented tracker row with a populated current stored score threshold, one test that proves only one entry remains `in-progress` after a status change, and one regression assertion in `src/cli/commands/tests/target.test.ts` confirming the existing target directory-loop tracker still renders the expected completed rows after the tracker refactor.</action>
  <verify>
    <automated>pnpm test -- --run src/cli/commands/tests/target-directory-tracker.test.ts src/cli/commands/tests/target.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/cli/commands/tests/target-directory-tracker.test.ts` contains a test covering a regrade-oriented tracker row with its current stored score threshold.
    - `src/cli/commands/tests/target-directory-tracker.test.ts` contains an assertion that only one entry is `in-progress` after a status update.
    - `src/cli/commands/tests/target.test.ts` still contains directory-loop tracker assertions for completed rows such as `| completed |`.
  </acceptance_criteria>
  <done>The tracker refactor is guarded by tests that protect both the new regrade shape and the existing target-loop behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test -- --run src/cli/commands/tests/target-directory-tracker.test.ts` exits 0
- [ ] `pnpm test -- --run src/cli/commands/tests/target.test.ts` exits 0
- [ ] Tracker Markdown still shows pending/in-progress/completed counts and round-trips updated rows
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The tracker module can represent regrade directory-loop entries without breaking target directory-loop semantics </success_criteria>

<output>
After completion, create `.planning/phases/01-regrade-directory-discovery-and-tracker-shape/01-01-SUMMARY.md`
</output>
