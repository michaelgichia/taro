---
phase: 04-runtime-guidance-and-regression-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/USER-GUIDE.md
  - .codex/skills/@taro-test/rtl-regrade/SKILL.md
  - .codex/skills/@taro-test/rtl-help/SKILL.md
  - agents/taro-regrade.md
  - commands/claude/@taro-test/rtl/regrade.md
  - commands/gemini/@taro-test/rtl/regrade.toml
  - commands/opencode/@taro-test/rtl-regrade.md
autonomous: true
requirements:
  - RGUX-01
user_setup: []
must_haves:
  truths:
    - Runtime-facing regrade docs must describe both single-file and directory-loop invocation.
    - The packaged regrade assets must explicitly route directory input into `__regrade <test-directory> --directory-loop`.
    - Help and README surfaces must explain tracker location and the `pending` → `in-progress` → `completed` row lifecycle.
  artifacts:
    - `README.md` and `docs/USER-GUIDE.md` describe directory-loop regrade usage and tracker behavior.
    - The authored regrade assets in `agents/` and `commands/` document the supported directory-loop command path.
    - Help-routing surfaces mention the batch regrade path alongside single-file regrade.
  key_links:
    - Regrade runtime guidance should mirror the existing target-runtime pattern for directory input.
    - The docs must describe the actual Phase 1-3 behavior already implemented in `src/cli/commands/regrade.ts`.
---

<objective>
Update the published runtime guidance so users can discover and correctly invoke directory-loop regrade from the packaged Taro surfaces.

Purpose: The feature exists, but the installed docs/help still describe only single-file regrade. Output: Canonical README, user guide, help-routing text, and packaged runtime assets that explain both single-file and `--directory-loop` regrade. </objective>

<execution_context> @$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md </execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-runtime-guidance-and-regression-coverage/04-RESEARCH.md
@.planning/phases/04-runtime-guidance-and-regression-coverage/04-VALIDATION.md
@.planning/phases/03-resume-retry-and-failure-semantics/03-02-SUMMARY.md
@README.md
@docs/USER-GUIDE.md
@.codex/skills/@taro-test/rtl-regrade/SKILL.md
@.codex/skills/@taro-test/rtl-help/SKILL.md
@agents/taro-regrade.md
@commands/claude/@taro-test/rtl/regrade.md
@commands/gemini/@taro-test/rtl/regrade.toml
@commands/opencode/@taro-test/rtl-regrade.md
@.codex/skills/@taro-test/rtl-target/SKILL.md
@agents/taro-target.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document directory-loop regrade in README and user guide</name>
  <files>README.md, docs/USER-GUIDE.md</files>
  <read_first>README.md, docs/USER-GUIDE.md, src/cli/commands/regrade.ts, .planning/phases/04-runtime-guidance-and-regression-coverage/04-RESEARCH.md</read_first>
  <action>Update `README.md` and `docs/USER-GUIDE.md` so the regrade guidance explicitly distinguishes single-file regrade from batch regrade. Add concrete examples showing `<test-directory> --directory-loop`, state that the tracker is written under `.taro/directory-loop/`, and describe the row lifecycle as `pending` → `in-progress` → `completed` with current score, updated score, and follow-up comments.</action>
  <verify>
    <automated>pnpm exec vitest run src/install/tests/codex-runtime.test.ts src/install/tests/prompt-runtimes.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `--directory-loop` in the regrade guidance section.
    - `docs/USER-GUIDE.md` contains `.taro/directory-loop/` in the grading or regrade guidance.
    - Both docs mention the status progression `pending`, `in-progress`, and `completed`.
  </acceptance_criteria>
  <done>The package docs describe the shipped batch regrade behavior instead of only the legacy single-file flow.</done>
</task>

<task type="auto">
  <name>Task 2: Update authored regrade runtime assets for directory input</name>
  <files>.codex/skills/@taro-test/rtl-regrade/SKILL.md, agents/taro-regrade.md, commands/claude/@taro-test/rtl/regrade.md, commands/gemini/@taro-test/rtl/regrade.toml, commands/opencode/@taro-test/rtl-regrade.md</files>
  <read_first>.codex/skills/@taro-test/rtl-regrade/SKILL.md, agents/taro-regrade.md, commands/claude/@taro-test/rtl/regrade.md, commands/gemini/@taro-test/rtl/regrade.toml, commands/opencode/@taro-test/rtl-regrade.md, .codex/skills/@taro-test/rtl-target/SKILL.md</read_first>
  <action>Revise the authored regrade runtime assets so they support two modes: single-file regrade and test-directory regrade with `--directory-loop`. Replace the blanket “Do not invent or invoke `__regrade`” wording with a precise rule: for a directory plus `--directory-loop`, run the runtime command `{{TARO_RUNTIME_COMMAND}} __regrade <test-directory> --directory-loop` or its installed equivalent; for single-file regrade, keep the existing delta/scoring contract. Include tracker-path reporting and the expected completed-row metadata in the response contract.</action>
  <verify>
    <automated>pnpm exec vitest run src/install/tests/codex-runtime.test.ts src/install/tests/prompt-runtimes.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The authored regrade assets mention `__regrade <test-directory> --directory-loop`.
    - The assets still describe the single-file regrade path.
    - The response contract or workflow mentions returning the tracker path for directory mode.
  </acceptance_criteria>
  <done>The authored runtime assets can safely guide users through either regrade mode.</done>
</task>

<task type="auto">
  <name>Task 3: Update help-routing surfaces to advertise batch regrade</name>
  <files>.codex/skills/@taro-test/rtl-help/SKILL.md, README.md, docs/USER-GUIDE.md</files>
  <read_first>.codex/skills/@taro-test/rtl-help/SKILL.md, README.md, docs/USER-GUIDE.md, .codex/skills/@taro-test/rtl-regrade/SKILL.md</read_first>
  <action>Update the help-routing and overview surfaces so they explicitly tell users that `regrade` supports both an existing test file and a test directory with `--directory-loop`. Mention that batch regrade writes a tracker under `.taro/directory-loop/` and is the correct choice when a user wants to regrade all tests in one directory rather than one file at a time.</action>
  <verify>
    <automated>pnpm exec vitest run src/install/tests/codex-runtime.test.ts src/install/tests/prompt-runtimes.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `.codex/skills/@taro-test/rtl-help/SKILL.md` mentions directory-loop regrade.
    - The overview docs no longer imply that regrade is single-file only.
    - The help surfaces point users at the tracker path or batch behavior when directory mode is relevant.
  </acceptance_criteria>
  <done>The routing layer surfaces the new regrade capability instead of hiding it behind implementation knowledge.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm exec vitest run src/install/tests/codex-runtime.test.ts src/install/tests/prompt-runtimes.test.ts` exits 0
- [ ] `README.md` and `docs/USER-GUIDE.md` both mention directory-loop regrade
- [ ] Packaged regrade assets mention the directory-loop runtime command path
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Users can discover and correctly invoke directory-loop regrade from the documented runtime surfaces </success_criteria>

<output>
After completion, create `.planning/phases/04-runtime-guidance-and-regression-coverage/04-01-SUMMARY.md`
</output>
