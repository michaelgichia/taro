---
phase: 04-runtime-guidance-and-regression-coverage
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/install/tests/codex-runtime.test.ts
  - src/install/tests/prompt-runtimes.test.ts
  - src/install/tests/verification.test.ts
  - src/cli/commands/tests/regrade.test.ts
autonomous: true
requirements:
  - RGUX-01
user_setup: []
must_haves:
  truths:
    - Installed runtime assets must be regression-tested for directory-loop regrade wording and launcher paths.
    - Packaged/install verification should fail if regrade guidance falls back to the old single-file-only contract.
    - The documented directory-loop invocation must stay aligned with the real regrade command smoke coverage.
  artifacts:
    - Install/runtime tests assert directory-loop regrade content in packaged Codex and prompt-runtime assets.
    - Verification or packaging tests cover the regrade assets as part of installed runtime proof.
    - Regrade command smoke coverage remains aligned with the documented tracker behavior.
  key_links:
    - Regression tests should consume the installed asset outputs rather than only the authored source files.
    - Regrade smoke assertions protect the actual command behavior that the runtime docs now describe.
---

<objective>
Add regression coverage that keeps the installed runtime guidance in sync with the implemented directory-loop regrade feature.

Purpose: Updating docs alone is not enough; Phase 4 must lock the new guidance into install/runtime tests so future asset changes cannot silently regress it. Output: Installed-asset assertions plus smoke coverage that prove the documented directory-loop regrade path remains accurate. </objective>

<execution_context> @$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md </execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-runtime-guidance-and-regression-coverage/04-RESEARCH.md
@.planning/phases/04-runtime-guidance-and-regression-coverage/04-VALIDATION.md
@.planning/phases/04-runtime-guidance-and-regression-coverage/04-01-PLAN.md
@src/install/tests/codex-runtime.test.ts
@src/install/tests/prompt-runtimes.test.ts
@src/install/tests/verification.test.ts
@src/cli/commands/tests/regrade.test.ts
@src/cli/commands/regrade.ts
@src/install/runtimes/codex.ts
@src/install/runtimes/prompt-runtimes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand installed-runtime assertions for directory-loop regrade content</name>
  <files>src/install/tests/codex-runtime.test.ts, src/install/tests/prompt-runtimes.test.ts</files>
  <read_first>src/install/tests/codex-runtime.test.ts, src/install/tests/prompt-runtimes.test.ts, agents/taro-regrade.md, commands/claude/@taro-test/rtl/regrade.md, commands/gemini/@taro-test/rtl/regrade.toml, commands/opencode/@taro-test/rtl-regrade.md</read_first>
  <action>Extend the installed-runtime tests so they assert the generated Codex and prompt-runtime regrade assets mention directory-loop support, the `__regrade <test-directory> --directory-loop` runtime command path, and tracker-path or batch-progress reporting. Keep the assertions at the installed-output level so the tests reflect what users actually receive after installation.</action>
  <verify>
    <automated>pnpm exec vitest run src/install/tests/codex-runtime.test.ts src/install/tests/prompt-runtimes.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/install/tests/codex-runtime.test.ts` contains an assertion for directory-loop regrade content.
    - `src/install/tests/prompt-runtimes.test.ts` contains assertions for the prompt-runtime regrade assets.
    - The tests fail if the old single-file-only wording returns without the new directory-loop path.
  </acceptance_criteria>
  <done>The installed regrade surfaces are protected against documentation regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Keep package/install verification aware of the updated regrade assets</name>
  <files>src/install/tests/verification.test.ts</files>
  <read_first>src/install/tests/verification.test.ts, src/install/tests/codex-runtime.test.ts, src/install/tests/prompt-runtimes.test.ts</read_first>
  <action>Update the package/install verification coverage if needed so Phase 4’s regrade guidance surfaces are still represented in package smoke proof or runtime verification expectations. Keep this focused on proving the updated regrade assets remain part of the packaged/install surface, not on re-testing install plumbing broadly.</action>
  <verify>
    <automated>pnpm exec vitest run src/install/tests/verification.test.ts src/install/tests/codex-runtime.test.ts src/install/tests/prompt-runtimes.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Verification or package smoke coverage still includes the regrade runtime assets.
    - The updated batch-regrade docs are not omitted from the packaged runtime proof.
    - The task does not broaden into unrelated install-surface refactors.
  </acceptance_criteria>
  <done>The updated regrade guidance is part of the verified install boundary.</done>
</task>

<task type="auto">
  <name>Task 3: Add a smoke assertion that keeps docs aligned with the implemented regrade loop</name>
  <files>src/cli/commands/tests/regrade.test.ts</files>
  <read_first>src/cli/commands/tests/regrade.test.ts, src/cli/commands/regrade.ts, README.md, docs/USER-GUIDE.md</read_first>
  <action>Add or adjust a focused smoke assertion in `src/cli/commands/tests/regrade.test.ts` that keeps the documented directory-loop contract grounded in the implementation: the command must report the tracker path, queue count, and completed-row metadata that the Phase 4 docs describe. Reuse the existing regrade integration tests instead of creating a second broad end-to-end harness.</action>
  <verify>
    <automated>pnpm exec vitest run src/cli/commands/tests/regrade.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/cli/commands/tests/regrade.test.ts` contains an assertion for tracker-path or queue reporting that matches the documented batch flow.
    - The smoke coverage still passes with the Phase 3 resume/failure semantics in place.
    - The task avoids duplicating broad directory-loop integration coverage already present.
  </acceptance_criteria>
  <done>The docs and runtime guidance remain anchored to a real passing regrade command path.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm exec vitest run src/install/tests/codex-runtime.test.ts src/install/tests/prompt-runtimes.test.ts` exits 0
- [ ] `pnpm exec vitest run src/install/tests/verification.test.ts src/cli/commands/tests/regrade.test.ts` exits 0
- [ ] Installed-asset tests prove directory-loop regrade guidance survives packaging/install flows
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The installed runtime guidance and the implemented regrade directory-loop behavior are protected by regression coverage </success_criteria>

<output>
After completion, create `.planning/phases/04-runtime-guidance-and-regression-coverage/04-02-SUMMARY.md`
</output>
