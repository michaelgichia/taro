---
phase: 02-sequential-regrade-loop-and-history-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/commands/regrade-runner.ts
  - src/cli/commands/tests/regrade-runner.test.ts
  - src/core/state.ts
  - src/core/tests/state.test.ts
autonomous: true
requirements:
  - RGST-01
  - RGST-02
  - RGST-03
user_setup: []
must_haves:
  truths:
    - A reusable single-test regrade runner can score an existing RTL test file and append a fresh generated-test snapshot without going through the directory loop.
    - Latest matching history lookup reuses the repo’s normalized `testFile` comparison so seeded tracker thresholds and appended snapshots stay aligned.
    - Regrade persistence preserves unrelated history entries and still keeps only the latest 5 snapshots for the target test file.
  artifacts:
    - `src/cli/commands/regrade-runner.ts` contains the reusable single-file regrade execution boundary.
    - `src/cli/commands/tests/regrade-runner.test.ts` covers matched-history reuse and fresh-history initialization.
    - `src/core/tests/state.test.ts` covers trimming and persistence behavior where Phase 2 needs stronger guarantees.
  key_links:
    - The runner reuses `appendGeneratedTestRecord()` and `normalizeGeneratedTestHistoryPath()` rather than inventing a second persistence path.
    - The runner returns structured result data that Phase `02-02` can write into completed tracker rows without rescoring the file a second time.
---

<objective>
Build the reusable single-test regrade execution boundary that Phase 2 directory orchestration will call for each queued test file.

Purpose: The directory loop should delegate scoring and history writes to one reusable unit so single-file regrade semantics and batch regrade semantics cannot drift. Output: A helper that reads the current test file, finds the latest matching stored snapshot when present, scores the latest contents, appends a fresh history entry, and reports the result in a structured shape. </objective>

<execution_context> @$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md </execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-sequential-regrade-loop-and-history-persistence/02-RESEARCH.md
@.planning/phases/02-sequential-regrade-loop-and-history-persistence/02-VALIDATION.md
@src/core/state.ts
@src/core/state-paths.ts
@src/core/scorer.ts
@src/types/score.ts
@src/cli/commands/regrade.ts
@.codex/skills/@taro-test/rtl-regrade/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add latest-match lookup and persistence parameter reuse for regrade</name>
  <files>src/cli/commands/regrade-runner.ts, src/core/state.ts</files>
  <read_first>src/core/state.ts, src/core/state-paths.ts, src/types/state.ts, .codex/skills/@taro-test/rtl-regrade/SKILL.md</read_first>
  <action>Create a reusable helper in `src/cli/commands/regrade-runner.ts` that loads the latest matching `generatedTests` record for a test file using `normalizeGeneratedTestHistoryPath(projectRoot, testFile)` and derives the persistence metadata the new snapshot should reuse. When a match exists, preserve its `packagePath` and `recordingFile` when possible. When no match exists, fall back to the best package profile or `"."` and persist `recordingFile: null`. Extract or add any narrowly-scoped state helper only if it reduces duplication instead of leaking directory-loop concerns into `state.ts`.</action>
  <verify>
    <automated>pnpm exec vitest run src/cli/commands/tests/regrade-runner.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/cli/commands/regrade-runner.ts` performs latest-match lookup using `normalizeGeneratedTestHistoryPath(`.
    - The runner or extracted helper can distinguish matched-history reuse from first-time history initialization.
    - The implementation does not require `regrade.ts` to know how `packagePath` or `recordingFile` reuse is derived.
  </acceptance_criteria>
  <done>The batch loop will have a reusable way to obtain correct prior-history context per test file.</done>
</task>

<task type="auto">
  <name>Task 2: Implement single-file regrade scoring and snapshot append behavior</name>
  <files>src/cli/commands/regrade-runner.ts, src/core/state.ts</files>
  <read_first>src/cli/commands/regrade-runner.ts, src/core/scorer.ts, src/core/state.ts, src/types/score.ts</read_first>
  <action>Implement a `runRegradeForTestFile()`-style helper that reads an existing RTL test file, scores the latest contents with the repo’s real scoring surface, appends a fresh generated-test record, and returns a structured result containing the prior score context, the new score result, and follow-up-comment material for tracker output. Preserve unrelated history entries exactly and keep latest-5 trimming behavior for the target test file by routing persistence through the existing state pipeline instead of hand-editing `.taro/state.json`.</action>
  <verify>
    <automated>pnpm exec vitest run src/cli/commands/tests/regrade-runner.test.ts src/core/tests/state.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The runner appends a fresh generated-test snapshot for each successful regrade.
    - The appended snapshot reuses the latest matching metadata when available and initializes cleanly when there is no prior history.
    - The implementation does not duplicate latest-5 trimming logic outside the existing state persistence surface.
  </acceptance_criteria>
  <done>The repo has a correct, reusable single-file regrade worker that updates state history on the success path.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused tests for matched reuse, fresh initialization, and history trimming</name>
  <files>src/cli/commands/tests/regrade-runner.test.ts, src/core/tests/state.test.ts</files>
  <read_first>src/cli/commands/tests/regrade.test.ts, src/core/tests/state.test.ts, src/cli/commands/regrade-runner.ts</read_first>
  <action>Add focused Vitest coverage proving: the runner reuses the latest matching `generatedTests` record when present, it still appends the first snapshot when none exists, and repeated regrades for the same file keep only the latest 5 snapshots while preserving unrelated file history. Keep these assertions at the runner/state level instead of pushing them into the higher-cost directory-loop integration test.</action>
  <verify>
    <automated>pnpm exec vitest run src/cli/commands/tests/regrade-runner.test.ts src/core/tests/state.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/cli/commands/tests/regrade-runner.test.ts` contains a matched-history reuse assertion.
    - `src/cli/commands/tests/regrade-runner.test.ts` contains a no-prior-history assertion.
    - `src/core/tests/state.test.ts` or the new runner tests prove the target file still trims to the latest 5 snapshots after repeated regrades.
  </acceptance_criteria>
  <done>The single-file runner is guarded by direct tests for the exact history semantics Phase 2 promises.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm exec vitest run src/cli/commands/tests/regrade-runner.test.ts` exits 0
- [ ] `pnpm exec vitest run src/core/tests/state.test.ts` exits 0
- [ ] A runner-level test proves matched-history reuse and a fresh-history fallback both work
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo has a reusable single-file regrade runner that appends correct history records and preserves latest-5 trimming semantics </success_criteria>

<output>
After completion, create `.planning/phases/02-sequential-regrade-loop-and-history-persistence/02-01-SUMMARY.md`
</output>
