---
phase: 15-structured-suite-planning-repo-aware-generation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/suite-planner.ts
  - src/core/suite-planner.test.ts
  - src/types/recording.ts
autonomous: true
requirements:
  - SUITE-01
  - SUITE-02

must_haves:
  truths:
    - "JS suite planning models meaningful scenarios and helper boundaries instead of treating recorder groups as finished tests"
    - "Multi-test output is only allowed when Taro can prove the required UI state can be recreated safely per scenario"
    - "Helper planning rules keep load-bearing assertions in test bodies instead of burying them inside setup helpers"
  artifacts:
    - path: "src/core/suite-planner.ts"
      provides: "Structured suite-plan contract with scenario, helper, and state-safety decisions"
      contains: "planJsSuite"
    - path: "src/core/suite-planner.test.ts"
      provides: "Unit coverage for scenario splitting, helper boundaries, and state-safe degradation"
      contains: "planJsSuite"
    - path: "src/types/recording.ts"
      provides: "Shared types needed to carry suite-plan metadata into later generation steps"
      contains: "ItGroup"
  key_links:
    - from: "src/core/suite-planner.ts"
      to: "src/core/mock-intelligence.ts"
      via: "state-safety and mutation lifecycle evidence"
      pattern: "mutationLifecycles"
    - from: "src/core/suite-planner.ts"
      to: "sample/sample-add-sale-test.tsx"
      via: "gold-standard scenario/helper boundaries"
      pattern: "SalesModule"
---

<objective>
Create the suite-planning contract for Phase 15.

Purpose: replace the current warning-only planner with structured scenario, helper, and state-safety decisions that later generation can realize into maintainable RTL suites.
Output: a richer suite-plan artifact plus unit tests that prove when flows must stay single-flow and when they may split into multiple scenarios safely.
</objective>

<execution_context>
@/Users/michaelgichia/.codex/get-shit-done/workflows/execute-plan.md
@/Users/michaelgichia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-structured-suite-planning-repo-aware-generation/15-RESEARCH.md
@.planning/phases/15-structured-suite-planning-repo-aware-generation/15-VALIDATION.md
@sample/AddSaleForm.test.tsx
@sample/sample-add-sale-test.tsx
@src/core/suite-planner.ts
@src/core/suite-planner.test.ts
@src/core/mock-intelligence.ts
@src/types/recording.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace warning-only suite planning with structured scenario/state-safety decisions</name>
  <files>src/core/suite-planner.ts, src/types/recording.ts</files>
  <action>
Extend the JS suite planner so it emits an explicit plan artifact instead of only `itGroups` plus warnings.

Required outcomes:
1. Model scenario candidates, helper/navigation candidates, render-boundary intent, and state-safety results in one structure that generation can consume later.
2. Distinguish between flows that may split into multiple `it(...)` blocks and flows that must stay single-flow because the state cannot be recreated safely.
3. Preserve explicit degraded states when Taro still lacks enough evidence to split safely or resolve the exact owning boundary.
4. Keep the planner compatible with truthful selector recovery from Phase 14; unresolved selector checkpoints remain valid inputs to the suite plan.

Do not yet emit code from this richer plan in this task; focus on the planning contract and decision rules.
  </action>
  <verify>
    <automated>npm run build && npm run test:run -- src/core/suite-planner.test.ts</automated>
  </verify>
  <done>
    The repo has a structured suite-plan artifact that makes state-safety and scenario-boundary decisions explicit before code generation starts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Lock helper and scenario-boundary rules with focused unit coverage</name>
  <files>src/core/suite-planner.test.ts</files>
  <action>
Add suite-planner tests that prove the Phase 15 boundary rules.

Required outcomes:
1. Cover wizard-style flows with mutation lifecycles and repeated mock targets, proving they stay single-flow unless state recreation is explicitly safe.
2. Cover simpler flows that may split into multiple user-facing scenarios with helper extraction.
3. Cover helper planning rules that forbid hiding meaningful assertions inside helpers while still allowing synchronization/setup steps.
4. Cover degraded states where the planner can identify a module boundary preference but not an exact render target yet.

Do not duplicate generator or CLI assertions here; keep this plan planner-focused.
  </action>
  <verify>
    <automated>npm run build && npm run test:run -- src/core/suite-planner.test.ts</automated>
  </verify>
  <done>
    Planner tests prove Phase 15 can tell the difference between safe scenario splitting and a flow that must remain one coordinated interaction path.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds.
2. `npm run test:run -- src/core/suite-planner.test.ts` succeeds.
3. Suite planning now exposes explicit scenario/helper/state-safety decisions instead of warning-only heuristics.
4. Unsafe multi-test splits degrade explicitly rather than silently producing brittle suites.
</verification>

<success_criteria>
- Structured suite-plan artifact exists
- State-safe vs unsafe multi-test decisions are explicit
- Helper boundaries are modeled without burying assertions
- Unit coverage locks the planning policy in place
</success_criteria>

<output>
After completion, create `.planning/phases/15-structured-suite-planning-repo-aware-generation/15-01-SUMMARY.md` documenting the suite-plan contract and state-safety decision rules.
</output>
