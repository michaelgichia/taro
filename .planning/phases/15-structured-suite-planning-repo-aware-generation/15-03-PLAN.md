---
phase: 15-structured-suite-planning-repo-aware-generation
plan: "03"
type: execute
wave: 3
depends_on:
  - 15-01
  - 15-02
files_modified:
  - src/cli/commands/generate.test.ts
  - src/core/boundary-intelligence.test.ts
  - src/core/generator.test.ts
  - sample/AddSaleForm.test.tsx
  - sample/sample-add-sale-test.tsx
autonomous: true
requirements:
  - SUITE-01
  - SUITE-03
  - SUITE-04

must_haves:
  truths:
    - "The Add Sale JS path moves toward the `SalesModule` gold standard instead of remaining a leaf-form boundary draft"
    - "Boundary and generator regressions prove helper/assertion separation, scoped queries, and real render-target output for supported flows"
    - "When repo-aware proof is missing, Tayo keeps explicit draft behavior rather than regressing back to hidden anti-patterns"
  artifacts:
    - path: "src/cli/commands/generate.test.ts"
      provides: "Sample-backed CLI regression coverage for repo-aware suite output"
      contains: "sample-rest-recordingextension-output"
    - path: "src/core/boundary-intelligence.test.ts"
      provides: "Oracle coverage that the gold-standard sample remains boundary-safe while anti-pattern samples still fail"
      contains: "sample-add-sale-test"
    - path: "src/core/generator.test.ts"
      provides: "Generator-level proof for helper/assertion split and render-target emission"
      contains: "generateTestFromGroups"
  key_links:
    - from: "src/cli/commands/generate.test.ts"
      to: "sample/sample-add-sale-test.tsx"
      via: "gold-standard output shape"
      pattern: "SalesModule"
    - from: "src/core/boundary-intelligence.test.ts"
      to: "sample/AddSaleForm.test.tsx"
      via: "anti-pattern regression oracle"
      pattern: "AddSaleForm"
---

<objective>
Prove that Phase 15 meaningfully improves the shipped JS suite shape.

Purpose: finish Phase 15 with sample-backed regression proof that supported flows generate closer to the repo gold standard while unsupported paths remain explicit drafts instead of hidden anti-patterns.
Output: boundary, generator, and CLI regressions that protect the new suite structure against backsliding.
</objective>

<execution_context>
@/Users/michaelgichia/.codex/taro/workflows/execute-plan.md
@/Users/michaelgichia/.codex/taro/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-structured-suite-planning-repo-aware-generation/15-RESEARCH.md
@.planning/phases/15-structured-suite-planning-repo-aware-generation/15-VALIDATION.md
@sample/AddSaleForm.test.tsx
@sample/sample-add-sale-test.tsx
@sample/sample-rest-recordingextension-output.js
@src/cli/commands/generate.test.ts
@src/core/boundary-intelligence.test.ts
@src/core/generator.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add gold-standard regression coverage for repo-aware Add Sale output</name>
  <files>src/cli/commands/generate.test.ts, src/core/generator.test.ts, sample/sample-add-sale-test.tsx</files>
  <action>
Use the Add Sale gold standard as the main regression oracle for Phase 15.

Required outcomes:
1. Add assertions that supported Add Sale generation resolves a real module render target instead of `render(<App />)`.
2. Add assertions that generated helpers avoid embedded assertions and that scoped queries are used where the flow requires them.
3. Keep the gold-standard sample as the reference point for desired structure without requiring byte-for-byte identical output.

Do not weaken Phase 14 selector-truth regressions while adding these structural expectations.
  </action>
  <verify>
    <automated>npm run build && npm run test:run -- src/cli/commands/generate.test.ts src/core/generator.test.ts</automated>
  </verify>
  <done>
    The Add Sale sample proves Phase 15 can generate repo-aware suites that move materially closer to the gold standard.
  </done>
</task>

<task type="auto">
  <name>Task 2: Preserve explicit draft behavior when repo-aware proof is insufficient</name>
  <files>src/cli/commands/generate.test.ts, src/core/boundary-intelligence.test.ts, sample/AddSaleForm.test.tsx</files>
  <action>
Add regression coverage for the negative path so Phase 15 does not overclaim repo awareness.

Required outcomes:
1. Add tests proving unsupported or weak-evidence flows still surface explicit draft/boundary warnings rather than silently choosing the wrong target.
2. Keep the anti-pattern AddSaleForm sample detectable by boundary intelligence so the oracle still distinguishes bad from good structure.
3. Ensure Phase 15 improvements do not reintroduce inline hook mocks, helper-embedded assertions, or positional selection as acceptable defaults.

Do not re-score the milestone here; Phase 16 owns final scoring and public proof.
  </action>
  <verify>
    <automated>npm run build && npm run test:run -- src/cli/commands/generate.test.ts src/core/boundary-intelligence.test.ts</automated>
  </verify>
  <done>
    Phase 15 has regression proof for both the supported repo-aware path and the explicit-draft fallback path.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds.
2. `npm run test:run -- src/cli/commands/generate.test.ts src/core/boundary-intelligence.test.ts src/core/generator.test.ts` succeeds.
3. Supported Add Sale output moves toward the `SalesModule` gold standard and away from the AddSaleForm anti-patterns.
4. Weak-evidence flows still degrade explicitly instead of silently choosing brittle boundaries.
</verification>

<success_criteria>
- Add Sale regression proves real structural improvement
- Boundary oracle still distinguishes bad and good suite structure
- Supported and degraded paths are both protected by tests
- Phase 14 selector truth remains intact while structure improves
</success_criteria>

<output>
After completion, create `.planning/phases/15-structured-suite-planning-repo-aware-generation/15-03-SUMMARY.md` documenting the gold-standard regression proof and explicit-draft fallback coverage.
</output>
