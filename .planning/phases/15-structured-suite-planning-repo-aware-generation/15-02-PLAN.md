---
phase: 15-structured-suite-planning-repo-aware-generation
plan: "02"
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - src/cli/commands/generate.ts
  - src/core/generator.ts
  - src/templates/test-template.ts
  - src/core/scanner.ts
  - src/core/mock-intelligence.ts
  - src/core/generator.test.ts
  - src/cli/commands/generate.test.ts
autonomous: true
requirements:
  - SUITE-03
  - SUITE-04

must_haves:
  truths:
    - "Repo evidence can resolve a real render target, imports, and shared mock strategy for supported flows instead of leaving `render(<App />)` placeholders"
    - "Generated helpers, `userEvent.setup()`, and query scopes follow repo-aware suite-plan decisions rather than a generic template"
    - "When repo evidence is insufficient, Tayo degrades explicitly without pretending it resolved a concrete module boundary"
  artifacts:
    - path: "src/cli/commands/generate.ts"
      provides: "JS generation orchestration that reads the richer suite-plan and repo-aware render-target decisions"
      contains: "createGenerateCommand"
    - path: "src/core/generator.ts"
      provides: "Code emission for repo-aware imports, render targets, helper functions, and scoped queries"
      contains: "generateTestFromGroups"
    - path: "src/templates/test-template.ts"
      provides: "Template support for non-placeholder render setup and helper-oriented multi-test output"
      contains: "describeBlockMultiIt"
    - path: "src/core/generator.test.ts"
      provides: "Generator coverage for repo-aware render targets and helper/scoping output"
      contains: "generateTestFromGroups"
  key_links:
    - from: "src/cli/commands/generate.ts"
      to: "src/core/suite-planner.ts"
      via: "structured suite-plan handoff"
      pattern: "planJsSuite"
    - from: "src/cli/commands/generate.ts"
      to: "src/core/scanner.ts"
      via: "repo convention evidence"
      pattern: "scanConventions"
    - from: "src/cli/commands/generate.ts"
      to: "src/core/mock-intelligence.ts"
      via: "shared mock recommendations"
      pattern: "analyzeMocks"
---

<objective>
Turn the structured suite plan into repo-aware generated code.

Purpose: replace placeholder `render(<App />)` output with real render targets, imports, helper structure, mock reuse, and query scoping when the repo provides enough evidence.
Output: shipped generation code that can realize supported JS flows as module-aware RTL suites instead of boundary drafts.
</objective>

<execution_context>
@/Users/michaelgichia/.codex/get-shit-done/workflows/execute-plan.md
@/Users/michaelgichia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-structured-suite-planning-repo-aware-generation/15-RESEARCH.md
@.planning/phases/15-structured-suite-planning-repo-aware-generation/15-VALIDATION.md
@sample/sample-add-sale-test.tsx
@src/cli/commands/generate.ts
@src/core/generator.ts
@src/templates/test-template.ts
@src/core/scanner.ts
@src/core/mock-intelligence.ts
@src/core/generator.test.ts
@src/cli/commands/generate.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve repo-aware render targets, imports, and shared mock strategy</name>
  <files>src/cli/commands/generate.ts, src/core/scanner.ts, src/core/mock-intelligence.ts</files>
  <action>
Add the repo-aware realization layer that turns suite-plan intent into concrete test targets.

Required outcomes:
1. Use repo evidence to resolve a likely module/container render target and import source for supported flows such as the Add Sale sample.
2. Use discovered conventions and repeated mock evidence to recommend or emit shared mocks/helpers instead of inline hook mocks where the repo clearly does so.
3. Preserve explicit degraded states when the repo does not provide enough evidence to resolve a trustworthy target.
4. Keep JSON input behavior stable; this plan only upgrades the JS-derived generation path.

Do not finalize helper code emission here; focus on resolving concrete repo-aware inputs for the generator layer.
  </action>
  <verify>
    <automated>npm run build && npm run test:run -- src/cli/commands/generate.test.ts</automated>
  </verify>
  <done>
    Tayo can resolve repo-aware generation inputs for supported flows instead of stopping at generic boundary warnings.
  </done>
</task>

<task type="auto">
  <name>Task 2: Emit repo-aware helpers, render setup, and scoped queries in generated tests</name>
  <files>src/core/generator.ts, src/templates/test-template.ts, src/core/generator.test.ts, src/cli/commands/generate.test.ts</files>
  <action>
Update JS test emission to consume the richer suite plan and repo-aware resolution.

Required outcomes:
1. Replace placeholder `render(<App />)` setup with the resolved render target/import pair for supported flows.
2. Emit helper functions that perform navigation/setup while keeping load-bearing assertions in the test body.
3. Emit scoped queries such as `within(dialog)` when the suite plan identifies repeated controls or step-local containers.
4. Keep degraded output explicit when repo-aware realization is still incomplete, rather than emitting a fake concrete target.

Do not try to finalize milestone-level scoring here; Phase 16 owns scoring calibration.
  </action>
  <verify>
    <automated>npm run build && npm run test:run -- src/core/generator.test.ts src/cli/commands/generate.test.ts</automated>
  </verify>
  <done>
    Generated JS tests can now render a real repo target with helper-oriented structure and scoped queries for supported flows.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds.
2. `npm run test:run -- src/core/generator.test.ts src/cli/commands/generate.test.ts` succeeds.
3. Supported JS flows no longer emit placeholder `render(<App />)` output.
4. Repo-aware output keeps assertions in test bodies and uses scoped queries/helpers where the repo evidence supports them.
</verification>

<success_criteria>
- Repo-aware render target resolution exists
- Generator can emit real imports/render setup
- Helper and scoped-query output follow suite-plan decisions
- Unsupported paths still degrade explicitly instead of overclaiming confidence
</success_criteria>

<output>
After completion, create `.planning/phases/15-structured-suite-planning-repo-aware-generation/15-02-SUMMARY.md` documenting repo-aware render-target resolution and helper-oriented JS generation.
</output>
