---
phase: 17-semantic-marker-intake
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/recording.ts
  - src/core/js-parser.ts
  - src/core/js-parser.test.ts
  - src/core/input-loader.ts
  - src/core/input-loader.test.ts
autonomous: true
requirements:
  - MARK-01
  - MARK-02

must_haves:
  truths:
    - "Recorder JS `dblClick` steps survive intake as semantic marker candidates instead of flattening immediately into ordinary click noise"
    - "Each candidate keeps the recorder context later phases need: step identity, source line, original gesture, recovered query or selector evidence, and proof-target text"
    - "Shared JS input loading remains compatible for ordinary interactions that are not semantic marker candidates"
  artifacts:
    - path: "src/types/recording.ts"
      provides: "Marker candidate and anchor metadata contracts that downstream phases can consume without rediscovering intent"
      contains: "SemanticMarkerCandidate"
    - path: "src/core/js-parser.ts"
      provides: "Recorder-JS parsing that preserves `dblClick` verification intent as structured metadata on parsed steps"
      contains: "parseJsRecording"
    - path: "src/core/input-loader.ts"
      provides: "Shared parsed-input envelope that carries marker-aware JS baseline metadata into the rest of the pipeline"
      contains: "loadInput"
    - path: "src/core/js-parser.test.ts"
      provides: "Sample-backed parser regressions for qualifying and non-qualifying recorder marker gestures"
      contains: "sample-rest-recordingextension-output"
  key_links:
    - from: "src/core/js-parser.ts"
      to: "src/types/recording.ts"
      via: "marker candidate metadata attached to parsed JS steps"
      pattern: "SemanticMarkerCandidate"
    - from: "src/core/input-loader.ts"
      to: "src/core/js-parser.ts"
      via: "shared input loading that preserves marker-aware baseline data"
      pattern: "parseJsRecording"
---

<objective>
Establish the marker-aware JS intake contract for Phase 17.

Purpose: preserve recorder `dblClick` verification intent as structured baseline data at the earliest safe boundary so later plans can classify and attach it without reconstructing raw recorder context.
Output: marker-aware recording types, parser-side candidate preservation, and shared-input regressions proving the metadata survives loading.
</objective>

<execution_context>
@/Users/michaelgichia/.codex/get-shit-done/workflows/execute-plan.md
@/Users/michaelgichia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-semantic-marker-intake/17-CONTEXT.md
@src/types/recording.ts
@src/core/js-parser.ts
@src/core/input-loader.ts
@src/core/js-parser.test.ts
@src/core/input-loader.test.ts
@sample/sample-rest-recordingextension-output.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define semantic marker candidate contracts for JS intake</name>
  <files>src/types/recording.ts, src/core/input-loader.ts</files>
  <action>
Extend the shared recording contracts so Phase 17 has an explicit place to store marker intent.

Required outcomes:
1. Add a marker-aware metadata contract for parsed JS steps that can represent candidate status, proof subject classification, source line context, and optional anchor linkage placeholders.
2. Preserve compatibility with existing `NormalizedStep`, `JsBaselineMetadata`, and `ParsedInput` consumers so JSON input and non-marker JS steps do not regress.
3. Model unresolved marker candidates explicitly rather than requiring later phases to infer absence from missing data.
4. Keep the contract intake-focused only; do not add assertion-generation shapes that belong to Phase 18.
  </action>
  <verify>
    <automated>npm run build && npm run test:run -- src/core/input-loader.test.ts src/core/js-parser.test.ts</automated>
  </verify>
  <done>
    The shared recording contract can carry semantic marker candidates end-to-end from JS parse output without breaking existing input loading.
  </done>
</task>

<task type="auto">
  <name>Task 2: Preserve `dblClick` proof candidates in the JS parser and shared input boundary</name>
  <files>src/core/js-parser.ts, src/core/js-parser.test.ts, src/core/input-loader.test.ts</files>
  <action>
Refactor the recorder-JS parser so `userEvent.dblClick(...)` can be recognized later as verification intent instead of being treated as an undifferentiated click.

Required outcomes:
1. Preserve the original recorder gesture, step id, recovered query or selector evidence, and proof-target text for `dblClick` steps in `parseJsRecording()`.
2. Use the existing sample recorder export plus focused snippets to prove the parser preserves proof-like targets such as headings, visible messages, and concrete values without reclassifying ordinary `click` steps.
3. Add regressions showing field-label `dblClick` steps remain distinguishable as non-qualifying candidates rather than being silently upgraded into proof.
4. Keep the shared input boundary loading the new marker-aware metadata automatically for JS inputs, with no behavior change for JSON.
  </action>
  <verify>
    <automated>npm run build && npm run test:run -- src/core/js-parser.test.ts src/core/input-loader.test.ts</automated>
  </verify>
  <done>
    JS recordings enter the pipeline with enough preserved `dblClick` evidence for later plans to qualify markers and attach them to user actions truthfully.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds.
2. `npm run test:run -- src/core/js-parser.test.ts src/core/input-loader.test.ts` succeeds.
3. Recorder-JS `dblClick` steps preserve marker candidate metadata instead of degrading into generic click noise at intake.
4. Non-marker JS actions and all JSON inputs continue through the shared loader boundary without regression.
</verification>

<success_criteria>
- Marker candidate contracts exist at the shared input boundary
- Parser preserves `dblClick` proof evidence and source context
- Non-qualifying proof subjects remain distinguishable instead of being upgraded implicitly
- Shared input loading remains stable for existing JSON and JS flows
</success_criteria>

<output>
After completion, create `.planning/phases/17-semantic-marker-intake/17-01-SUMMARY.md` documenting the semantic-marker intake contract and JS parser preservation changes.
</output>
