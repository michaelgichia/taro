---
phase: 17-semantic-marker-intake
plan: "02"
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - src/types/recording.ts
  - src/core/recording-intelligence.ts
  - src/core/recording-intelligence.test.ts
autonomous: true
requirements:
  - MARK-01
  - MARK-02

must_haves:
  truths:
    - "Qualifying proof-like `dblClick` steps on visible headings, messages, and concrete values are treated as semantic markers instead of being removed as routine dblClick noise"
    - "Each preserved marker is attached to the nearest prior major transition step, or is kept as an explicit unresolved marker candidate when no valid anchor exists"
    - "Marker-aware cleanup preserves truly interactive trailing clicks only when the same-target click is a real control interaction, not a proof target"
  artifacts:
    - path: "src/core/recording-intelligence.ts"
      provides: "Marker-aware cleanup, qualification, anchor linkage, and diagnostics for analyzed recordings"
      contains: "analyzeRecording"
    - path: "src/types/recording.ts"
      provides: "Normalized marker-link and unresolved-marker contracts shared by analysis and downstream phases"
      contains: "SemanticMarkerLink"
    - path: "src/core/recording-intelligence.test.ts"
      provides: "Regression coverage for marker qualification, anchor selection, unresolved handling, and paired click behavior"
      contains: "filterNoiseSteps"
  key_links:
    - from: "src/core/recording-intelligence.ts"
      to: "src/types/recording.ts"
      via: "marker-link and unresolved-marker analysis contract"
      pattern: "SemanticMarkerLink"
    - from: "src/core/recording-intelligence.ts"
      to: "src/core/js-parser.ts"
      via: "marker qualification built on preserved recorder-JS gesture metadata"
      pattern: "originalType"
---

<objective>
Replace blanket dblClick cleanup with semantic marker intake and action linkage.

Purpose: classify preserved JS marker candidates according to the Phase 17 rules, attach them to the user action they verify, and keep unresolved proof gestures visible instead of silently discarding them.
Output: marker-aware analyzed recordings, deterministic anchor linkage, and regressions for qualification and pair-handling rules.
</objective>

<execution_context>
@/Users/michaelgichia/.codex/taro/workflows/execute-plan.md
@/Users/michaelgichia/.codex/taro/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-semantic-marker-intake/17-CONTEXT.md
@src/types/recording.ts
@src/core/js-parser.ts
@src/core/recording-intelligence.ts
@src/core/recording-intelligence.test.ts
@sample/sample-rest-recordingextension-output.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Qualify proof-like `dblClick` steps and attach them to major transition anchors</name>
  <files>src/core/recording-intelligence.ts, src/types/recording.ts, src/core/recording-intelligence.test.ts</files>
  <action>
Refactor recording cleanup and analysis so Phase 17 recognizes semantic markers as verification intent rather than treating every `dblClick` as noise.

Required outcomes:
1. Qualify marker candidates only on the JS path and only when the `dblClick` immediately follows a significant UI transition and points at a visible proof subject such as a heading, user-facing message, or concrete value.
2. Exclude routine field labels, field names, and similar non-proof text from marker qualification in this phase even if they use `dblClick`.
3. Link each qualified marker to the nearest prior major transition step such as open, continue, submit, save, or a consistent state-changing equivalent.
4. Preserve proof-like candidates with no clear valid anchor as unresolved marker candidates instead of dropping them or fabricating an attachment.
5. Keep Chrome Recorder JSON behavior unchanged in this phase.
  </action>
  <verify>
    <automated>npm run build && npm run test:run -- src/core/recording-intelligence.test.ts</automated>
  </verify>
  <done>
    Analyzed recordings now distinguish real semantic markers from ordinary dblClick noise and attach them to the user action they verify when truthful evidence exists.
  </done>
</task>

<task type="auto">
  <name>Task 2: Make click-cluster cleanup marker-aware and keep diagnostics truthful</name>
  <files>src/core/recording-intelligence.ts, src/core/recording-intelligence.test.ts</files>
  <action>
Update click clustering so marker gestures are preserved correctly when recorder exports emit a same-target `dblClick` plus `click` pair.

Required outcomes:
1. When a proof-like marker `dblClick` is followed immediately by a same-target `click`, preserve marker intent instead of collapsing the pair into generic click noise.
2. Keep the trailing click as a real interaction only when the target evidence indicates a genuinely interactive control; non-interactive proof targets should not survive as ordinary user actions.
3. Extend diagnostics so preserved markers and unresolved marker candidates are distinguishable from removed pure dblClick noise.
4. Add regression coverage for paired marker/click exports, invalid proof targets, and unresolved-anchor cases using the existing recorder sample plus focused fixtures.
  </action>
  <verify>
    <automated>npm run build && npm run test:run -- src/core/recording-intelligence.test.ts</automated>
  </verify>
  <done>
    Marker-aware cleanup keeps proof intent truthful, preserves only legitimate follow-up clicks, and reports what happened clearly.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds.
2. `npm run test:run -- src/core/recording-intelligence.test.ts` succeeds.
3. Qualifying JS `dblClick` proof gestures are preserved as semantic markers with valid anchor linkage or unresolved-marker state.
4. Non-interactive same-target trailing clicks no longer survive as ordinary user actions when they only exist to accompany marker proof.
5. JSON cleanup behavior stays unchanged for Phase 17.
</verification>

<success_criteria>
- Marker qualification follows the Phase 17 proof-subject rules
- Anchor linkage prefers prior major transitions and avoids routine field edits
- Unresolved marker candidates remain explicit
- Click-pair cleanup is marker-aware and diagnostically truthful
</success_criteria>

<output>
After completion, create `.planning/phases/17-semantic-marker-intake/17-02-SUMMARY.md` documenting the marker-qualification heuristics, anchor-linking rules, and marker-aware cleanup behavior.
</output>
