---
phase: 17-semantic-marker-intake
plan: "03"
type: execute
wave: 3
depends_on:
  - 17-02
files_modified:
  - src/core/baseline-normalizer.ts
  - src/core/suite-planner.ts
  - src/core/suite-planner.test.ts
  - src/cli/commands/generate.ts
  - src/cli/commands/generate.test.ts
autonomous: true
requirements:
  - MARK-02

must_haves:
  truths:
    - "Semantic marker linkage survives baseline normalization, suite planning, and generate-path rehydration without being converted into assertions yet"
    - "Marker proof stays attached to the action it verifies even when steps are regrouped into intent groups, helpers, and planned scenarios"
    - "Public cleanup reporting distinguishes preserved semantic markers from removed dblClick noise so the user-facing pipeline stays truthful"
  artifacts:
    - path: "src/core/baseline-normalizer.ts"
      provides: "Baseline rehydration that preserves marker linkage and unresolved marker metadata on rebuilt steps"
      contains: "normalizeJsBaseline"
    - path: "src/core/suite-planner.ts"
      provides: "Scenario and helper planning that retains marker-to-action linkage for later assertion conversion"
      contains: "planJsSuite"
    - path: "src/cli/commands/generate.ts"
      provides: "Public generate-path preservation and marker-aware cleanup summary without Phase 18 assertion emission"
      contains: "summarizeCleanup"
    - path: "src/core/suite-planner.test.ts"
      provides: "Regression coverage proving marker-linked steps remain attached after regrouping into helpers and scenarios"
      contains: "planJsSuite"
    - path: "src/cli/commands/generate.test.ts"
      provides: "Public-flow proof that preserved markers survive generation planning while non-interactive proof clicks do not"
      contains: "createGenerateCommand"
  key_links:
    - from: "src/core/baseline-normalizer.ts"
      to: "src/core/recording-intelligence.ts"
      via: "marker metadata rehydration after analysis and selector recovery"
      pattern: "metadata"
    - from: "src/cli/commands/generate.ts"
      to: "src/core/suite-planner.ts"
      via: "marker-aware suite planning and rehydration across the public pipeline"
      pattern: "planJsSuite"
---

<objective>
Keep semantic marker linkage alive through the downstream JS pipeline.

Purpose: ensure the marker evidence and action attachment established earlier in Phase 17 survive regrouping, planning, and public generate flow rehydration so Phase 18 can convert markers without reopening intake logic.
Output: marker-aware baseline normalization, suite planning retention, generate-path compatibility, and end-to-end regressions for downstream preservation.
</objective>

<execution_context>
@/Users/michaelgichia/.codex/get-shit-done/workflows/execute-plan.md
@/Users/michaelgichia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-semantic-marker-intake/17-CONTEXT.md
@src/core/baseline-normalizer.ts
@src/core/suite-planner.ts
@src/core/suite-planner.test.ts
@src/cli/commands/generate.ts
@src/cli/commands/generate.test.ts
@src/core/recording-intelligence.ts
@sample/sample-rest-recordingextension-output.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preserve marker linkage through baseline normalization and suite planning</name>
  <files>src/core/baseline-normalizer.ts, src/core/suite-planner.ts, src/core/suite-planner.test.ts</files>
  <action>
Thread semantic marker metadata through the downstream JS planning layers without taking over assertion generation.

Required outcomes:
1. Rehydrate marker metadata whenever steps are rebuilt from baseline evidence so anchor linkage and unresolved-marker state remain available after selector recovery or step remapping.
2. Preserve the marker-to-action relationship inside intent groups, helper plans, and scenario plans so later assertion emission can place the verification on the correct user flow.
3. Keep marker steps additive metadata only in Phase 17; do not emit assertions or mutate scenario structure solely to satisfy marker conversion early.
4. Add suite-planner regressions proving preserved markers stay attached after regrouping into helpers and scenarios.
  </action>
  <verify>
    <automated>npm run build && npm run test:run -- src/core/suite-planner.test.ts</automated>
  </verify>
  <done>
    Downstream planning surfaces still know which user action each semantic marker verifies, even after regrouping and step rehydration.
  </done>
</task>

<task type="auto">
  <name>Task 2: Keep the public generate path marker-aware without converting markers yet</name>
  <files>src/cli/commands/generate.ts, src/cli/commands/generate.test.ts</files>
  <action>
Update the public JS pipeline so preserved semantic markers stay truthful and visible through generation planning.

Required outcomes:
1. Preserve marker metadata through `loadInput` → `normalizeJsBaseline` → `analyzeRecording` → `planJsSuite` → `rehydrateSuitePlan` without dropping the action attachment.
2. Adjust cleanup reporting so preserved semantic markers are no longer described as removed dblClick noise, while unresolved markers remain visible for later handling.
3. Add a public-flow JS regression proving non-interactive proof markers do not survive as ordinary clicks even though the linked marker metadata remains available downstream.
4. Leave assertion conversion, unresolved-marker warnings, and marker coverage scoring to later phases.
  </action>
  <verify>
    <automated>npm run build && npm run test:run -- src/core/suite-planner.test.ts src/cli/commands/generate.test.ts</automated>
  </verify>
  <done>
    The shipped generate path preserves Phase 17 marker linkage truthfully and stays ready for Phase 18 conversion work.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds.
2. `npm run test:run -- src/core/suite-planner.test.ts src/cli/commands/generate.test.ts` succeeds.
3. Marker linkage survives baseline normalization, scenario planning, and generate-path rehydration.
4. Public cleanup output no longer misreports preserved semantic markers as discarded dblClick noise.
5. No marker-derived assertions are emitted yet; Phase 18 still owns conversion.
</verification>

<success_criteria>
- Marker attachment survives downstream JS planning layers
- Helpers and scenarios retain enough linkage for later assertion placement
- Public generate flow remains truthful about preserved markers versus removed noise
- Phase 17 stops at intake and preservation, not assertion emission
</success_criteria>

<output>
After completion, create `.planning/phases/17-semantic-marker-intake/17-03-SUMMARY.md` documenting downstream marker preservation through baseline normalization, suite planning, and the public generate path.
</output>
