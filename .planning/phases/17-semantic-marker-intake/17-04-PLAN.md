---
phase: 17-semantic-marker-intake
plan: "04"
type: execute
wave: 4
depends_on:
  - 17-03
files_modified:
  - src/core/recording-intelligence.ts
  - src/core/recording-intelligence.test.ts
autonomous: true
requirements:
  - MARK-01
  - MARK-02
gap_closure: true

must_haves:
  truths:
    - "State-changing opener actions such as `Add Sale (Invoice)` qualify as valid marker anchors instead of leaving the immediately following proof marker unresolved"
    - "Backward marker-anchor resolution keeps scanning past intervening non-anchor steps until it finds the nearest earlier valid state-changing transition, or truthfully leaves the marker unresolved"
    - "Automated regressions exercise the representative recorder sample so the opener marker and later review-flow proof markers fail fast if attachment regresses again"
  artifacts:
    - path: "src/core/recording-intelligence.ts"
      provides: "Broader major-transition classification and resilient backward anchor search for semantic markers"
      contains: "findNearestPriorMajorTransitionStep"
    - path: "src/core/recording-intelligence.test.ts"
      provides: "Sample-backed regression coverage for opener-anchor and later review-flow marker attachment"
      contains: "sample-rest-recordingextension-output"
  key_links:
    - from: "src/core/recording-intelligence.ts"
      to: "src/core/recording-intelligence.test.ts"
      via: "anchor heuristics and backward search proven against representative recorder markers"
      pattern: "Add Sale \\(Invoice\\)"
    - from: "src/core/recording-intelligence.test.ts"
      to: "sample/sample-rest-recordingextension-output.js"
      via: "runtime-equivalent sample assertions for real recorder anchor behavior"
      pattern: "john.doe@namiri\\.tech"
---

<objective>
Close the verified Phase 17 attachment gaps without reopening already-passed intake or downstream-preservation work.

Purpose: broaden marker anchor recognition enough to attach real recorder proof markers to the state-changing action they verify, then lock the representative sample behavior into automated regressions.
Output: improved anchor classification and backward-search behavior in recording intelligence, plus sample-backed tests that fail on the verified regressions.
</objective>

<execution_context>
@/Users/michaelgichia/.codex/taro/workflows/execute-plan.md
@/Users/michaelgichia/.codex/taro/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-semantic-marker-intake/17-CONTEXT.md
@.planning/phases/17-semantic-marker-intake/17-VERIFICATION.md
@.planning/phases/17-semantic-marker-intake/17-02-PLAN.md
@src/core/recording-intelligence.ts
@src/core/recording-intelligence.test.ts
@sample/sample-rest-recordingextension-output.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Broaden valid anchor recognition and make backward search resilient</name>
  <files>src/core/recording-intelligence.ts</files>
  <action>
Refactor marker attachment so Phase 17 resolves real recorder proof markers to the state-changing action that opened or advanced the UI instead of relying on the current narrow hard-coded transition list.

Required outcomes:
1. Expand major-transition recognition beyond the current limited verbs so opener and review-flow actions such as `Add Sale (Invoice)` and similar state-changing transitions count as valid anchors when they plausibly change visible UI state.
2. Keep the heuristic truthful: routine fills, selects, field edits, and other non-transition steps must still be excluded as anchors.
3. Update backward anchor search so it does not stop at the first prior non-sync step that is not itself an anchor; it should continue scanning backward until it finds the nearest earlier valid state-changing transition or exhausts the current flow.
4. Preserve explicit unresolved-marker behavior when no valid earlier transition exists rather than attaching to a weak or fabricated anchor.
5. Limit changes to Phase 17 attachment heuristics only; do not introduce Phase 18 assertion emission or extra downstream planning behavior.
  </action>
  <verify>
    <automated>npm run build && npm run test:run -- src/core/recording-intelligence.test.ts</automated>
  </verify>
  <done>
    Representative recorder markers now attach to truthful state-changing anchors, while genuinely unanchored proof markers remain unresolved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add sample-backed regressions for the verified failing anchor cases</name>
  <files>src/core/recording-intelligence.test.ts</files>
  <action>
Add focused regression coverage that reproduces the exact failures called out in `17-VERIFICATION.md` so future heuristic regressions fail in CI instead of only during manual runtime checks.

Required outcomes:
1. Add a representative-sample test proving the heading marker for `Add Sale (Invoice)` attaches to the triggering click step instead of staying `unresolved` with `missing-anchor`.
2. Add sample-backed coverage proving later review-flow proof markers, including the visible phone number and email markers identified in verification, can scan backward past intervening non-anchor steps and attach to the nearest earlier valid state-changing transition.
3. Keep at least one negative regression showing that proof-like markers with no valid earlier state-changing transition still remain unresolved, so the broader heuristic does not over-attach weak evidence.
4. Keep the assertions at the analysis layer where the bug lives; do not expand this gap plan into Phase 18 assertion-generation or Phase 19 reporting coverage.
  </action>
  <verify>
    <automated>npm run build && npm run test:run -- src/core/recording-intelligence.test.ts</automated>
  </verify>
  <done>
    Automated coverage now catches the real sample-anchor failures that Phase 17 verification exposed.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds.
2. `npm run test:run -- src/core/recording-intelligence.test.ts` succeeds.
3. The representative sample's `Add Sale (Invoice)` marker no longer resolves to `unresolved/missing-anchor`; it attaches to the triggering state-changing action.
4. Later review-flow proof markers in the same sample no longer stay unresolved when a valid earlier state-changing transition exists behind intervening non-anchor steps.
5. Markers with no truthful earlier state-changing anchor still remain explicitly unresolved.
</verification>

<success_criteria>
- Major-transition anchor recognition covers real state-changing opener/review actions without upgrading routine edits into anchors
- Backward anchor search skips over irrelevant prior steps instead of aborting early
- Representative sample regressions cover both the opener marker and later review-flow proof markers
- Phase 17 scope stays limited to marker attachment heuristics and proof coverage
</success_criteria>

<output>
After completion, create `.planning/phases/17-semantic-marker-intake/17-04-SUMMARY.md` documenting the anchor-heuristic expansion, backward-search behavior, and the new sample-backed regression coverage.
</output>
