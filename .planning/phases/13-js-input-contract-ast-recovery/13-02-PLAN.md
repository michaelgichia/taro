---
phase: 13-js-input-contract-ast-recovery
plan: "02"
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - package.json
  - src/core/js-parser.ts
  - src/core/js-parser.test.ts
autonomous: true
requirements:
  - INPUT-03
  - QUERY-01

must_haves:
  truths:
    - "Recorder JS intent is recovered structurally from nested AST expressions rather than by string slicing"
    - "Accessible query evidence survives parsing with method and semantic options intact"
    - "Fallback selector evidence is preserved truthfully without doing Phase 14 selector-strengthening work"
  artifacts:
    - path: "src/core/js-parser.ts"
      provides: "Rich recorder-JS baseline recovery with nested query and assertion extraction"
      contains: "parseJsRecording"
    - path: "src/core/js-parser.test.ts"
      provides: "Real recorder-shape regression coverage using the Phase 13 sample"
      contains: "sample-rest-recordingextension-output"
    - path: "package.json"
      provides: "Babel AST utility support for safer node-guarded parsing"
      contains: "@babel/types"
  key_links:
    - from: "src/core/js-parser.ts"
      to: "src/types/recording.ts"
      via: "JS baseline artifact and query/assertion descriptor types"
      pattern: "QueryDescriptor"
    - from: "src/core/js-parser.test.ts"
      to: "sample/sample-rest-recordingextension-output.js"
      via: "golden recorder fixture coverage"
      pattern: "sample-rest-recordingextension-output"
---

<objective>
Recover truthful recorder JS baseline intent from the AST.

Purpose: eliminate the main Phase 13 failure mode where nested recorder expressions collapse into fake actions, fake targets, and unusable assertion context.
Output: a richer `parseJsRecording()` path backed by real recorder-shape tests and safe AST node handling.
</objective>

<execution_context>
@/Users/michaelgichia/.codex/get-shit-done/workflows/execute-plan.md
@/Users/michaelgichia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-js-input-contract-ast-recovery/13-RESEARCH.md
@.planning/phases/13-js-input-contract-ast-recovery/13-VALIDATION.md
@src/core/js-parser.ts
@src/core/js-parser.test.ts
@src/types/recording.ts
@sample/sample-rest-recordingextension-output.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strengthen AST parsing around real recorder shapes</name>
  <files>package.json, src/core/js-parser.ts</files>
  <action>
Refactor `src/core/js-parser.ts` so it recovers recorder intent structurally.

Required outcomes:
1. Add `@babel/types` if needed so AST node handling can use explicit guards instead of broad `any`.
2. Recover nested `userEvent.*` operands such as `screen.getByRole(...)`, `screen.getByText(...)`, and `document.querySelector(...)` as structured action targets.
3. Recover assertion context for `expect(location.href).toBe(...)`, `expect(document.title).toBe(...)`, and future-compatible expectation subjects.
4. Recover the actual recorder `test('...')` title rather than relying only on brittle doc-comment extraction.
5. Preserve fallback CSS selector evidence without strengthening it in this plan.

Do not redesign suite grouping or final code generation here.
  </action>
  <verify>
    <automated>rg -n "@babel/types|expect\\(|querySelector|test\\(" src/core/js-parser.ts package.json</automated>
  </verify>
  <done>
    `parseJsRecording()` returns truthful AST-derived baseline data for the recorder shapes Phase 13 must support.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace toy parser tests with recorder-shaped regression coverage</name>
  <files>src/core/js-parser.test.ts</files>
  <action>
Upgrade parser coverage to match real recorder exports.

Required outcomes:
1. Add focused tests for nested `userEvent(screen.getBy...)`, `userEvent(document.querySelector(...))`, URL/title assertions, and title extraction.
2. Add golden assertions using `sample/sample-rest-recordingextension-output.js`.
3. Verify recovered metadata preserves action order, query method/quality, accessible-name evidence, fallback selectors, and environment URL.
4. Keep current quality-classification tests if still useful, but stop relying on toy-only snippets as the primary proof.

Do not add final CLI integration expectations here; keep this plan parser-focused.
  </action>
  <verify>
    <automated>npm run build && npm run test:run -- src/core/js-parser.test.ts</automated>
  </verify>
  <done>
    Parser coverage proves Phase 13 can recover real recorder intent from the sample baseline without flattening nested queries.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds.
2. `npm run test:run -- src/core/js-parser.test.ts` succeeds.
3. The sample recorder fixture no longer degrades into fake `"click"`/`"type"` targets during parsing.
4. Fallback selector evidence is preserved but not strengthened yet.
</verification>

<success_criteria>
- Nested recorder expressions are recovered structurally
- Accessible query intent survives parsing
- Real sample-based parser coverage exists
- Phase 14 selector work does not leak into this plan
</success_criteria>

<output>
After completion, create `.planning/phases/13-js-input-contract-ast-recovery/13-02-SUMMARY.md` documenting the AST recovery changes and sample-backed parser coverage.
</output>
