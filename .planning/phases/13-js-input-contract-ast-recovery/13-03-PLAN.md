---
phase: 13-js-input-contract-ast-recovery
plan: "03"
type: execute
wave: 3
depends_on:
  - 13-01
  - 13-02
files_modified:
  - src/core/baseline-normalizer.ts
  - src/cli/commands/generate.ts
  - src/core/recording-intelligence.ts
  - src/types/recording.ts
autonomous: true
requirements:
  - INPUT-01
  - INPUT-02
  - INPUT-03
  - QUERY-01

must_haves:
  truths:
    - "Recorder JS uses the same shipped `tayo generate` flow shape as JSON input instead of a special one-off orchestration branch"
    - "JS baseline artifacts are normalized into the shared recording contract without losing accessible query evidence"
    - "Phase 13 improves baseline fidelity without pre-solving final suite planning or selector-strengthening policy"
  artifacts:
    - path: "src/core/baseline-normalizer.ts"
      provides: "Dedicated translation from JS baseline artifact to shared recording contract"
      contains: "normalizeJsBaseline"
    - path: "src/cli/commands/generate.ts"
      provides: "Shared CLI orchestration path for JSON and JS generation entry"
      contains: "loadInput"
    - path: "src/core/recording-intelligence.ts"
      provides: "Grouping-safe handling of recovered JS assertion and sync metadata"
      contains: "analyzeRecording"
  key_links:
    - from: "src/cli/commands/generate.ts"
      to: "src/core/input-loader.ts"
      via: "shared input-loading path"
      pattern: "loadInput"
    - from: "src/core/baseline-normalizer.ts"
      to: "src/core/recording-intelligence.ts"
      via: "normalized JS recording handed to shared analysis"
      pattern: "NormalizedRecording"
---

<objective>
Normalize the recovered JS baseline and put it on the shipped CLI path.

Purpose: make the Phase 13 contract real in the public product flow so recorder JS enters `tayo generate` like JSON while preserving the richer baseline evidence needed by later phases.
Output: JS baseline normalization plus CLI integration through the shared loader and shared analysis path.
</objective>

<execution_context>
@/Users/michaelgichia/.codex/get-shit-done/workflows/execute-plan.md
@/Users/michaelgichia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-js-input-contract-ast-recovery/13-RESEARCH.md
@.planning/phases/13-js-input-contract-ast-recovery/13-VALIDATION.md
@src/core/input-loader.ts
@src/core/js-parser.ts
@src/cli/commands/generate.ts
@src/core/recording-intelligence.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a dedicated JS baseline normalizer</name>
  <files>src/core/baseline-normalizer.ts, src/types/recording.ts</files>
  <action>
Create the translation layer from recovered JS baseline artifacts into the shared recording contract.

Required outcomes:
1. Add `src/core/baseline-normalizer.ts` with one clear entrypoint for converting the JS artifact into the shared recording shape.
2. Preserve stable step IDs, accessible query evidence, fallback selector evidence, and assertion context in the normalized result.
3. Keep JSON parsing unchanged; this layer exists specifically to keep AST-specific logic out of the JSON parser.
4. Make the normalized result safe for `recording-intelligence.ts` and later resolver/generator phases to consume.

Do not implement selector strengthening or suite planning logic here.
  </action>
  <verify>
    <automated>rg -n "normalizeJsBaseline|NormalizedRecording|QueryDescriptor|AssertionDescriptor" src/core/baseline-normalizer.ts src/types/recording.ts</automated>
  </verify>
  <done>
    The repo has a dedicated JS baseline normalizer that preserves Phase 13 metadata while producing a shared recording contract.
  </done>
</task>

<task type="auto">
  <name>Task 2: Route recorder JS through the shared generation flow</name>
  <files>src/cli/commands/generate.ts, src/core/recording-intelligence.ts</files>
  <action>
Refactor the shipped CLI path so recorder JS and JSON share the same public flow shape.

Required outcomes:
1. Replace the early JS-only branch in `src/cli/commands/generate.ts` with orchestration built on the shared input loader and JS normalizer.
2. Preserve support for `--dry-run`, `--output`, and `--force` when the input file is recorder JS.
3. Ensure `recording-intelligence.ts` can consume normalized JS assertions/sync steps without exploding grouping due to parser artifacts.
4. Keep selector strengthening policy and final suite-shaping work deferred to later phases.

Do not rewrite the generator for sample-quality helper extraction in this plan.
  </action>
  <verify>
    <automated>npm run build && node dist/index.js generate sample/sample-rest-recordingextension-output.js --dry-run</automated>
  </verify>
  <done>
    Recorder JS uses the same shipped `tayo generate` flow shape as JSON input and produces truthful baseline-oriented output instead of a copied transcript branch.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds.
2. `node dist/index.js generate sample/sample-rest-recordingextension-output.js --dry-run` succeeds.
3. Recorder JS uses the shared loader/normalizer path rather than a separate early-return branch.
4. Normalized JS steps preserve accessible query evidence for later phases.
</verification>

<success_criteria>
- JS and JSON share one shipped flow shape
- JS baseline artifact is normalized cleanly
- Accessible query intent survives to shared analysis
- Phase 15 suite-planning work remains out of scope
</success_criteria>

<output>
After completion, create `.planning/phases/13-js-input-contract-ast-recovery/13-03-SUMMARY.md` documenting the JS normalization layer and shared CLI-path integration.
</output>
