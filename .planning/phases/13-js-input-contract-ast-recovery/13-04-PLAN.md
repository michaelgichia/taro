---
phase: 13-js-input-contract-ast-recovery
plan: "04"
type: execute
wave: 4
depends_on:
  - 13-03
files_modified:
  - src/core/js-parser.test.ts
  - src/core/input-loader.test.ts
  - src/cli/commands/generate.test.ts
  - src/core/parser.test.ts
  - src/core/recording-intelligence.test.ts
autonomous: true
requirements:
  - INPUT-01
  - INPUT-02
  - INPUT-03
  - QUERY-01

must_haves:
  truths:
    - "Users can keep generating from both recorder JS and Chrome Recorder JSON without Phase 13 breaking either public path"
    - "Recorder JS stays green through the real `tayo generate` surface rather than depending on helper-only assumptions"
    - "Phase 13 preserves truthful baseline evidence without pretending fallback selectors have already been strengthened into later-phase queries"
  artifacts:
    - path: "src/cli/commands/generate.test.ts"
      provides: "CLI-level parity coverage for recorder JS input and public flags"
      contains: "generate"
    - path: "src/core/input-loader.test.ts"
      provides: "Regression coverage for shared source loading"
      contains: "loadInput"
    - path: "src/core/parser.test.ts"
      provides: "Explicit JSON-path regression coverage during the JS milestone"
      contains: "parseRecording"
  key_links:
    - from: "src/cli/commands/generate.test.ts"
      to: "src/cli/commands/generate.ts"
      via: "public CLI parity verification"
      pattern: "generate"
    - from: "src/core/parser.test.ts"
      to: "src/core/parser.ts"
      via: "JSON regression proof"
      pattern: "parseRecording"
---

<objective>
Lock in regression proof and public-flow validation for Phase 13.

Purpose: make Phase 13 shippable by proving the real CLI `.js` path works, the JSON path does not regress, and baseline recovery stays faithful to the phase boundary.
Output: full regression coverage for shared loading, real recorder JS dry-run behavior, and JSON parity.
</objective>

<execution_context>
@/Users/michaelgichia/.codex/taro/workflows/execute-plan.md
@/Users/michaelgichia/.codex/taro/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-js-input-contract-ast-recovery/13-RESEARCH.md
@.planning/phases/13-js-input-contract-ast-recovery/13-VALIDATION.md
@src/cli/commands/generate.ts
@src/core/input-loader.ts
@src/core/parser.ts
@src/core/js-parser.ts
@sample/sample-rest-recordingextension-output.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CLI and shared-loader regression coverage</name>
  <files>src/cli/commands/generate.test.ts, src/core/input-loader.test.ts, src/core/recording-intelligence.test.ts</files>
  <action>
Add regression proof for the public `.js` flow.

Required outcomes:
1. Add CLI-level tests covering recorder JS with `--dry-run`, `--output`, and `--force`.
2. Add or expand shared-loader tests proving `.json` and `.js` source detection stays truthful.
3. Add coverage proving normalized JS assertions no longer explode intent grouping in obvious ways.
4. Keep tests focused on Phase 13 fidelity, not Phase 15 helper-rich suite shaping.
  </action>
  <verify>
    <automated>npm run build && npm run test:run -- src/cli/commands/generate.test.ts src/core/input-loader.test.ts src/core/recording-intelligence.test.ts</automated>
  </verify>
  <done>
    The public recorder-JS path and shared loader have real regression coverage tied to Phase 13 expectations.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add explicit JSON parity coverage and phase-boundary assertions</name>
  <files>src/core/parser.test.ts, src/core/js-parser.test.ts</files>
  <action>
Close Phase 13 with parity proof and boundary assertions.

Required outcomes:
1. Add or expand JSON parser tests so the milestone proves no regression in the existing `.json` path.
2. Add phase-boundary assertions proving Phase 13 preserves fallback selector evidence without strengthening it into invented accessible queries.
3. Keep the sample recorder fixture as the canonical JS baseline regression asset.
4. Ensure the combined coverage maps directly to INPUT-01, INPUT-02, INPUT-03, and QUERY-01.
  </action>
  <verify>
    <automated>npm run build && npm run test:run -- src/core/js-parser.test.ts src/core/parser.test.ts</automated>
  </verify>
  <done>
    Phase 13 closes with explicit regression proof for both `.js` and `.json` paths and with tests that keep later-phase selector work out of scope.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds.
2. `npm run test:run -- src/cli/commands/generate.test.ts src/core/input-loader.test.ts src/core/recording-intelligence.test.ts src/core/js-parser.test.ts src/core/parser.test.ts` succeeds.
3. Recorder JS parity is proven through the real CLI surface.
4. JSON parsing remains green during the JS milestone.
</verification>

<success_criteria>
- CLI `.js` path has regression proof
- Shared loader has regression proof
- JSON parity is explicit
- Phase 13 boundary assertions prevent selector-strengthening leakage
</success_criteria>

<output>
After completion, create `.planning/phases/13-js-input-contract-ast-recovery/13-04-SUMMARY.md` documenting the regression coverage, JSON parity proof, and phase-boundary assertions.
</output>
