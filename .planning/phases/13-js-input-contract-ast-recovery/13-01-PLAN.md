---
phase: 13-js-input-contract-ast-recovery
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/recording.ts
  - src/core/input-loader.ts
  - src/core/input-loader.test.ts
  - src/core/parser.ts
autonomous: true
requirements:
  - INPUT-01
  - INPUT-03
  - QUERY-01

must_haves:
  truths:
    - "Users can feed both Chrome Recorder JSON and recorder JS into the same shipped generation flow without switching commands or entrypoints"
    - "Recorder JS reaches downstream generation with preserved baseline metadata instead of collapsing into flat transcript-only steps"
    - "Chrome Recorder JSON input keeps working while the shared input-loading boundary is introduced"
  artifacts:
    - path: "src/core/input-loader.ts"
      provides: "Single source-detection and parsed-input loading entrypoint for JSON and JS recordings"
      contains: "loadInput"
    - path: "src/types/recording.ts"
      provides: "Shared parsed-input, step-id, and JS metadata contracts for baseline recovery"
      contains: "ParsedInput"
    - path: "src/core/input-loader.test.ts"
      provides: "Automated coverage for source detection and shared envelope behavior"
      contains: "loadInput"
  key_links:
    - from: "src/core/input-loader.ts"
      to: "src/core/parser.ts"
      via: "JSON loading path"
      pattern: "parseRecording"
    - from: "src/core/input-loader.ts"
      to: "src/core/js-parser.ts"
      via: "recorder JS loading path"
      pattern: "parseJsRecording"
    - from: "src/core/input-loader.ts"
      to: "src/types/recording.ts"
      via: "shared parsed-input contract"
      pattern: "ParsedInput"
---

<objective>
Establish the shared input contract and loader boundary for Phase 13.

Purpose: create the contract that lets JSON and recorder JS share the same shipped generation flow while still allowing JS-specific baseline metadata to survive parsing.
Output: parsed-input types, shared source loader, and tests that prove JSON and JS both enter the pipeline through one explicit contract.
</objective>

<execution_context>
@/Users/michaelgichia/.codex/taro/workflows/execute-plan.md
@/Users/michaelgichia/.codex/taro/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-js-input-contract-ast-recovery/13-RESEARCH.md
@.planning/phases/13-js-input-contract-ast-recovery/13-VALIDATION.md
@src/types/recording.ts
@src/core/parser.ts
@src/core/js-parser.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the shared parsed-input and baseline metadata contracts</name>
  <files>src/types/recording.ts</files>
  <action>
Extend `src/types/recording.ts` with the contract needed for Phase 13 without breaking existing JSON behavior.

Required outcomes:
1. Add stable step-id support that later phases can reuse.
2. Add a `ParsedInput` envelope (or equivalently named shared contract) that distinguishes JSON and JS sources without leaking `isJsFormat` branching through the CLI.
3. Add JS baseline metadata types for structured query, selector, and assertion evidence so `js-parser.ts` does not need to flatten everything into `NormalizedStep`.
4. Keep existing exports compatible for downstream files that already import recording types.

Do not implement selector strengthening policy or suite-planning shapes in this plan.
  </action>
  <verify>
    <automated>rg -n "ParsedInput|QueryDescriptor|AssertionDescriptor|stepId|id:" src/types/recording.ts</automated>
  </verify>
  <done>
    `src/types/recording.ts` exports a shared input contract and JS baseline metadata types that later plans can consume without reopening the boundary.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add a shared source loader for JSON and JS inputs</name>
  <files>src/core/input-loader.ts, src/core/input-loader.test.ts, src/core/parser.ts</files>
  <action>
Create the source-loading seam for Phase 13.

Required outcomes:
1. Add `src/core/input-loader.ts` with one exported entrypoint that detects JSON vs recorder JS source and returns the shared parsed-input envelope.
2. Keep JSON parsing delegated to the existing `parseRecording()` path instead of mixing AST-specific logic into `src/core/parser.ts`.
3. Explicitly route recorder JS inputs to `parseJsRecording()` through the same loader boundary so the JS handoff is visible in the design and code.
4. Add or update tests proving source detection works for `.json`, `.js`, and `@jest-environment-options` content.
5. Keep the loader structured so later plans can thread JS baseline metadata through normalization and generation.

Do not route the CLI through this loader yet; that belongs to a later plan in this phase.
  </action>
  <verify>
    <automated>npm run build && npm run test:run -- src/core/input-loader.test.ts</automated>
  </verify>
  <done>
    The repo has one explicit input loader that returns a shared parsed-input envelope for JSON and recorder JS sources.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds.
2. `npm run test:run -- src/core/input-loader.test.ts` succeeds.
3. JSON input still routes through `parseRecording()` without AST-specific leakage.
4. The new contracts are sufficient for later plans to add JS baseline recovery without changing the public CLI surface.
</verification>

<success_criteria>
- Shared parsed-input contract exists
- Stable step-id and JS metadata hooks exist
- JSON remains compatible
- Source loading is centralized before CLI integration work begins
</success_criteria>

<output>
After completion, create `.planning/phases/13-js-input-contract-ast-recovery/13-01-SUMMARY.md` documenting the shared input contract and loader boundary.
</output>
