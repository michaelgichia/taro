---
phase: 05-recording-intelligence-recovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/recording.ts
  - src/core/parser.ts
  - src/core/recording-intelligence.ts
  - src/core/recording-intelligence.test.ts
autonomous: true
requirements:
  - REC-01
  - REC-02
  - REC-03

must_haves:
  truths:
    - "Parsed recorder steps preserve enough metadata to reason about duplicate clicks, dblClick noise, and cursor wandering"
    - "Recording intelligence filters redundant same-target clicks before generation"
    - "Recording intelligence distinguishes doubleClick-style noise from a single semantic click"
    - "Pointer wandering and other non-semantic movement noise are removed from the cleaned step list"
  artifacts:
    - path: "src/core/recording-intelligence.ts"
      provides: "Noise filtering primitives and a public analyzeRecording entry point"
      exports: ["filterNoiseSteps", "analyzeRecording"]
    - path: "src/core/recording-intelligence.test.ts"
      provides: "Automated verification for REC-01 through REC-03 heuristics"
  key_links:
    - from: "src/core/parser.ts"
      to: "src/types/recording.ts"
      via: "Richer normalized step metadata"
      pattern: "selectors|offsetX|offsetY|x|y|assertedEvents"
    - from: "src/core/recording-intelligence.ts"
      to: "src/types/recording.ts"
      via: "Analyzed recording types"
      pattern: "RecordingDiagnostics|AnalyzedRecording"
---

<objective>
Preserve recorder metadata and implement the first-pass noise filtering layer for recording intelligence.

Purpose: Phase 5 cannot filter redundant clicks, dblClick noise, or cursor wandering unless the parsing layer stops discarding the evidence needed for those heuristics. This plan establishes the analyzable recording shape and delivers a tested filtering module for REC-01 through REC-03.
</objective>

<execution_context>
@/Users/michaelgichia/.codex/get-shit-done/workflows/execute-plan.md
@/Users/michaelgichia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-recording-intelligence-recovery/05-RESEARCH.md
@.planning/phases/05-recording-intelligence-recovery/05-VALIDATION.md
@src/types/recording.ts
@src/core/parser.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend recording types for analyzable metadata</name>
  <files>src/types/recording.ts</files>
  <behavior>
    - normalized or analyzed steps can preserve selector arrays and source metadata
    - JSON-derived steps can carry coordinate and offset metadata for pointer-noise heuristics
    - analyzed recordings expose diagnostics about removed and retained steps
  </behavior>
  <action>
Update `src/types/recording.ts` to introduce the minimum type surface needed for recording intelligence while keeping the existing generation path usable.

Add or extend types so the codebase can represent:
- source-aware normalized steps (`json` vs `js`)
- preserved recorder metadata (`selectors`, `assertedEvents`, `offsetX`, `offsetY`, `x`, `y`)
- recording diagnostics (`removedRedundantClicks`, `removedDoubleClickNoise`, `removedCursorWander`)
- an analyzed recording result that still exposes `title`, `steps`, and `rawStepCount`

Do not break existing generator imports. The existing `NormalizedStep` and `NormalizedRecording` call sites should stay compatible after the change.
  </action>
  <verify>
    <automated>cd /Users/michaelgichia/workspace/taro && npm run build</automated>
  </verify>
  <done>
`src/types/recording.ts` exports the recording-intelligence types needed by parser and analyzer code without breaking existing compilation.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve recorder metadata in parser.ts</name>
  <files>src/core/parser.ts</files>
  <behavior>
    - parseRecording preserves raw recorder metadata needed for REC-01 through REC-03
    - doubleClick remains distinguishable as source evidence even if normalized action stays generation-friendly
    - returned recording shape remains compatible with downstream generation
  </behavior>
  <action>
Update `src/core/parser.ts` so JSON parsing carries forward the recorder metadata introduced in Task 1.

Requirements:
- keep the current generation-oriented fields (`action`, `target`, `value`, `originalType`)
- preserve raw evidence needed for filtering heuristics on the normalized step object
- do not silently erase `doubleClick` intent at parse time
- keep `parseRecording()` API stable enough that `generate.ts` can adopt the analyzer in a later plan without a major rewrite

Avoid adding heuristics here. The parser should preserve evidence, not decide what noise to remove.
  </action>
  <verify>
    <automated>cd /Users/michaelgichia/workspace/taro && npm run build</automated>
  </verify>
  <done>
`parseRecording()` returns steps that still compile with the current pipeline and now retain the metadata needed by the recording-intelligence layer.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement noise filtering and unit coverage</name>
  <files>src/core/recording-intelligence.ts, src/core/recording-intelligence.test.ts</files>
  <behavior>
    - adjacent same-target click bursts collapse into one semantic click
    - dblClick-style duplicate noise is filtered intentionally
    - pointer wandering or non-semantic movement steps are removed
    - diagnostics report how many noisy steps were removed by category
  </behavior>
  <action>
Create `src/core/recording-intelligence.ts` and `src/core/recording-intelligence.test.ts`.

Implementation requirements:
- export a public `analyzeRecording(recording)` helper
- implement deterministic helper(s) for REC-01 through REC-03
- keep the output stable and side-effect free
- preserve meaningful click/fill/assert steps while removing only noise

Testing requirements:
- add automated coverage for redundant clicks on the same target
- add automated coverage for double-click noise handling
- add automated coverage for cursor wandering / movement noise removal
- assert diagnostics counts alongside cleaned step output so verification is explicit
  </action>
  <verify>
    <automated>cd /Users/michaelgichia/workspace/taro && npm run test:run -- src/core/recording-intelligence.test.ts && npm run build</automated>
  </verify>
  <done>
`src/core/recording-intelligence.test.ts` passes and proves REC-01 through REC-03 behavior against the new analyzer.
  </done>
</task>

</tasks>

<verification>
1. `npm run test:run -- src/core/recording-intelligence.test.ts` passes
2. `npm run build` passes
3. Parser output still compiles against the existing generator path
</verification>

<success_criteria>
The codebase can now represent recorder metadata, preserve it during JSON parsing, and apply deterministic filtering for redundant clicks, dblClick noise, and cursor wandering with automated test coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/05-recording-intelligence-recovery/05-01-SUMMARY.md`
</output>
