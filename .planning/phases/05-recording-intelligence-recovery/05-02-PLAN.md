---
phase: 05-recording-intelligence-recovery
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/types/recording.ts
  - src/core/recording-intelligence.ts
  - src/core/recording-intelligence.test.ts
autonomous: true
requirements:
  - REC-04

must_haves:
  truths:
    - "Cleaned recordings are reduced into deterministic intent-oriented groups before generation"
    - "Intent boundaries preserve the actual user journey rather than the raw recorder event burst"
    - "Analyzer diagnostics expose both cleaned-step counts and inferred intent groups"
  artifacts:
    - path: "src/core/recording-intelligence.ts"
      provides: "Intent grouping and analyzed recording assembly"
      exports: ["inferIntentGroups", "analyzeRecording"]
    - path: "src/core/recording-intelligence.test.ts"
      provides: "REC-04 verification for intent inference heuristics"
  key_links:
    - from: "src/core/recording-intelligence.ts"
      to: "src/types/recording.ts"
      via: "Intent-group and diagnostics types"
      pattern: "IntentGroup|RecordingDiagnostics"
---

<objective>
Infer actual user intent from cleaned recorder sequences.

Purpose: Filtering noise is not enough to close the audit gap. Tayo also needs to reduce low-level event bursts into a smaller set of intent-oriented groups so the pipeline can reason about what the user was trying to accomplish, not just what the recorder captured.
</objective>

<execution_context>
@/Users/michaelgichia/.codex/taro/workflows/execute-plan.md
@/Users/michaelgichia/.codex/taro/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-recording-intelligence-recovery/05-RESEARCH.md
@.planning/phases/05-recording-intelligence-recovery/05-VALIDATION.md
@.planning/phases/05-recording-intelligence-recovery/05-01-PLAN.md
@src/core/recording-intelligence.ts
@src/core/recording-intelligence.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define intent grouping heuristics</name>
  <files>src/types/recording.ts, src/core/recording-intelligence.ts</files>
  <behavior>
    - navigate or major route changes start a new intent boundary
    - open/fill/submit/assert sequences are grouped into stable user-intent chunks
    - click/assert confirmation pairs can remain in the same logical group
  </behavior>
  <action>
Extend the recording-intelligence model with explicit intent-group support.

Implement deterministic heuristics for REC-04, such as:
- new group on navigate or explicit view transition
- consecutive fill/select steps belong to the current form-edit intent
- submit/assert or open/assert pairs finalize an intent group when they complete a meaningful user action

Keep the heuristics simple and testable. Avoid speculative NLP or probabilistic naming.
  </action>
  <verify>
    <automated>cd /Users/michaelgichia/workspace/tayo && npm run test:run -- src/core/recording-intelligence.test.ts</automated>
  </verify>
  <done>
The analyzer can now derive stable intent groups from cleaned step sequences and represent them in typed output.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand analyzeRecording() with diagnostics and intent output</name>
  <files>src/core/recording-intelligence.ts</files>
  <behavior>
    - analyzeRecording returns cleaned steps and inferred intent groups together
    - diagnostics describe raw steps, removed noise, filtered steps, and resulting intent count
    - output stays deterministic for the same input recording
  </behavior>
  <action>
Update `analyzeRecording()` so it becomes the single public entry point for recording intelligence.

The returned result should:
- reuse the REC-01 through REC-03 filtering from Plan 01
- append REC-04 intent groups
- expose structured diagnostics suitable for CLI logging and later verification

Keep internal helpers private unless tests need direct access.
  </action>
  <verify>
    <automated>cd /Users/michaelgichia/workspace/tayo && npm run test:run -- src/core/recording-intelligence.test.ts && npm run build</automated>
  </verify>
  <done>
`analyzeRecording()` now returns the cleaned recording plus deterministic intent-group metadata and diagnostics.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove REC-04 with focused test fixtures</name>
  <files>src/core/recording-intelligence.test.ts</files>
  <behavior>
    - tests show noisy event bursts collapse into fewer intent groups
    - tests prove meaningful actions are retained after intent reduction
    - diagnostics and intent group names remain understandable and stable
  </behavior>
  <action>
Expand `src/core/recording-intelligence.test.ts` with fixtures that exercise realistic flows:
- open dialog -> assert visible
- fill multiple fields -> submit -> assert success
- repeated click noise before the real action

The assertions must prove REC-04 directly, not indirectly through step counts alone.
  </action>
  <verify>
    <automated>cd /Users/michaelgichia/workspace/tayo && npm run test:run -- src/core/recording-intelligence.test.ts</automated>
  </verify>
  <done>
The test suite demonstrates that Tayo now infers actual user-intent groups from cleaned recordings.
  </done>
</task>

</tasks>

<verification>
1. `npm run test:run -- src/core/recording-intelligence.test.ts` passes with REC-04 coverage
2. `npm run build` passes
3. Intent groups are deterministic and included in analyzer diagnostics
</verification>

<success_criteria>
The recording-intelligence layer returns a cleaned, intent-aware recording that reflects the actual user flow rather than the raw recorder event stream.
</success_criteria>

<output>
After completion, create `.planning/phases/05-recording-intelligence-recovery/05-02-SUMMARY.md`
</output>
