---
phase: 05-recording-intelligence-recovery
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - src/cli/commands/generate.ts
  - src/core/js-parser.ts
autonomous: false
requirements:
  - REC-01
  - REC-02
  - REC-03
  - REC-04

must_haves:
  truths:
    - "The generate pipeline analyzes recordings before code generation instead of generating directly from raw parsed steps"
    - "JSON recordings benefit from recording cleanup without regressing the existing generation path"
    - "JS recordings pass through the same analysis interface, even if only limited metadata is available"
    - "Phase 4 scoring and post-write verification still run after recording cleanup"
  artifacts:
    - path: "src/cli/commands/generate.ts"
      provides: "CLI integration for analyzeRecording() in both input paths"
      contains: "analyzeRecording"
    - path: "src/core/js-parser.ts"
      provides: "Analysis-interface compatibility for JS recordings"
      contains: "source"
  key_links:
    - from: "src/cli/commands/generate.ts"
      to: "src/core/recording-intelligence.ts"
      via: "Import and call analyzeRecording() before generation"
      pattern: "analyzeRecording"
    - from: "src/cli/commands/generate.ts"
      to: "src/core/scorer.ts"
      via: "Scoring remains after cleanup and generation"
      pattern: "scoreGeneratedTest"
---

<objective>
Wire recording intelligence into the real generate pipeline without regressing existing behavior.

Purpose: Phase 5 only closes the audit gap if the analyzer is actually used during generation. This plan integrates the cleaned, intent-aware recording into both JSON and JS generation paths while preserving the Phase 3 query/test-design work and the Phase 4 scoring and verification flow.
</objective>

<execution_context>
@/Users/michaelgichia/.codex/get-shit-done/workflows/execute-plan.md
@/Users/michaelgichia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-recording-intelligence-recovery/05-RESEARCH.md
@.planning/phases/05-recording-intelligence-recovery/05-VALIDATION.md
@.planning/phases/05-recording-intelligence-recovery/05-01-PLAN.md
@.planning/phases/05-recording-intelligence-recovery/05-02-PLAN.md
@src/cli/commands/generate.ts
@src/core/js-parser.ts
@src/core/recording-intelligence.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate analyzeRecording() into generate.ts</name>
  <files>src/cli/commands/generate.ts</files>
  <behavior>
    - parsed recordings are analyzed before generation in both input paths
    - generation consumes cleaned steps rather than raw parsed steps
    - cleanup can emit a brief diagnostic summary without disrupting dry-run or write flows
  </behavior>
  <action>
Update `src/cli/commands/generate.ts` so recording analysis sits between parsing and generation.

Requirements:
- JSON path: call `analyzeRecording()` after `parseRecording()`
- JS path: adapt the parsed JS result to the same analysis interface before `generateTestFromGroups()`
- do not disturb the existing order of scoring, file writing, post-write verification, or convention learning
- keep dry-run behavior intact

Use conservative console output such as a single cleanup summary line when steps are removed or intents are inferred.
  </action>
  <verify>
    <automated>cd /Users/michaelgichia/workspace/taro && npm run build</automated>
  </verify>
  <done>
The CLI now runs recording intelligence before generation in both supported input formats.
  </done>
</task>

<task type="auto">
  <name>Task 2: Make JS parse output analyzer-compatible</name>
  <files>src/core/js-parser.ts</files>
  <behavior>
    - JS-derived recordings carry enough metadata for the shared analyzer contract
    - limited JS metadata does not block analysis integration
    - existing query/test-design behavior remains intact
  </behavior>
  <action>
Update `src/core/js-parser.ts` only as needed to support the shared analyzer contract from Plans 01 and 02.

Keep this change minimal:
- add source metadata or compatibility fields if the analyzer expects them
- do not rewrite the query or grouping logic from Phase 3
- preserve the current `parseJsRecording()` behavior for query extraction and it-group generation
  </action>
  <verify>
    <automated>cd /Users/michaelgichia/workspace/taro && npm run test:run -- src/core/js-parser.test.ts src/core/recording-intelligence.test.ts && npm run build</automated>
  </verify>
  <done>
JS recordings can pass through the shared recording-intelligence interface without breaking the existing Phase 3 pipeline.
  </done>
</task>

<task type="manual">
  <name>Task 3: Verify end-to-end cleanup behavior</name>
  <files>src/cli/commands/generate.ts</files>
  <action>
Run one end-to-end verification using a synthetic noisy recording fixture or temporary recording file.

Confirm:
- duplicate click noise is absent from the generated output
- the meaningful user journey is still represented
- score logging and post-write verification still appear after generation
- the command still succeeds for at least one JS-format recording and one JSON-format recording
  </action>
  <verify>
    <manual>Run `npm run build`, then execute `taro generate` against temporary noisy JSON and JS fixtures and inspect the output plus console summary.</manual>
  </verify>
  <done>
Manual verification confirms recording cleanup is active in the real CLI pipeline and does not regress the existing generator flow.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes
2. `npm run test:run -- src/core/js-parser.test.ts src/core/recording-intelligence.test.ts` passes
3. Manual CLI verification confirms cleanup + scoring + post-write verification coexist
</verification>

<success_criteria>
`taro generate` now routes parsed recordings through recording intelligence before generation, giving cleaner output while preserving the existing scoring and verification pipeline.
</success_criteria>

<output>
After completion, create `.planning/phases/05-recording-intelligence-recovery/05-03-SUMMARY.md`
</output>
