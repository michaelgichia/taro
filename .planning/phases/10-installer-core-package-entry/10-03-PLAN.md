---
phase: 10-installer-core-package-entry
plan: "03"
type: execute
wave: 3
depends_on:
  - "10-01"
  - "10-02"
files_modified:
  - src/install/resolver.ts
  - src/install/planner.ts
  - src/install/summary.ts
  - src/cli/commands/install.ts
autonomous: true
requirements:
  - INST-03

must_haves:
  truths:
    - "Location selection can be represented per selected runtime"
    - "Before writing, the installer shows a short summary of chosen runtimes and locations"
    - "Interactive installs require final confirmation before any writes occur"
    - "If confirmation is declined, the installer exits cleanly and states nothing changed"
  artifacts:
    - path: "src/install/resolver.ts"
      provides: "Runtime/location resolution primitives for the Phase 10 install plan"
      contains: "resolveInstallTargets"
    - path: "src/install/planner.ts"
      provides: "Deterministic install plan model built from selected runtimes and locations"
      contains: "buildInstallPlan"
    - path: "src/install/summary.ts"
      provides: "Short prewrite summary and verification-command-led success output"
      contains: "renderInstallSummary"
  key_links:
    - from: "src/cli/commands/install.ts"
      to: "src/install/planner.ts"
      via: "plan creation before any write step"
      pattern: "buildInstallPlan"
    - from: "src/cli/commands/install.ts"
      to: "src/install/summary.ts"
      via: "confirmation and success messaging"
      pattern: "renderInstallSummary"
---

<objective>
Create the deterministic install plan, location resolution layer, and confirmation/summary flow for Phase 10.

Purpose: Complete the pre-write installer behavior so users can see what their choices mean before runtime payload writing begins in the next phase.
Output: Install-plan objects, short summary/confirmation flow, and clean cancellation behavior.
</objective>

<execution_context>
@/Users/michaelgichia/.codex/taro/workflows/execute-plan.md
@/Users/michaelgichia/.codex/taro/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-installer-core-package-entry/10-CONTEXT.md
@src/cli/commands/install.ts
@src/install/types.ts
@src/install/options.ts
@src/install/prompts.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build runtime/location resolution and install planning primitives</name>
  <files>src/install/resolver.ts, src/install/planner.ts</files>
  <action>
Create the core data layer for Phase 10's installation planning.

In `src/install/resolver.ts`:
1. Model global and local installation targets as resolved plan inputs
2. Accept per-runtime location selections
3. Keep the code ready for runtime-specific directories in Phase 11 without hardcoding payload writes yet

In `src/install/planner.ts`:
1. Turn normalized installer choices into a deterministic install plan object
2. Preserve per-runtime location choices in the plan
3. Expose enough structured data for summary, confirmation, and later payload execution

Do not implement actual runtime asset copying in this plan.
  </action>
  <verify>
    <automated>rg -n "resolveInstallTargets|buildInstallPlan|location" src/install/resolver.ts src/install/planner.ts</automated>
  </verify>
  <done>
    A deterministic install plan can be built from the user's selections without writing any payload files yet.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add prewrite summary, confirmation, and clean cancel flow</name>
  <files>src/install/summary.ts, src/cli/commands/install.ts</files>
  <action>
Implement the user-facing checkpoint immediately before writes.

Requirements:
1. Render a short summary that shows selected runtimes and chosen locations
2. Require explicit confirmation in the interactive flow before any write step begins
3. If the user declines confirmation, exit cleanly and report that nothing changed
4. Structure the success output so it can lead with runtime-specific verification commands once Phase 11 adds the payload details

Update `src/cli/commands/install.ts` to:
1. Build the install plan before any write logic
2. Call the summary/confirmation flow
3. Stop cleanly when confirmation is declined
4. Keep the path open for Phase 11 to insert actual payload execution after confirmation
  </action>
  <verify>
    <automated>rg -n "confirm|nothing changed|summary|verification" src/install/summary.ts src/cli/commands/install.ts</automated>
  </verify>
  <done>
    The installer shows a short summary, requires confirmation, and exits cleanly without writes when declined.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds.
2. The installer command can build a structured plan from selected runtimes and per-runtime locations.
3. Interactive runs show a short summary before writes.
4. Declining confirmation exits cleanly and reports that nothing changed.
</verification>

<success_criteria>
- Per-runtime location choices are represented in the install plan
- Prewrite summary exists and stays short
- Final confirmation is mandatory in the interactive path
- Clean cancellation behavior is implemented
</success_criteria>

<output>
After completion, create `.planning/phases/10-installer-core-package-entry/10-03-SUMMARY.md` documenting install-plan modeling and confirmation-flow work.
</output>
