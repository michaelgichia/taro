---
phase: 10-installer-core-package-entry
plan: "02"
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - src/install/types.ts
  - src/install/options.ts
  - src/install/prompts.ts
  - src/cli/commands/install.ts
autonomous: true
requirements:
  - INST-02
  - INST-04

must_haves:
  truths:
    - "Interactive flow asks for runtime selection before installation location"
    - "Interactive runtime selection supports a custom subset, not only one runtime or all"
    - "Non-interactive installs accept --claude/--opencode/--gemini/--codex/--all with --global/--local"
    - "Non-TTY runs without required flags fail clearly instead of guessing defaults"
  artifacts:
    - path: "src/install/types.ts"
      provides: "Installer runtime and location types"
      contains: "RuntimeTarget"
    - path: "src/install/options.ts"
      provides: "Flag normalization and validation for interactive/non-interactive install input"
      contains: "normalizeInstallOptions"
    - path: "src/install/prompts.ts"
      provides: "Runtime-first interactive prompt flow"
      contains: "promptForInstallChoices"
  key_links:
    - from: "src/cli/commands/install.ts"
      to: "src/install/options.ts"
      via: "flag normalization"
      pattern: "normalizeInstallOptions"
    - from: "src/cli/commands/install.ts"
      to: "src/install/prompts.ts"
      via: "interactive flow entry"
      pattern: "promptForInstallChoices"
---

<objective>
Define and implement the installer selection model for both interactive and non-interactive usage.

Purpose: Lock the Phase 10 user input behavior so runtime choice, flag handling, and no-TTY failure behavior are deterministic and aligned with the captured context.
Output: Typed installer selection model, prompt flow, and flag normalization layer integrated with the install command from 10-01.
</objective>

<execution_context>
@/Users/michaelgichia/.codex/get-shit-done/workflows/execute-plan.md
@/Users/michaelgichia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-installer-core-package-entry/10-CONTEXT.md
@src/cli/commands/install.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create installer types and option normalization</name>
  <files>src/install/types.ts, src/install/options.ts</files>
  <action>
Create installer-focused modules that model the Phase 10 selection behavior.

In `src/install/types.ts`, define typed concepts for:
- supported runtimes
- selected runtime subsets
- installation location choices
- normalized install request data passed into later planning steps

In `src/install/options.ts`, implement normalization/validation that:
1. Accepts the installer flags declared in the CLI command
2. Resolves `--all` against explicit runtime flags consistently
3. Enforces that non-interactive runs must include enough information to continue
4. Produces clear, user-facing validation errors instead of default guesses

Keep runtime-specific path details out of this plan; only normalize the selection intent.
  </action>
  <verify>
    <automated>rg -n "RuntimeTarget|InstallLocation|normalizeInstallOptions|all" src/install/types.ts src/install/options.ts</automated>
  </verify>
  <done>
    Installer selection types exist and non-interactive flag input can be normalized without guessing defaults.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement runtime-first interactive prompt flow</name>
  <files>src/install/prompts.ts, src/cli/commands/install.ts</files>
  <action>
Implement the interactive selection behavior described in `10-CONTEXT.md`.

Requirements for the prompt layer:
1. Ask for runtime selection before any location question
2. Support a true custom multi-select interaction across Claude Code, OpenCode, Gemini CLI, and Codex
3. Avoid silently preselecting a runtime
4. Hand back the selected runtime set in a form the installer command can continue with

Then update `src/cli/commands/install.ts` so:
1. Interactive runs call the prompt flow when flags do not fully specify the install
2. Non-TTY runs without sufficient flags fail with a short actionable error
3. The command structure cleanly separates prompt collection from later install-plan building
  </action>
  <verify>
    <automated>rg -n "promptForInstallChoices|process\\.stdin|TTY|interactive" src/install/prompts.ts src/cli/commands/install.ts</automated>
  </verify>
  <done>
    Interactive installs are runtime-first and custom-subset capable, and non-interactive runs fail clearly when under-specified.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds.
2. `node dist/index.js install --help` shows the runtime and location flags.
3. A no-TTY/non-interactive code path without runtime flags exits with a clear actionable error.
4. Interactive code paths route through the new runtime-first prompt module.
</verification>

<success_criteria>
- Runtime selection happens before location selection
- Multi-runtime custom subsets are supported
- Non-interactive flags are normalized consistently
- No-TTY installs do not guess defaults
</success_criteria>

<output>
After completion, create `.planning/phases/10-installer-core-package-entry/10-02-SUMMARY.md` documenting the selection-model and prompt-flow work.
</output>
