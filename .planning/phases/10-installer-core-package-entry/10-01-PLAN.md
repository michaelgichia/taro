---
phase: 10-installer-core-package-entry
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.ts
  - src/cli/commands/install.ts
autonomous: true
requirements:
  - INST-01
  - DIST-01

must_haves:
  truths:
    - "Running the package root enters installer-first behavior instead of generator-first help"
    - "An explicit install command exists alongside the installer-first root behavior"
    - "The existing generate command remains reachable during the installer pivot"
    - "@tayo-dev/rtl remains the only package users invoke for installation"
  artifacts:
    - path: "src/index.ts"
      provides: "Commander root wired for installer-first entry behavior"
      contains: "install"
    - path: "src/cli/commands/install.ts"
      provides: "Installer command entrypoint and shared top-level flow"
      contains: "createInstallCommand"
  key_links:
    - from: "src/index.ts"
      to: "src/cli/commands/install.ts"
      via: "install command import and/or root fallback"
      pattern: "install"
    - from: "src/index.ts"
      to: "src/cli/commands/generate.ts"
      via: "coexisting generator command registration"
      pattern: "generate"
---

<objective>
Establish the installer as the package's primary entry behavior while preserving access to generation.

Purpose: Deliver the product-surface pivot for Phase 10 so `@tayo-dev/rtl` feels like an installer-first package and not just a test generator CLI.
Output: Updated root CLI wiring plus a dedicated install command entrypoint.
</objective>

<execution_context>
@/Users/michaelgichia/.codex/get-shit-done/workflows/execute-plan.md
@/Users/michaelgichia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-installer-core-package-entry/10-CONTEXT.md
@src/index.ts
@src/cli/commands/generate.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create installer command entrypoint</name>
  <files>src/cli/commands/install.ts</files>
  <action>
Create a new `src/cli/commands/install.ts` module that defines the installer command surface and top-level action callback.

The command should:
1. Export `createInstallCommand(): Command`
2. Own the shared install flags needed in Phase 10:
   - `--claude`
   - `--opencode`
   - `--gemini`
   - `--codex`
   - `--all`
   - `--global`
   - `--local`
3. Delegate interactive and non-interactive behavior to follow-on installer modules rather than placing all logic inline
4. Reserve space for installer-first status output and explicit `install` command help text

Do not implement runtime-specific payload writing in this plan. Keep the command boundary focused on Phase 10 concerns only.
  </action>
  <verify>
    <automated>rg -n "createInstallCommand|--claude|--opencode|--gemini|--codex|--all|--global|--local" src/cli/commands/install.ts</automated>
  </verify>
  <done>
    `src/cli/commands/install.ts` exists, exports `createInstallCommand`, and declares the installer command surface for both interactive and non-interactive install entry.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewire the root CLI to be installer-first</name>
  <files>src/index.ts</files>
  <action>
Update `src/index.ts` so the package root becomes installer-first while generation remains available.

Required behaviors:
1. Import and register the new install command from `src/cli/commands/install.ts`
2. Keep `createGenerateCommand()` registered so `taro generate` remains available during the pivot
3. Change root/no-subcommand behavior so a normal package invocation routes into the installer-first experience rather than showing generator-focused help
4. Ensure help output emphasizes setup/install first and generation second
5. Keep the implementation structured so later phases can extend installer behavior without reopening the root command wiring

Do not remove generation support in this plan.
  </action>
  <verify>
    <automated>rg -n "createInstallCommand|createGenerateCommand|install" src/index.ts</automated>
  </verify>
  <done>
    Root CLI wiring is installer-first, explicit install command exists, and generation remains reachable.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with the new install command module included.
2. `node dist/index.js --help` shows installer/setup-oriented top-level help.
3. `node dist/index.js install --help` exits successfully.
4. `node dist/index.js generate --help` still exits successfully.
</verification>

<success_criteria>
- Package root no longer behaves like a generator-first shell
- Explicit `install` command exists
- `generate` remains available
- No second installer package is introduced
</success_criteria>

<output>
After completion, create `.planning/phases/10-installer-core-package-entry/10-01-SUMMARY.md` documenting the root CLI and install command changes.
</output>
