---
phase: 09-package-publish
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/index.ts
autonomous: true
requirements:
  - PKG-01
  - PKG-02

must_haves:
  truths:
    - "package.json name field is @tayo/rtl"
    - "package.json version field is 1.0.0"
    - "package.json files field whitelists dist/, README.md, and LICENSE"
    - "package.json exports field maps the package root to dist/index.js"
    - "package.json engines field requires node >=18"
    - "version string in src/index.ts matches 1.0.0"
  artifacts:
    - path: "package.json"
      provides: "npm package manifest with all required fields"
      contains: "@tayo/rtl"
    - path: "src/index.ts"
      provides: "CLI entry point with correct version"
      contains: "1.0.0"
  key_links:
    - from: "package.json"
      to: "dist/index.js"
      via: "bin.taro and exports field"
      pattern: "dist/index\\.js"
---

<objective>
Update package.json with the correct npm publish fields and bump the version to 1.0.0 in both package.json and the CLI entry point.

Purpose: Prepare the package manifest so npm consumers get a correctly named, versioned, and scoped package with proper node engine constraints and clean file inclusion.
Output: Updated package.json and src/index.ts with all required fields set.
</objective>

<execution_context>
@/Users/michaelgichia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/michaelgichia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update package.json with all required npm fields</name>
  <files>package.json</files>
  <action>
    Edit package.json with the following changes (all others remain as-is):

    1. Change `"name": "taro"` to `"name": "@tayo/rtl"`

    2. Change `"version": "0.1.0"` to `"version": "1.0.0"`

    3. Add a `"files"` field (whitelist for npm pack — only these are included in the published tarball):
       ```json
       "files": [
         "dist",
         "README.md",
         "LICENSE"
       ]
       ```

    4. Add an `"exports"` field pointing consumers to the compiled entry:
       ```json
       "exports": {
         ".": "./dist/index.js"
       }
       ```

    5. Add an `"engines"` field requiring Node 18 minimum (ESM + modern APIs used throughout):
       ```json
       "engines": {
         "node": ">=18"
       }
       ```

    Place `files` after `type`, `exports` after `bin`, and `engines` after `exports`. Keep existing `scripts`, `keywords`, `license`, `dependencies`, and `devDependencies` unchanged.

    Final top-level key order: name, version, description, type, bin, files, scripts, exports, engines, keywords, license, dependencies, devDependencies.
  </action>
  <verify>
    <automated>node -e "const p=JSON.parse(require('fs').readFileSync('/Users/michaelgichia/workspace/taro/.claude/worktrees/hopeful-austin/package.json','utf8')); console.assert(p.name==='@tayo/rtl','name'); console.assert(p.version==='1.0.0','version'); console.assert(Array.isArray(p.files),'files'); console.assert(p.exports['.'],'exports'); console.assert(p.engines.node,'engines'); console.log('OK')"</automated>
  </verify>
  <done>
    package.json contains: name="@tayo/rtl", version="1.0.0", files=["dist","README.md","LICENSE"], exports={".":"./dist/index.js"}, engines={node:">=18"}. JSON is valid. All existing fields preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update hardcoded version string in src/index.ts</name>
  <files>src/index.ts</files>
  <action>
    In src/index.ts, find the `.version()` call:

    ```typescript
    .version('0.1.0', '-v, --version', 'Output the current version')
    ```

    Change it to:

    ```typescript
    .version('1.0.0', '-v, --version', 'Output the current version')
    ```

    No other changes to this file. The version string must match package.json exactly so `taro --version` outputs `1.0.0` after the build.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('/Users/michaelgichia/workspace/taro/.claude/worktrees/hopeful-austin/src/index.ts','utf8'); console.assert(s.includes(\"version('1.0.0'\"),'version string'); console.assert(!s.includes(\"version('0.1.0'\"),'old version gone'); console.log('OK')"</automated>
  </verify>
  <done>
    src/index.ts version string reads '1.0.0'. No '0.1.0' remains. TypeScript file is otherwise unchanged.
  </done>
</task>

</tasks>

<verification>
1. `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"` exits 0 (valid JSON).
2. `node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf8')); [p.name,p.version,p.files,p.exports,p.engines].forEach((v,i)=>{if(!v) throw new Error('missing field '+i)}); console.log('all fields present')"` prints success.
3. `grep "1.0.0" src/index.ts` returns a match.
</verification>

<success_criteria>
- package.json name is @tayo/rtl
- package.json version is 1.0.0
- package.json has files, exports, and engines fields
- src/index.ts version string is 1.0.0
- No JSON syntax errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/09-package-publish/09-01-SUMMARY.md` documenting what was changed in package.json and src/index.ts.
</output>
