---
phase: 09-package-publish
plan: "02"
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - dist/index.js
  - dist/index.d.ts
autonomous: false
requirements:
  - PKG-03
  - PKG-04

must_haves:
  truths:
    - "Running tsc exits 0 with no TypeScript errors"
    - "dist/index.js exists after tsc"
    - "node dist/index.js --help prints CLI help without error"
    - "taro --version outputs 1.0.0"
    - "npm publish --dry-run exits 0 and lists dist/, README.md in the tarball"
  artifacts:
    - path: "dist/index.js"
      provides: "Compiled CLI entry point"
    - path: "dist/index.d.ts"
      provides: "Type declarations"
  key_links:
    - from: "package.json bin.taro"
      to: "dist/index.js"
      via: "npm bin symlink on install"
      pattern: "dist/index\\.js"
---

<objective>
Compile the TypeScript source with tsc, verify the resulting dist/ directory is functional, run a dry-run publish to confirm the correct files are included in the tarball, and provide manual publish instructions to the user.

Purpose: Confirm the package builds cleanly and that npm pack selects only the whitelisted distribution files before the user performs the actual publish with their own credentials.
Output: Verified dist/ directory + dry-run output + manual publish instructions at checkpoint.
</objective>

<execution_context>
@/Users/michaelgichia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/michaelgichia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-package-publish/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build with tsc and verify dist output</name>
  <files>dist/index.js</files>
  <action>
    Run the TypeScript build from the project root:

    ```bash
    cd /Users/michaelgichia/workspace/taro/.claude/worktrees/hopeful-austin && npm run build
    ```

    If tsc exits non-zero, report the full error output — do not attempt to fix TypeScript errors silently. Stop and surface all errors to the user.

    After a successful build, run both verification commands:

    ```bash
    node dist/index.js --help
    node dist/index.js --version
    ```

    The --help output must include the `generate` subcommand. The --version output must print `1.0.0`.

    Also confirm dist/ contains at minimum:
    - `dist/index.js`
    - `dist/index.d.ts`
    - `dist/cli/` (subdirectory with compiled command files)
  </action>
  <verify>
    <automated>cd /Users/michaelgichia/workspace/taro/.claude/worktrees/hopeful-austin && npm run build && node dist/index.js --help && node dist/index.js --version | grep -q '1.0.0' && echo "BUILD OK"</automated>
  </verify>
  <done>
    tsc exits 0. dist/index.js exists. `node dist/index.js --help` prints help that includes the generate command. `node dist/index.js --version` prints 1.0.0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Dry-run publish to verify tarball contents</name>
  <files>package.json</files>
  <action>
    Run npm publish in dry-run mode to confirm what files would be included in the tarball:

    ```bash
    cd /Users/michaelgichia/workspace/taro/.claude/worktrees/hopeful-austin && npm publish --dry-run --access public 2>&1
    ```

    Inspect the output. The file list MUST include:
    - Files under `dist/` (JS, d.ts, map files)
    - `README.md`
    - `package.json` (always auto-included by npm)

    The file list MUST NOT include:
    - `src/` directory
    - `node_modules/`
    - `*.test.ts` files
    - `.planning/` directory

    If unexpected files appear (e.g., src/ or node_modules/), the `files` field in package.json is wrong — report the discrepancy without attempting to auto-fix.

    Capture and record the dry-run output (tarball size and file list) for inclusion in the checkpoint summary.
  </action>
  <verify>
    <automated>cd /Users/michaelgichia/workspace/taro/.claude/worktrees/hopeful-austin && npm publish --dry-run --access public 2>&1 | tee /tmp/npm-dry-run.txt && grep -q "dist/" /tmp/npm-dry-run.txt && echo "DRY-RUN OK"</automated>
  </verify>
  <done>
    npm publish --dry-run exits 0. Tarball includes dist/ files and README.md. src/ and node_modules/ are not in the tarball.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual publish instructions</name>
  <action>Present the manual publish instructions to the user. Claude has completed all automated verification steps (build, --help, --version, dry-run). The actual npm publish requires user credentials and must be performed manually.</action>
  <what-built>
    Build and dry-run verification complete:
    - TypeScript compiled to dist/ via tsc
    - `node dist/index.js --help` confirmed working (shows generate subcommand)
    - `node dist/index.js --version` outputs 1.0.0
    - `npm publish --dry-run` confirmed correct tarball contents

    The package is ready for publish. The actual publish requires your npm credentials and must be run manually.
  </what-built>
  <how-to-verify>
    To publish the package to npm:

    1. Ensure you are logged in to npm with an account that has access to the @tayo scope:
       ```bash
       npm whoami
       # If not logged in:
       npm login
       ```

    2. If the @tayo npm scope does not yet exist, create it first at https://www.npmjs.com/org/create

    3. From the project root, publish the scoped package publicly:
       ```bash
       cd /Users/michaelgichia/workspace/taro/.claude/worktrees/hopeful-austin
       npm publish --access public
       ```

    4. Verify the publish succeeded:
       ```bash
       npm view @tayo/rtl
       ```

    5. Test that npx works from a clean environment (outside this project):
       ```bash
       cd /tmp && npx @tayo/rtl generate --help
       ```
       Expected: CLI help prints showing the generate command options.
  </how-to-verify>
  <verify>User confirms publish result</verify>
  <done>User has publish instructions and can run npm publish --access public at their convenience.</done>
  <resume-signal>Type "published" after npm publish succeeds, or "skip" to mark phase complete without publishing (e.g., for local-only testing)</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` exits 0 — no TypeScript errors.
2. `node dist/index.js --help` exits 0 and prints help text containing "generate".
3. `node dist/index.js --version` prints `1.0.0`.
4. `npm publish --dry-run --access public` exits 0 and file list contains `dist/` entries.
5. `dist/` directory exists and contains `index.js` and `index.d.ts`.
</verification>

<success_criteria>
- tsc produces a dist/ directory without TypeScript errors
- node dist/index.js --help works and shows the generate subcommand
- node dist/index.js --version prints 1.0.0
- npm publish --dry-run exits 0 with correct tarball contents (dist, README.md, package.json only)
- User has manual publish instructions for `npm publish --access public`
</success_criteria>

<output>
After completion, create `.planning/phases/09-package-publish/09-02-SUMMARY.md` documenting build verification results, dry-run output summary, and publish instructions provided to user.
</output>
