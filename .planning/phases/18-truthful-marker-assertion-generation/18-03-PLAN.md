---
phase: 18-truthful-marker-assertion-generation
plan: "03"
type: execute
wave: 3
depends_on:
  - 18-01
  - 18-02
files_modified:
  - src/core/generator.ts
  - src/templates/test-template.ts
  - src/cli/commands/generate.ts
  - src/core/generator.test.ts
  - src/cli/commands/generate.test.ts
autonomous: true
requirements:
  - ASSERT-01
  - ASSERT-02
  - ASSERT-03
  - ASSERT-04
  - SAFE-01
  - SAFE-02

must_haves:
  truths:
    - "Generated tests emit explicit visible-proof assertions in scenario bodies, not hidden helper checks or replayed marker gestures"
    - "Role-plus-name, exact visible text or value, and bounded form-context fallback all render as async-safe visible assertions using the winning proof only"
    - "Marker assertions strengthen the generated scenario additively while unresolved or unsupported markers remain absent from emitted proof code"
  artifacts:
    - path: "src/core/generator.ts"
      provides: "Code emission for scenario-level marker assertions with explicit visible proof"
      contains: "generateTestFromGroups"
    - path: "src/templates/test-template.ts"
      provides: "Stable assertion rendering for async-safe visible marker proof"
      contains: "stepTemplate"
    - path: "src/cli/commands/generate.ts"
      provides: "JS generation handoff that preserves planned marker assertions while suppressing marker gesture replay"
      contains: "createGenerateCommand"
    - path: "src/core/generator.test.ts"
      provides: "Generator coverage for marker assertion rendering and helper-boundary ordering"
      contains: "generateTestFromGroups"
    - path: "src/cli/commands/generate.test.ts"
      provides: "Sample-backed public-flow regressions for truthful marker assertion output"
      contains: "createGenerateCommand"
  key_links:
    - from: "src/core/generator.ts"
      to: "src/core/suite-planner.ts"
      via: "planned marker assertions emitted in scenario-body order"
      pattern: "planJsSuite"
    - from: "src/cli/commands/generate.ts"
      to: "src/core/generator.ts"
      via: "public JS generation path for marker assertion output"
      pattern: "generateTestFromGroups"
    - from: "src/cli/commands/generate.test.ts"
      to: "sample/sample-rest-recordingextension-output.js"
      via: "end-to-end marker assertion regression coverage"
      pattern: "sample-rest-recordingextension-output"
---

<objective>
Ship truthful marker assertions in generated RTL output.

Purpose: turn planned marker assertions into explicit, readable scenario-body proof in the final generated code while preserving helper boundaries, additive coverage, and truthful degradation for unresolved markers.
Output: generator and CLI behavior that emits async-safe visible proof, suppresses marker click replay, and is locked by focused generator plus sample-backed public-flow regressions.
</objective>

<execution_context>
@/Users/michaelgichia/.codex/get-shit-done/workflows/execute-plan.md
@/Users/michaelgichia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-truthful-marker-assertion-generation/18-CONTEXT.md
@src/core/generator.ts
@src/templates/test-template.ts
@src/cli/commands/generate.ts
@src/core/generator.test.ts
@src/cli/commands/generate.test.ts
@sample/sample-rest-recordingextension-output.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Emit planned marker assertions as explicit visible proof in scenario bodies</name>
  <files>src/core/generator.ts, src/templates/test-template.ts, src/cli/commands/generate.ts</files>
  <action>
Update JS generation so Phase 18 planned marker assertions become the final emitted RTL proof.

Required outcomes:
1. Render marker-derived assertions only in scenario bodies and keep helper functions assertion-free, even when the anchor action came from a helper.
2. Emit async-safe visible proof using the winning query and matcher combination, such as `findBy*` or an equivalent explicit wait that still reads as a real assertion.
3. When visible text or concrete value proof wins, use the exact recorded text or value as the emitted visible proof rather than a looser inferred matcher.
4. When form-context fallback wins, emit a visibility assertion for the control itself and do not convert surrounding fill/select actions into inferred `toHaveValue(...)` or similar value assertions.
5. Preserve current marker-gesture suppression so original marker `dblClick` steps do not render as user interactions in helpers or scenarios.
6. Keep Phase 19 out of scope: do not add coverage counts, zero-conversion failures, or unresolved-marker reporting here.
  </action>
  <verify>
    <automated>npm run build && npm run test:run -- src/core/generator.test.ts src/cli/commands/generate.test.ts</automated>
  </verify>
  <done>
    Generated JS output contains truthful marker-derived assertions in the right place and no longer relies on replayed marker gestures.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add generator and CLI regressions for strongest-proof output and non-fabrication rules</name>
  <files>src/core/generator.test.ts, src/cli/commands/generate.test.ts</files>
  <action>
Lock the Phase 18 output contract with focused tests and one representative sample-backed flow.

Required outcomes:
1. Add generator tests proving role-and-name proof, exact visible text proof, exact concrete visible value proof, and label-before-placeholder fallback all render as explicit visible assertions.
2. Add coverage proving only one strongest proof is emitted per anchor and that the assertion appears after the helper call when the anchor action lives inside a helper.
3. Add negative coverage proving ambiguous field labels, generic containers, icon-only targets, CSS-only evidence, and unresolved markers do not produce fabricated assertions.
4. Add end-to-end `tayo generate` regressions showing marker clicks stay absent from emitted user actions while the new marker assertions appear in the generated scenario output.
5. Include at least one public-flow regression whose source recording carries a label-backed or placeholder-backed marker so ASSERT-03 is proven on the actual `tayo generate` path, including helper-boundary ordering and marker-click suppression.
6. Keep the tests focused on Phase 18 generation behavior rather than Phase 19 reporting or scoring gates.
  </action>
  <verify>
    <automated>npm run build && npm run test:run -- src/core/generator.test.ts src/cli/commands/generate.test.ts</automated>
  </verify>
  <done>
    The shipped generation path is locked against regressions in marker assertion placement, strongest-proof selection, and non-fabrication guardrails.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds.
2. `npm run test:run -- src/core/generator.test.ts src/cli/commands/generate.test.ts` succeeds.
3. Generated output shows explicit visible marker assertions after the anchor action or helper call, not inside helpers.
4. Only one strongest proof is emitted per anchor, and original marker `dblClick` gestures do not appear as user actions.
5. Text and value winners use exact recorded proof, while form-context fallback asserts visibility only and never inferred values.
6. A public `tayo generate` regression proves label/placeholder fallback from source marker to emitted visibility assertion.
7. Unsupported or unresolved markers remain absent from emitted proof code.
</verification>

<success_criteria>
- Scenario bodies emit explicit, async-safe marker proof
- Helper functions remain assertion-free
- Strongest-proof and exact-proof behavior are covered end to end
- ASSERT-03 is covered on the public `tayo generate` path, not only in unit tests
- Unsupported evidence still produces no fabricated assertion output
</success_criteria>

<output>
After completion, create `.planning/phases/18-truthful-marker-assertion-generation/18-03-SUMMARY.md` documenting final marker assertion emission, helper-boundary ordering, and sample-backed regression coverage.
</output>
