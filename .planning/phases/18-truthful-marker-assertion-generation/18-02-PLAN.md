---
phase: 18-truthful-marker-assertion-generation
plan: "02"
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - src/types/recording.ts
  - src/core/suite-planner.ts
  - src/core/suite-planner.test.ts
autonomous: true
requirements:
  - ASSERT-04
  - SAFE-01
  - SAFE-02

must_haves:
  truths:
    - "Marker-derived assertions are placed in scenario bodies immediately after the verified anchor action, or immediately after the helper call when that anchor lives inside a helper"
    - "Helper functions remain assertion-free and sync-only even when they contain the anchor action that a marker verifies"
    - "Only one strongest resolved proof is planned per anchor action; weaker duplicates and unresolved markers do not create clusters of redundant expectations"
    - "Every Phase 18-convertible marker is reified into planner-visible marker state before generation, so marker gestures no longer leak through as replayed click steps"
  artifacts:
    - path: "src/types/recording.ts"
      provides: "Planned marker-assertion metadata that suite planning and generation can share"
      contains: "PlannedMarkerAssertion"
    - path: "src/core/suite-planner.ts"
      provides: "Scenario-level placement of resolved marker assertions without leaking them into helpers"
      contains: "planJsSuite"
    - path: "src/core/suite-planner.test.ts"
      provides: "Regression coverage for helper-boundary placement and strongest-proof selection"
      contains: "planJsSuite"
  key_links:
    - from: "src/core/suite-planner.ts"
      to: "src/core/resolver.ts"
      via: "resolved marker assertions attached to planned scenario structure"
      pattern: "resolveSemanticMarkerAssertion"
    - from: "src/core/suite-planner.test.ts"
      to: "src/core/suite-planner.ts"
      via: "scenario-body placement and helper-boundary regressions"
      pattern: "planJsSuite"
---

<objective>
Attach truthful marker assertions to the correct scenario block without contaminating helpers.

Purpose: translate resolved marker evidence into planned scenario-body assertions that appear exactly where users expect them in generated tests, while keeping helpers as navigation utilities only.
Output: suite-planner support for marker-derived assertion placement, strongest-proof dedupe per anchor, and focused tests around helper boundaries and additive behavior.
</objective>

<execution_context>
@/Users/michaelgichia/.codex/taro/workflows/execute-plan.md
@/Users/michaelgichia/.codex/taro/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-truthful-marker-assertion-generation/18-CONTEXT.md
@src/types/recording.ts
@src/core/resolver.ts
@src/core/suite-planner.ts
@src/core/suite-planner.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend suite-plan shapes to carry scenario-level marker assertions</name>
  <files>src/types/recording.ts, src/core/suite-planner.ts</files>
  <action>
Refactor suite-planning data so resolved markers can be emitted at the scenario layer without treating helper steps as assertion owners.

Required outcomes:
1. Add or extend planning metadata so a scenario can carry marker-derived assertion steps or equivalent assertion-intent records tied to a specific anchor step.
2. Preserve helper `assertionPolicy: 'sync-only'` and avoid adding marker expectations directly to helper bodies.
3. Keep the planned assertion placement deterministic when the anchor step belongs to a helper: the helper call should stay in the scenario, and the marker assertion should appear immediately after that call.
4. Preserve existing non-marker scenario structure and ordinary parser-driven assertion steps.
5. Reify every Phase 18-convertible marker into explicit planner-visible marker state before generation, so candidate-only marker steps cannot survive downstream as ordinary click interactions.
  </action>
  <verify>
    <automated>npm run build && npm run test:run -- src/core/suite-planner.test.ts</automated>
  </verify>
  <done>
    The suite-planner contract can express scenario-body marker assertions while keeping helper plans assertion-free.
  </done>
</task>

<task type="auto">
  <name>Task 2: Select one strongest proof per anchor and place it additively in planned scenarios</name>
  <files>src/core/suite-planner.ts, src/core/suite-planner.test.ts</files>
  <action>
Use the Phase 18 resolver output to attach marker-derived assertions to the correct scenario position.

Required outcomes:
1. For each anchor action, choose only one resolved marker assertion using the locked strength order and ignore weaker duplicates once a stronger proof exists.
2. Insert the winning marker assertion immediately after the anchor action in the scenario body, or immediately after the helper call if the anchor action is helper-owned.
3. Keep original scenario coverage additive: marker assertions must strengthen the scenario without replacing happy-path, validation, or failure-flow steps already planned.
4. Leave unresolved or unsupported markers as non-emitted metadata only; do not force placeholder assertions into scenario plans.
5. Ensure candidate-only marker steps that become Phase 18-resolved or explicitly unresolved are no longer eligible to replay as ordinary click interactions in downstream generation.
6. Add planner tests proving helper-boundary placement, strongest-proof selection, unresolved-marker non-emission, replay suppression readiness, and continued helper sync-only policy.
  </action>
  <verify>
    <automated>npm run build && npm run test:run -- src/core/suite-planner.test.ts</automated>
  </verify>
  <done>
    Planned scenarios now know exactly where one truthful marker assertion belongs, and helpers remain clean navigation helpers.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds.
2. `npm run test:run -- src/core/suite-planner.test.ts` succeeds.
3. Resolved marker assertions are planned immediately after the anchor action or helper call that they verify.
4. Only one strongest proof is planned per anchor, unresolved markers do not generate placeholder assertions, and converted marker steps no longer remain ordinary click candidates downstream.
5. Helpers remain `sync-only` and assertion-free.
</verification>

<success_criteria>
- Suite planning can carry scenario-body marker assertions explicitly
- Helper-owned anchors still yield scenario-level assertions right after the helper call
- Duplicate markers collapse to one strongest proof per anchor
- Converted markers are reified into non-replayed planner state before generation
- Marker assertions remain additive and do not replace existing scenario coverage
</success_criteria>

<output>
After completion, create `.planning/phases/18-truthful-marker-assertion-generation/18-02-SUMMARY.md` documenting suite-planner placement, strongest-proof selection, and helper-boundary behavior for marker assertions.
</output>
