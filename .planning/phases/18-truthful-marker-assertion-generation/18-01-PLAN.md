---
phase: 18-truthful-marker-assertion-generation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/recording.ts
  - src/core/resolver.ts
  - src/core/resolver.test.ts
  - src/core/recording-intelligence.ts
  - src/core/recording-intelligence.test.ts
autonomous: true
requirements:
  - ASSERT-01
  - ASSERT-02
  - ASSERT-03
  - SAFE-02

must_haves:
  truths:
    - "Resolved semantic markers use one explicit strongest-evidence order only: role-plus-name first, then exact visible text or concrete visible value, then narrow form-context fallback"
    - "Field-label recorder markers that may truthfully map to one real control survive analysis as Phase 18-consumable marker evidence instead of being flattened into ordinary click noise"
    - "Only visible, user-facing proof may resolve; screenshots, hidden implementation details, generic containers, icon-only targets, CSS-only selectors, and ambiguous field associations remain unresolved"
    - "Marker conversion never infers field values from prior user actions; form-context fallback proves control visibility only, while text or value proof uses the exact recorded visible proof when that evidence wins"
  artifacts:
    - path: "src/types/recording.ts"
      provides: "Structured marker-assertion resolution types shared by planning and generation"
      contains: "SemanticMarkerAssertion"
    - path: "src/core/resolver.ts"
      provides: "Truthful semantic-marker assertion resolution with bounded fallback policy"
      contains: "resolveSemanticMarkerAssertion"
    - path: "src/core/resolver.test.ts"
      provides: "Focused evidence-order and guardrail coverage for marker assertion resolution"
      contains: "resolveSemanticMarkerAssertion"
    - path: "src/core/recording-intelligence.ts"
      provides: "Analysis-layer preservation of Phase 18 field-context marker evidence with truthful unresolved handling"
      contains: "analyzeRecording"
    - path: "src/core/recording-intelligence.test.ts"
      provides: "Regression coverage proving form-context marker candidates survive cleanup without being upgraded into fabricated proof"
      contains: "filterNoiseSteps"
  key_links:
    - from: "src/core/resolver.ts"
      to: "src/types/recording.ts"
      via: "marker-assertion resolution contract"
      pattern: "SemanticMarkerAssertion"
    - from: "src/core/resolver.test.ts"
      to: "src/core/resolver.ts"
      via: "truthful evidence-order and guardrail regression coverage"
      pattern: "resolveSemanticMarkerAssertion"
    - from: "src/core/recording-intelligence.ts"
      to: "src/core/resolver.ts"
      via: "field-context marker evidence preserved through analysis for later truthful resolution"
      pattern: "field-label"
---

<objective>
Define the truthful marker-assertion resolution contract for Phase 18.

Purpose: convert semantic-marker evidence into a structured assertion intent only when user-facing proof is strong enough, while leaving weak or ambiguous markers unresolved instead of fabricating output.
Output: reusable assertion-resolution types, analysis support for field-context marker survival, resolver logic for strongest-proof selection, and unit coverage for the locked evidence order and guardrails.
</objective>

<execution_context>
@/Users/michaelgichia/.codex/get-shit-done/workflows/execute-plan.md
@/Users/michaelgichia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-truthful-marker-assertion-generation/18-CONTEXT.md
@src/types/recording.ts
@src/core/recording-intelligence.ts
@src/core/recording-intelligence.test.ts
@src/core/resolver.ts
@src/core/resolver.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a structured semantic-marker assertion contract</name>
  <files>src/types/recording.ts, src/core/recording-intelligence.ts</files>
  <action>
Extend the recording/generation contracts so Phase 18 has an explicit representation for truthful marker assertions and explicit unresolved outcomes.

Required outcomes:
1. Add a marker-assertion type that can represent the winning proof kind, the exact query expression to use, the matcher or visibility expectation to emit, the linked anchor step, and enough source metadata for downstream placement.
2. Model resolution outcomes so later planning and generation can distinguish resolved assertions from unresolved marker evidence without re-deriving the policy ad hoc.
3. Keep the contract Phase 18 scoped: no Phase 19 coverage counts, zero-conversion quality gates, or warning/report aggregation in this plan.
4. Preserve compatibility with existing marker-link metadata from Phase 17 and the current generator/query descriptor shapes whenever practical.
5. Extend analysis-layer marker preservation so `field-label` recorder markers that could truthfully resolve to a single real form control survive cleanup as Phase 18-consumable evidence, with valid anchor linkage or explicit unresolved state rather than being dropped as noise.
  </action>
  <verify>
    <automated>npm run build && npm run test:run -- src/core/recording-intelligence.test.ts src/core/resolver.test.ts</automated>
  </verify>
  <done>
    The repo has an explicit contract for truthful marker assertions, and Phase 18-eligible form-context marker evidence survives analysis instead of disappearing upstream.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement strongest-proof marker resolution and lock the guardrails with unit tests</name>
  <files>src/core/resolver.ts, src/core/resolver.test.ts, src/core/recording-intelligence.test.ts</files>
  <action>
Implement the Phase 18 evidence-order policy in the resolver seam that already understands accessible query recovery.

Required outcomes:
1. Resolve to role-and-name assertions when accessible role evidence and an accessible name are present, reusing existing query evidence before weaker fallback logic.
2. When stronger accessible evidence is absent, resolve truthfully to exact visible text or exact concrete visible value proof before any form-context fallback.
3. Allow label-or-placeholder fallback only for real form controls such as textbox, textarea, combobox, spinbutton, checkbox, and radio-style inputs; within that fallback, prefer label-based queries before placeholder-based queries.
4. Leave markers unresolved when field association is weak, multiple controls could match, the target is a generic wrapper or read-only review row, the evidence is CSS-only or icon-only, or the proof depends on hidden implementation details.
5. Produce async-safe assertion intent so downstream generation can wait for proof to appear before asserting visibility.
6. Add focused tests covering the strongest-proof order, exact text/value behavior, label-before-placeholder precedence, and negative cases for ambiguity and unsupported evidence.
7. Add analysis-layer regressions proving qualifying form-context candidates now survive cleanup while ambiguous field-adjacent markers still remain unresolved and non-fabricated.
  </action>
  <verify>
    <automated>npm run build && npm run test:run -- src/core/recording-intelligence.test.ts src/core/resolver.test.ts</automated>
  </verify>
  <done>
    Semantic markers now resolve only when truthful visible proof exists, and the locked fallback policy is covered by fast unit tests.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds.
2. `npm run test:run -- src/core/recording-intelligence.test.ts src/core/resolver.test.ts` succeeds.
3. Resolver outputs respect the exact evidence order: role-plus-name, then exact text/value, then bounded label-or-placeholder fallback.
4. Field-context marker candidates needed for ASSERT-03 survive analysis in a form Phase 18 can consume, while ambiguous form context, generic containers, icon-only targets, CSS-only selectors, and hidden implementation details stay unresolved.
5. Resolved form-context assertions prove visibility only and do not infer extra value assertions from surrounding interactions.
</verification>

<success_criteria>
- Marker-assertion resolution types exist and are reusable downstream
- Analysis preserves ASSERT-03 candidate evidence instead of flattening it away
- Resolver enforces the locked strongest-proof order
- Unsupported or ambiguous evidence remains unresolved instead of fabricated
- Unit coverage locks label-before-placeholder, exact-proof, and async-safe behavior in place
</success_criteria>

<output>
After completion, create `.planning/phases/18-truthful-marker-assertion-generation/18-01-SUMMARY.md` documenting the marker-assertion resolution contract, field-context marker preservation, strongest-proof order, and bounded fallback guardrails.
</output>
