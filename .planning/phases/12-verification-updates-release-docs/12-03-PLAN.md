---
phase: 12-verification-updates-release-docs
plan: "03"
type: execute
wave: 3
depends_on:
  - "12-01"
  - "12-02"
files_modified:
  - README.md
autonomous: true
requirements:
  - DIST-04

must_haves:
  truths:
    - "The README is installer-first and matches the real `@tayo-dev/rtl` onboarding flow"
    - "Interactive, non-interactive, update, and development installation paths are all documented using the actual shipped commands"
    - "Release-facing docs only describe behavior already proven by Phase 12 automation"
  artifacts:
    - path: "README.md"
      provides: "Release-ready onboarding and installation documentation"
      contains: "Getting Started"
  key_links:
    - from: "README.md"
      to: "src/cli/commands/install.ts"
      via: "documented installer flags and flow"
      pattern: "--all"
    - from: "README.md"
      to: "src/install/verification.ts"
      via: "documented verification commands reflect the verification contract"
      pattern: "@tayo-dev/rtl:help"
---

<objective>
Rewrite release-facing docs around the installer-first package and verified runtime onboarding flow.

Purpose: satisfy the documentation requirement only after rerun semantics and verification behavior are stable and tested.
Output: README sections for interactive install, non-interactive install, staying updated, development installation, and verification commands.
</objective>

<execution_context>
@/Users/michaelgichia/.codex/get-shit-done/workflows/execute-plan.md
@/Users/michaelgichia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-verification-updates-release-docs/12-RESEARCH.md
@.planning/phases/12-verification-updates-release-docs/12-VALIDATION.md
@README.md
@package.json
@src/cli/commands/install.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README onboarding around installer-first usage</name>
  <files>README.md</files>
  <action>
Update the README to match the real shipped installer behavior.

Required outcomes:
1. Add a Getting Started section centered on `npx @tayo-dev/rtl@latest` and the runtime/location prompts.
2. Document non-interactive install commands for Claude Code, OpenCode, Gemini CLI, Codex, and all runtimes.
3. Document staying-updated guidance using the same installer package.
4. Add a development installation section that reflects package-local testing or development usage accurately.
5. Preserve generator documentation, but move it after installer onboarding so the README matches the current product entrypoint.
  </action>
  <verify>
    <automated>rg -n "Getting Started|Staying Updated|Non-interactive Install|Development Installation|@tayo-dev/rtl@latest" README.md</automated>
  </verify>
  <done>
    README onboarding is installer-first and covers the required release-ready flows.
  </done>
</task>

<task type="auto">
  <name>Task 2: Align README verification commands with the tested Phase 12 contract</name>
  <files>README.md</files>
  <action>
Make the README verification guidance trustworthy.

Required outcomes:
1. Document the real runtime-specific verification commands for Claude Code, OpenCode, Gemini CLI, and Codex.
2. Keep the documented commands aligned with the automated verification contract established in Plan 12-02.
3. Avoid speculative or future-state instructions that are not proven by the code and package smoke checks.
4. Ensure the README still explains how `tayo generate` fits after installation.
  </action>
  <verify>
    <automated>npm pack --dry-run</automated>
  </verify>
  <done>
    The README reflects only verified installer and runtime behavior.
  </done>
</task>

</tasks>

<verification>
1. `rg -n "Getting Started|Staying Updated|Non-interactive Install|Development Installation" README.md` succeeds.
2. `npm pack --dry-run` succeeds after the README update.
3. Manual review confirms the README reads as installer-first and matches the runtime verification commands produced by the code.
</verification>

<success_criteria>
- README is installer-first
- Documented install and update flows match actual CLI behavior
- Verification commands in docs match tested runtime verification logic
- Development installation guidance matches the shipped package boundary
</success_criteria>

<output>
After completion, create `.planning/phases/12-verification-updates-release-docs/12-03-SUMMARY.md` documenting the README and release-doc changes.
</output>
