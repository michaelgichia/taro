---
phase: 12-verification-updates-release-docs
plan: "02"
type: execute
wave: 2
depends_on:
  - "12-01"
files_modified:
  - src/install/verification.ts
  - src/install/verification.test.ts
  - src/cli/commands/install.ts
  - src/cli/commands/install.test.ts
  - package.json
autonomous: true
requirements:
  - DIST-03

must_haves:
  truths:
    - "Runtime-specific verification commands are backed by explicit verification logic instead of only printed strings"
    - "Package-level verification proves the shipped tarball contains the runtime assets needed by the installer"
    - "Install completion output is derived from the same verification contract used by automated tests"
  artifacts:
    - path: "src/install/verification.ts"
      provides: "Runtime verification helpers for installed assets and command expectations"
      contains: "verifyInstalledRuntime"
    - path: "src/install/verification.test.ts"
      provides: "Automated runtime verification coverage"
      contains: "verificationCommand"
    - path: "package.json"
      provides: "Release-facing package smoke command or script entry"
      contains: "pack"
  key_links:
    - from: "src/cli/commands/install.ts"
      to: "src/install/verification.ts"
      via: "install completion reporting and verification trust"
      pattern: "verifyInstalledRuntime"
    - from: "src/install/verification.test.ts"
      to: "package.json"
      via: "package-level smoke proof"
      pattern: "npm pack"
---

<objective>
Make runtime verification commands trustworthy and prove the shipped package contains the installer assets they rely on.

Purpose: turn Phase 11’s printed verification commands into tested, package-backed verification behavior.
Output: runtime verification helpers, automated checks, and package smoke proof for the tarball boundary.
</objective>

<execution_context>
@/Users/michaelgichia/.codex/get-shit-done/workflows/execute-plan.md
@/Users/michaelgichia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-verification-updates-release-docs/12-RESEARCH.md
@.planning/phases/12-verification-updates-release-docs/12-VALIDATION.md
@src/install/writer.ts
@src/install/summary.ts
@src/cli/commands/install.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add runtime verification helpers and CLI integration</name>
  <files>src/install/verification.ts, src/cli/commands/install.ts, src/cli/commands/install.test.ts</files>
  <action>
Implement the verification layer for installed runtimes.

Required outcomes:
1. Add a verification module that can inspect an installed runtime target and confirm the documented verification command maps to the expected installed asset or skill.
2. Integrate that verification contract into install completion behavior so verification output is grounded in checked state rather than only metadata.
3. Keep verification filesystem-local and deterministic; do not depend on launching external runtime binaries.
4. Preserve per-runtime reporting for Claude Code, Gemini CLI, OpenCode, and Codex.
  </action>
  <verify>
    <automated>rg -n "verifyInstalledRuntime|verificationCommand" src/install/verification.ts src/cli/commands/install.ts src/cli/commands/install.test.ts</automated>
  </verify>
  <done>
    Verification commands are supported by explicit verification logic and surfaced through the install flow.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add package smoke proof for the shipped tarball</name>
  <files>src/install/verification.test.ts, package.json</files>
  <action>
Prove the packaged artifact contains the runtime assets used by verification.

Required outcomes:
1. Add automated tests or a deterministic smoke harness that exercises verification against the package boundary.
2. Use `npm pack` as part of the package-level proof so verification is not repo-only.
3. Assert that the packed artifact includes `dist`, `assets`, and the runtime files required by the documented verification commands.
4. Keep the smoke proof suitable for CI and local development.
  </action>
  <verify>
    <automated>npm run test:run -- src/install/verification.test.ts</automated>
  </verify>
  <done>
    Runtime verification is proven against the package boundary, not only the workspace tree.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds.
2. `npm run test:run -- src/install/verification.test.ts src/cli/commands/install.test.ts` succeeds.
3. `npm pack` completes and the resulting tarball contains runtime assets alongside `dist`.
4. Automated checks prove the documented verification commands map to real installed assets for all four runtimes.
</verification>

<success_criteria>
- Verification commands are backed by runtime inspection logic
- Tarball verification proves the shipped package includes runtime assets
- Install completion output reflects verified runtime state
- DIST-03 has automated proof instead of string-only reporting
</success_criteria>

<output>
After completion, create `.planning/phases/12-verification-updates-release-docs/12-02-SUMMARY.md` documenting runtime verification and package smoke coverage.
</output>
