---
phase: 12-verification-updates-release-docs
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/install/writer.ts
  - src/install/executor.ts
  - src/install/summary.ts
  - src/cli/commands/install.ts
  - src/install/write-execution.test.ts
  - src/cli/commands/install.test.ts
autonomous: true
requirements:
  - DIST-02

must_haves:
  truths:
    - "Re-running the installer repairs missing owned assets and refreshes unchanged owned assets without manual cleanup"
    - "User-edited installer assets remain protected and are not silently overwritten during repair/update runs"
    - "Runtime-level results distinguish repaired or updated paths from protected or blocked paths"
  artifacts:
    - path: "src/install/writer.ts"
      provides: "Safe rerun and repair semantics for installer-owned assets"
      contains: "writeInstallPlan"
    - path: "src/install/executor.ts"
      provides: "Runtime result aggregation that preserves repair/protection outcomes"
      contains: "executeInstallPlan"
    - path: "src/install/write-execution.test.ts"
      provides: "Automated rerun and repair coverage"
      contains: "installer-owned-modified"
  key_links:
    - from: "src/cli/commands/install.ts"
      to: "src/install/writer.ts"
      via: "rerun behavior surfaced through the install command"
      pattern: "executeInstallPlan"
    - from: "src/install/write-execution.test.ts"
      to: "src/install/writer.ts"
      via: "repair and protection assertions"
      pattern: "writeInstallPlan"
---

<objective>
Make installer reruns safe and repair-capable for owned assets.

Purpose: satisfy the “rerun without manual cleanup” requirement by evolving the Phase 11 write path from conservative first-write behavior into deterministic update/repair behavior.
Output: updated write semantics, clearer rerun reporting, and tests that prove repairable assets succeed while protected assets stay untouched.
</objective>

<execution_context>
@/Users/michaelgichia/.codex/taro/workflows/execute-plan.md
@/Users/michaelgichia/.codex/taro/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-verification-updates-release-docs/12-RESEARCH.md
@.planning/phases/12-verification-updates-release-docs/12-VALIDATION.md
@src/install/writer.ts
@src/install/executor.ts
@src/install/summary.ts
@src/cli/commands/install.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Evolve write semantics for safe reruns and repairs</name>
  <files>src/install/writer.ts, src/install/executor.ts, src/install/summary.ts</files>
  <action>
Implement Phase 12 repair/update behavior for installer-owned assets.

Required outcomes:
1. Treat unchanged installer-owned assets as safe to refresh during reruns instead of requiring manual confirmation.
2. Recreate missing installer-owned assets when a manifest proves Tayo owns the runtime install.
3. Keep user-edited installer-owned assets protected and visible in the result output.
4. Keep colliding non-Tayo files blocked and untouched.
5. Produce result states that distinguish successful repair/update from protected or blocked paths.

Do not add destructive cleanup of unrelated runtime files in this plan.
  </action>
  <verify>
    <automated>rg -n "installer-owned|installer-owned-modified|external-collision|repair|update" src/install/writer.ts src/install/executor.ts src/install/summary.ts</automated>
  </verify>
  <done>
    The writer/executor contract supports safe reruns and repair of owned assets without weakening protection guarantees.
  </done>
</task>

<task type="auto">
  <name>Task 2: Cover rerun and repair behavior through CLI and integration tests</name>
  <files>src/install/write-execution.test.ts, src/cli/commands/install.test.ts, src/cli/commands/install.ts</files>
  <action>
Add automated coverage for the Phase 12 rerun contract.

Required outcomes:
1. Tests prove an unchanged owned install can be rerun non-interactively without manual cleanup.
2. Tests prove a missing owned asset is recreated during repair.
3. Tests prove user-edited installer-owned assets remain protected.
4. CLI-level output reflects repaired/updated/protected runtime outcomes clearly.
5. Existing first-install success cases remain green.
  </action>
  <verify>
    <automated>npm run test:run -- src/install/write-execution.test.ts src/cli/commands/install.test.ts</automated>
  </verify>
  <done>
    Repair/update behavior is covered at both the write layer and the CLI layer.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds.
2. `npm run test:run -- src/install/write-execution.test.ts src/cli/commands/install.test.ts` succeeds.
3. A temp-directory rerun after deleting an owned asset repairs the missing file without manual cleanup.
4. A temp-directory rerun after manually editing an owned asset reports protection and preserves the manual edit.
</verification>

<success_criteria>
- Reruns refresh unchanged owned assets automatically
- Missing owned assets are repaired automatically
- User-edited owned assets remain protected
- CLI reporting makes update/repair outcomes visible
</success_criteria>

<output>
After completion, create `.planning/phases/12-verification-updates-release-docs/12-01-SUMMARY.md` documenting the repair/update semantics and rerun coverage.
</output>
