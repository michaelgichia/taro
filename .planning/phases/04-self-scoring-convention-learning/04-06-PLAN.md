---
phase: 04-self-scoring-convention-learning
plan: 06
type: execute
wave: 1
depends_on: [05]
files_modified:
  - src/core/orchestrator.ts
autonomous: true
user_setup: []
gap_closure: true

must_haves:
  truths:
    - "Output is scored before writing - scoreTest explicitly called and displayed"
    - "Post-write verification runs - uncommented and called after file write"
    - "File actually written - writeFileSync called after audit passes"
    - "Convention learning invoked - learnConventions actually called"
  artifacts:
    - path: "src/core/orchestrator.ts"
      provides: "Complete integration of scorer/learner into generation pipeline"
  key_links:
    - from: "src/core/orchestrator.ts"
      to: "src/scorer/index.ts"
      via: "scoreTest called for user-visible quality score"
    - from: "src/core/orchestrator.ts"
      to: "fs"
      via: "writeFileSync to persist test files"
---

<objective>
Complete the integration - enable all pipeline stages

Purpose: Close remaining gaps from second VERIFICATION.md - enable score display, file writing, post-verify, and convention learning
Output: Fully functional generation pipeline with all scoring/audit/verify/learn stages
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/core/orchestrator.ts
@.planning/phases/04-self-scoring-convention-learning/04-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable post-write verification</name>
  <files>src/core/orchestrator.ts</files>
  <action>
    Uncomment line 141 (postWriteVerification call) and ensure it runs after file write:
    - Remove the // comment prefix
    - Ensure it receives the actual file path
    - Log the verification results
  </action>
  <verify>TypeScript compiles, postWriteVerification is called in the flow</verify>
  <done>Post-write verification enabled</done>
</task>

<task type="auto">
  <name>Task 2: Enable actual file writing</name>
  <files>src/core/orchestrator.ts</files>
  <action>
    Replace the "Would write" log (lines 135-136) with actual file write:
    - Import { writeFileSync } from 'fs' if not already imported
    - Create output directory if needed (mkdirSync with recursive: true)
    - Call writeFileSync to write test code to file
    - Log success with file path
  </action>
  <verify>Test file is actually created on disk after generation</verify>
  <done>Test files written to disk</done>
</task>

<task type="auto">
  <name>Task 3: Invoke convention learning</name>
  <files>src/core/orchestrator.ts</files>
  <action>
    Replace the "Convention learning complete" log (line 147) with actual call:
    - Call learnConventions(process.cwd()) to analyze existing tests
    - Handle result (log success/failure)
    - This should run after successful test generation
  </action>
  <verify>learnConventions is invoked during pipeline execution</verify>
  <done>Convention learning invoked</done>
</task>

<task type="auto">
  <name>Task 4: Display quality score to user</name>
  <files>src/core/orchestrator.ts</files>
  <action>
    Add explicit scoreTest call before file write:
    - Call scoreTest(generatedCode) before preWriteAudit
    - Log the quality score to user (e.g., "Quality Score: 85/100")
    - Include criteria breakdown in log
  </action>
  <verify>Quality score displayed before file is written</verify>
  <done>Quality score visible to user</done>
</task>

</tasks>

<verification>
- postWriteVerification runs after file is written
- Test file exists on disk after generation
- learnConventions is called during pipeline
- Quality score displayed to user before file write
</verification>

<success_criteria>
- [ ] postWriteVerification is called (not commented)
- [ ] writeFileSync writes actual file
- [ ] learnConventions() is invoked
- [ ] scoreTest() output displayed to user
</success_criteria>

<output>
After completion, create `.planning/phases/04-self-scoring-convention-learning/04-06-SUMMARY.md`
</output>
