---
phase: 04-self-scoring-convention-learning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scorer/index.ts
  - src/scorer/types.ts
  - src/scorer/quality-gates.ts
  - package.json
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Output is scored before writing - generated tests receive quality score"
    - "Quality gates evaluate test structure, query robustness, and assertion quality"
    - "Scoring uses AST-based analysis via @typescript-eslint/typescript-estree"
  artifacts:
    - path: "src/scorer/index.ts"
      provides: "Main scorer export and orchestrator"
      exports: ["scoreTest", "QualityScore"]
    - path: "src/scorer/types.ts"
      provides: "Type definitions for scoring"
      exports: ["QualityScore", "QualityCriteria", "ScoringResult"]
    - path: "src/scorer/quality-gates.ts"
      provides: "AST-based quality gate evaluation"
      exports: ["evaluateQualityGates", "StructureRules", "QueryRules"]
  key_links:
    - from: "src/scorer/index.ts"
      to: "src/scorer/quality-gates.ts"
      via: "import and call evaluateQualityGates"
    - from: "src/scorer/quality-gates.ts"
      to: "@typescript-eslint/typescript-estree"
      via: "AST parsing for code analysis"
---

<objective>
Create scorer infrastructure that evaluates generated test quality before and after file creation

Purpose: Implements SCR-01 (output scored before writing) using AST-based quality gates
Output: Working scorer module with quality evaluation capabilities
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/types/recording.ts
@.planning/phases/04-self-scoring-convention-learning/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up scorer module structure</name>
  <files>src/scorer/index.ts, src/scorer/types.ts, package.json</files>
  <action>
    Create src/scorer/ directory with:
    - src/scorer/types.ts: Define QualityScore interface with criteria (structure, queries, matchers, noFragility), overall score (0-100), issues array
    - src/scorer/index.ts: Create scorer orchestrator with scoreTest() function that takes code string and returns QualityScore
    
    Install dependencies:
    - npm install @typescript-eslint/typescript-estree eslint typescript --save
    
    Export QualityScore and scoreTest from index.ts
  </action>
  <verify>npm install completes, TypeScript compiles without errors, src/scorer/index.ts exports scoreTest</verify>
  <done>Scorer module structure exists with type definitions and main orchestrator</done>
</task>

<task type="auto">
  <name>Task 2: Implement quality gates evaluation</name>
  <files>src/scorer/quality-gates.ts</files>
  <action>
    Create src/scorer/quality-gates.ts with:
    - parse() from @typescript-eslint/typescript-estree to convert code to AST
    - evaluateQualityGates(code: string): QualityScore function
    - AST traversal to detect:
      - Structure: has describe block, has it/test block
      - Queries: count robust queries (getByRole, getByLabelText, getByText) vs fragile (querySelector, getByTestId)
      - Matchers: has expect statements with matchers
      - NoFragility: no CSS selectors or test IDs as primary queries
    - Calculate weighted score: structure 25%, queries 25%, matchers 30%, noFragility 20%
  </action>
  <verify>Run: npx ts-node -e "import { evaluateQualityGates } from './src/scorer/quality-gates'; console.log(evaluateQualityGates('describe(\"test\", () => { it(\"works\", () => { expect(true).toBe(true); }); });'))"</verify>
  <done>Quality gates evaluate code and return structured score with criteria breakdown</done>
</task>

</tasks>

<verification>
- Scorer module compiles without TypeScript errors
- Quality gates correctly identify describe/it blocks in test code
- Score calculation produces 0-100 overall with criteria breakdown
</verification>

<success_criteria>
- [ ] src/scorer/index.ts exports scoreTest function
- [ ] src/scorer/types.ts exports QualityScore interface
- [ ] Quality gates return breakdown: structure, queries, matchers, noFragility
- [ ] Score ranges 0-100 with meaningful differentiation
</success_criteria>

<output>
After completion, create `.planning/phases/04-self-scoring-convention-learning/04-01-SUMMARY.md`
</output>
