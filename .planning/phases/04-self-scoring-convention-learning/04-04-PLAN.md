---
phase: 04-self-scoring-convention-learning
plan: 04
type: execute
wave: 2
depends_on: [03]
files_modified:
  - src/learner/storage.ts
  - src/learner/index.ts
  - package.json
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Conventions persist across runs - learned patterns are stored in .taro/ directory"
    - "SQLite database stores conventions with versioning"
    - "Convention store provides load/save functionality for learned patterns"
  artifacts:
    - path: "src/learner/storage.ts"
      provides: "SQLite-based convention persistence"
      exports: ["ConventionStore", "createStore"]
  key_links:
    - from: "src/learner/index.ts"
      to: "src/learner/storage.ts"
      via: "import ConventionStore for persistence"
    - from: "src/learner/storage.ts"
      to: ".taro/conventions.db"
      via: "SQLite database file in project .taro directory"
---

<objective>
Implement convention persistence using SQLite storage

Purpose: Implements CNV-02 (conventions persist across runs) and CNV-03 (faster subsequent runs via caching)
Output: ConventionStore class with SQLite persistence in .taro/ directory
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/learner/types.ts
@src/learner/analyzer.ts
@.planning/phases/04-self-scoring-convention-learning/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install better-sqlite3 dependency</name>
  <files>package.json</files>
  <action>
    Install better-sqlite3 for SQLite storage:
    - npm install better-sqlite3 --save
    - npm install -D @types/better-sqlite3
    
    Note: better-sqlite3 requires native compilation. If issues occur, the code should gracefully handle it or suggest sql.js as alternative.
  </action>
  <verify>npm install completes and better-sqlite3 is in package.json dependencies</verify>
  <done>better-sqlite3 installed for SQLite storage</done>
</task>

<task type="auto">
  <name>Task 2: Create ConventionStore with SQLite persistence</name>
  <files>src/learner/storage.ts</files>
  <action>
    Create src/learner/storage.ts with:
    - ConventionStore class with:
      - constructor(dbPath: string) - opens/creates SQLite database
      - init() - creates tables: conventions (key, value, updated_at), cache (key, value, expires_at)
      - saveConventions(conventions: TestConvention) - stores all convention categories
      - loadConventions(): TestConvention | null - loads stored conventions
      - getCached(key: string): any | null - retrieves cached value
      - setCached(key: string, value: any, ttlSeconds?: number) - stores cached value with optional TTL
    
    Database schema:
    - conventions: id, key (unique), value (JSON), updated_at
    - cache: key (primary), value (JSON), expires_at
    
    Store in .taro/ directory relative to project root
  </action>
  <verify>ConventionStore can save and load conventions without data loss</verify>
  <done>ConventionStore provides SQLite-based persistence</done>
</task>

<task type="auto">
  <name>Task 3: Integrate storage into learner orchestrator</name>
  <files>src/learner/index.ts</files>
  <action>
    Update src/learner/index.ts to:
    - Export ConventionStore from ./storage
    - Update learnConventions() to:
      1. Find test directories in project (src/__tests__, tests/, **/*.test.ts)
      2. Extract conventions using analyzer
      3. Save to ConventionStore in .taro/conventions.db
      4. Return learned conventions
    
    Add getConventions(projectRoot: string) function that:
      1. Loads conventions from .taro/conventions.db
      2. Returns TestConvention if exists, null otherwise
    
    Ensure .taro directory is created if it doesn't exist
  </action>
  <verify>learnConventions saves to .taro/, getConventions loads from .taro/</verify>
  <done>Learner orchestrator integrates storage for persistence</done>
</task>

</tasks>

<verification>
- ConventionStore saves/loads complete TestConvention
- Cache with TTL works correctly
- .taro/conventions.db is created in project directory
- getConventions returns null if no stored conventions
</verification>

<success_criteria>
- [ ] ConventionStore uses better-sqlite3 for synchronous operations
- [ ] Conventions persist across separate process invocations
- [ ] Cache supports TTL for time-based invalidation
- [ ] .taro directory created automatically
</success_criteria>

<output>
After completion, create `.planning/phases/04-self-scoring-convention-learning/04-04-SUMMARY.md`
</output>
