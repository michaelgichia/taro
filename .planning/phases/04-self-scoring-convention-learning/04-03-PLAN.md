---
phase: 04-self-scoring-convention-learning
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/learner/index.ts
  - src/learner/types.ts
  - src/learner/analyzer.ts
  - package.json
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Taro derives conventions from observation - existing test patterns are analyzed"
    - "Convention analysis extracts naming patterns, query preferences, and matcher styles"
    - "AST-based pattern detection identifies test structure conventions"
  artifacts:
    - path: "src/learner/index.ts"
      provides: "Learning orchestrator export"
      exports: ["learnConventions", "ConventionStore"]
    - path: "src/learner/types.ts"
      provides: "Convention type definitions"
      exports: ["TestConvention", "ConventionKey"]
    - path: "src/learner/analyzer.ts"
      provides: "AST-based convention analysis"
      exports: ["analyzeTestFile", "extractConventions"]
  key_links:
    - from: "src/learner/index.ts"
      to: "src/learner/analyzer.ts"
      via: "import analyzeTestFile for pattern extraction"
    - from: "src/learner/analyzer.ts"
      to: "@typescript-eslint/typescript-estree"
      via: "AST parsing for code analysis"
    - from: "src/learner/analyzer.ts"
      to: "src/scorer/quality-gates.ts"
      via: "reuse AST traversal logic"
---

<objective>
Create convention learning module that analyzes existing test patterns

Purpose: Implements CNV-01 (Taro derives conventions from observation)
Output: Analyzer that extracts naming, query, and matcher conventions from test files
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/scorer/quality-gates.ts
@.planning/phases/04-self-scoring-convention-learning/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up learner module structure</name>
  <files>src/learner/index.ts, src/learner/types.ts</files>
  <action>
    Create src/learner/ directory with:
    - src/learner/types.ts: Define TestConvention interface with:
      - naming: { pattern: 'camelCase' | 'kebab-case' | 'snake_case', describePrefix, itTemplate }
      - structure: { describePerComponent, helpersInDescribe, setupLocation }
      - queries: { preferred: string[], avoided: string[] }
      - matchers: { common: string[] }
      - imports: { common: string[] }
    - src/learner/index.ts: Create learnConventions(testDir: string) function that returns TestConvention
    
    Export TestConvention and learnConventions
  </action>
  <verify>TypeScript compiles with new learner types, exports are available</verify>
  <done>Learner module structure exists with type definitions</done>
</task>

<task type="auto">
  <name>Task 2: Implement convention analyzer</name>
  <files>src/learner/analyzer.ts</files>
  <action>
    Create src/learner/analyzer.ts with:
    - analyzeTestFile(filePath: string): Partial<TestConvention> function
    - extractConventions(testDir: string): TestConvention function
    
    Analyze and extract:
    - Naming: detect describe block name patterns (camelCase, kebab-case, snake_case)
    - Queries: count usage of getByRole, getByLabelText, getByText, querySelector, getByTestId
    - Matchers: detect common matchers (toBeInTheDocument, toHaveValue, toHaveBeenCalledWith, etc.)
    - Imports: extract common import statements
    
    Use @typescript-eslint/typescript-estree for AST parsing
    Scan test files matching: **/*.{test,spec}.{ts,tsx}
  </action>
  <verify>Run analyzer on sample test file, returns structure with naming, queries, matchers, imports</verify>
  <done>Convention analyzer extracts patterns from existing test files</done>
</task>

</tasks>

<verification>
- Learner types define complete convention structure
- Analyzer correctly identifies naming patterns from describe blocks
- Query and matcher preferences are extracted from AST analysis
</verification>

<success_criteria>
- [ ] TestConvention type covers naming, structure, queries, matchers, imports
- [ ] analyzeTestFile returns partial convention for single file
- [ ] extractConventions combines multiple files into unified convention
- [ ] Naming pattern detection works for camelCase, kebab-case, snake_case
</success_criteria>

<output>
After completion, create `.planning/phases/04-self-scoring-convention-learning/04-03-SUMMARY.md`
</output>
