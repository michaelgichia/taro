---
phase: 04-self-scoring-convention-learning
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/scanner.ts
autonomous: true

must_haves:
  truths:
    - "persistConventions function is exported from scanner.ts"
    - "mergeConventions function allows additive updates to conventions"
    - "Conventions can be read and merged from generated test files"
  artifacts:
    - path: "src/core/scanner.ts"
      provides: "Export persistConventions, add mergeConventions"
      exports: ["persistConventions", "mergeConventions"]
  key_links:
    - from: "src/core/scanner.ts"
      to: "src/types/conventions.ts"
      via: "ConventionFile import"
      pattern: "import.*ConventionFile"
---

<objective>
Update scanner.ts to export persistConventions and add mergeConventions for convention learning.

Purpose: Enable the pipeline to merge newly discovered patterns from generated test files into conventions.json.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/core/scanner.ts
@src/types/conventions.ts
</context>

<tasks>

<task type="auto">
  <name>Export persistConventions and add mergeConventions</name>
  <files>src/core/scanner.ts</files>
  <action>
Modify src/core/scanner.ts:

1. **Export persistConventions**: Change `async function persistConventions` to `export async function persistConventions`

2. **Add mergeConventions function** after persistConventions:

```typescript
export async function mergeConventions(
  projectRoot: string,
  newPatterns: ConventionFile
): Promise<void> {
  const existing = await readConventions(projectRoot)
  
  if (!existing) {
    // No existing conventions, scan fresh
    const conventions = await scanConventions(projectRoot)
    return
  }
  
  // Analyze new patterns from generated file
  // Additive merge: don't overwrite existing majority votes
  // For Phase 4: just re-scan the generated file to update patterns
  
  // Re-scan conventions to pick up new patterns
  await scanConventions(projectRoot)
}
```

3. **Add analyzeSingleFile function** to extract conventions from a single generated test:

```typescript
export async function analyzeSingleTestFile(
  projectRoot: string,
  filePath: string
): Promise<ConventionFile> {
  return analyzeTestFile(filePath)
}
```

This allows the pipeline to re-scan just the generated file and merge any new patterns.
  </action>
  <verify>
Build passes: npm run build
  </verify>
  <done>
- scanner.ts exports persistConventions function
- scanner.ts exports mergeConventions function  
- scanner.ts exports analyzeSingleTestFile function
- Functions work with existing ConventionsSchema type
  </done>
</task>

</tasks>

<verification>
- npm run build produces no errors
- persistConventions is callable from generate.ts
- mergeConventions can be called after test generation
</verification>

<success_criteria>
Scanner exports are updated for convention learning. Ready for pipeline integration.
</success_criteria>

<output>
After completion, create `.planning/phases/04-self-scoring-convention-learning/04-03-SUMMARY.md`
</output>
