---
phase: 04-self-scoring-convention-learning
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scorer/pre-audit.ts
  - src/scorer/post-verify.ts
  - src/scorer/index.ts
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Pre-write audit validates test structure before file creation"
    - "Post-write verification confirms syntax validity and import correctness"
    - "Blocking issues prevent file creation, warnings are logged but don't block"
  artifacts:
    - path: "src/scorer/pre-audit.ts"
      provides: "Pre-write structural validation"
      exports: ["preWriteAudit", "AuditResult"]
    - path: "src/scorer/post-verify.ts"
      provides: "Post-write syntax and import verification"
      exports: ["postWriteVerification", "VerificationResult"]
  key_links:
    - from: "src/scorer/index.ts"
      to: "src/scorer/pre-audit.ts"
      via: "import preWriteAudit for pre-generation validation"
    - from: "src/scorer/index.ts"
      to: "src/scorer/post-verify.ts"
      via: "import postWriteVerification for post-file validation"
    - from: "src/scorer/pre-audit.ts"
      to: "src/scorer/quality-gates.ts"
      via: "reuse quality evaluation logic"
---

<objective>
Implement pre-write audit and post-write verification for test files

Purpose: Implements SCR-02 (pre-write audit) and SCR-03 (post-write verification)
Output: Audit and verification functions that integrate with generation pipeline
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/scorer/quality-gates.ts
@.planning/phases/04-self-scoring-convention-learning/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement pre-write audit</name>
  <files>src/scorer/pre-audit.ts</files>
  <action>
    Create src/scorer/pre-audit.ts with:
    - preWriteAudit(code: string): AuditResult interface
    - Blocking checks: has describe block, has at least one test case, has expect statements
    - Warning checks: uses fragile queries, missing common matchers
    - Use @typescript-eslint/typescript-estree for AST parsing
    - Return { valid: boolean, blocking: string[], warnings: string[] }
    
    Structure:
    - Import parse from @typescript-eslint/typescript-estree
    - Import evaluateQualityGates from ./quality-gates
    - Run quality gates first
    - Add additional structural checks (has imports, has render/setup)
  </action>
  <verify>Test: preWriteAudit('describe("test", () => { it("works", () => { expect(true).toBe(true); }); });') returns valid: true</verify>
  <done>Pre-write audit validates test structure and returns blocking/warning issues</done>
</task>

<task type="auto">
  <name>Task 2: Implement post-write verification</name>
  <files>src/scorer/post-verify.ts</files>
  <action>
    Create src/scorer/post-verify.ts with:
    - postWriteVerification(filePath: string): VerificationResult interface
    - Checks: file exists, valid TypeScript syntax, required imports present
    - Required imports: @testing-library/react, describe, it (or test), expect
    - Warnings: screen.debug() present, skipped tests (test.skip/it.skip)
    - Use parse() to validate syntax
    - Return { valid: boolean, errors: string[], warnings: string[] }
    
    Error cases to handle:
    - File doesn't exist
    - Syntax parse error
    - Missing required imports
  </action>
  <verify>Create temp test file, run postWriteVerification on it, returns valid: true with correct structure</verify>
  <done>Post-write verification confirms file validity and import correctness</done>
</task>

<task type="auto">
  <name>Task 3: Integrate audit/verify into scorer orchestrator</name>
  <files>src/scorer/index.ts</files>
  <action>
    Update src/scorer/index.ts to export:
    - preWriteAudit from ./pre-audit
    - postWriteVerification from ./post-verify
    - Add orchestrateWithScoring() function that:
      1. Takes recording and output path
      2. Generates test code
      3. Runs preWriteAudit
      4. If blocking issues, returns failure without writing
      5. If passes, writes file
      6. Runs postWriteVerification
      7. Returns complete result with score and any warnings
  </action>
  <verify>orchestrateWithScoring() function exists and is callable</verify>
  <done>Scorer orchestrator integrates pre/post validation with generation</done>
</task>

</tasks>

<verification>
- Pre-write audit blocks generation for invalid structure
- Post-write verification catches syntax errors and missing imports
- Orchestrator combines generation + scoring + validation flow
</verification>

<success_criteria>
- [ ] preWriteAudit returns blocking issues that prevent file creation
- [ ] postWriteVerification checks syntax and imports
- [ ] orchestrateWithScoring combines all three phases
- [ ] Warnings don't block but are reported
</success_criteria>

<output>
After completion, create `.planning/phases/04-self-scoring-convention-learning/04-02-SUMMARY.md`
</output>
