---
phase: 04-self-scoring-convention-learning
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/orchestrator.ts
  - src/scorer/index.ts
  - src/learner/index.ts
autonomous: true
user_setup: []
gap_closure: true

must_haves:
  truths:
    - "Output is scored before writing - orchestrateWithScoring called during generation"
    - "Pre-write audit passes - preWriteAudit called before file write"
    - "Post-write verification runs - postWriteVerification called after file write"
  artifacts:
    - path: "src/core/orchestrator.ts"
      provides: "Integration of scorer and learner into generation pipeline"
      must_import: ["scorer", "learner"]
  key_links:
    - from: "src/core/orchestrator.ts"
      to: "src/scorer/index.ts"
      via: "import orchestrateWithScoring"
    - from: "src/core/orchestrator.ts"
      to: "src/learner/index.ts"
      via: "import learnConventions, getConventions"
---

<objective>
Integrate scorer and learner modules into the core orchestrator

Purpose: Close gaps from VERIFICATION.md - wire scorer/preWriteAudit/postWriteVerification and learner into orchestrator.ts
Output: Working integration with scoring/audit/verification in generation flow
</objective>

<execution_context>
@~/.opencode/taro/workflows/execute-plan.md
@~/.opencode/taro/templates/summary.md
</execution_context>

<context>
@src/core/orchestrator.ts
@src/scorer/index.ts
@src/learner/index.ts
@.planning/phases/04-self-scoring-convention-learning/04-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Import scorer module in orchestrator</name>
  <files>src/core/orchestrator.ts</files>
  <action>
    Add import for scorer functions in src/core/orchestrator.ts:
    - Import { orchestrateWithScoring, preWriteAudit, postWriteVerification } from '../scorer/index.js'

    This replaces the placeholder at line 102-103 with actual integration.
  </action>
  <verify>TypeScript compiles with scorer imports</verify>
  <done>Scorer module imported in orchestrator</done>
</task>

<task type="auto">
  <name>Task 2: Import learner module in orchestrator</name>
  <files>src/core/orchestrator.ts</files>
  <action>
    Add import for learner functions in src/core/orchestrator.ts:
    - Import { learnConventions, getConventions } from '../learner/index.js'
  </action>
  <verify>TypeScript compiles with learner imports</verify>
  <done>Learner module imported in orchestrator</done>
</task>

<task type="auto">
  <name>Task 3: Integrate scoring into generation flow</name>
  <files>src/core/orchestrator.ts</files>
  <action>
    Replace the placeholder (lines 101-103) with actual generation + scoring flow:

    1. First call learnConventions() to get existing conventions
    2. Generate test code (currently placeholder - just log for now)
    3. Call preWriteAudit on generated code
    4. If audit passes (no blocking), write file
    5. Call postWriteVerification on written file
    6. Log results to user

    The generation itself can remain a placeholder but the scoring/audit/verify flow should be wired.
  </action>
  <verify>Run: npx ts-node src/core/orchestrator.ts generate test.json --output ./tests (with test file)</verify>
  <done>Scoring, preWriteAudit, postWriteVerification called in generation flow</done>
</task>

</tasks>

<verification>
- orchestrator.ts imports scorer module
- orchestrator.ts imports learner module
- Generation flow calls preWriteAudit before writing
- Generation flow calls postWriteVerification after writing
- Results logged to user
</verification>

<success_criteria>
- [ ] orchestrateWithScoring or equivalent is called during generation
- [ ] preWriteAudit is called before file write
- [ ] postWriteVerification is called after file write
- [ ] Blocking audit issues prevent file from being written
- [ ] Verification results are logged to user
</success_criteria>

<output>
After completion, create `.planning/phases/04-self-scoring-convention-learning/04-05-SUMMARY.md`
</output>
