---
phase: 02-sequential-regrade-loop-and-history-persistence
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/cli/commands/target-directory-tracker.ts
  - src/cli/commands/regrade.ts
  - src/cli/commands/tests/target-directory-tracker.test.ts
  - src/cli/commands/tests/regrade.test.ts
autonomous: true
requirements:
  - RGTRK-04
  - RGEX-01
user_setup: []
must_haves:
  truths:
    - A successful `regrade --directory-loop` run processes the queued tests sequentially until no pending entries remain.
    - Completed tracker rows record the new score threshold plus follow-up comments derived from the regrade result.
    - Manual-review results stay explicit in tracker output and command logs without deferring completion-state persistence to a later phase.
  artifacts:
    - `src/cli/commands/regrade.ts` contains the success-path sequential directory loop that calls the reusable runner from `02-01`.
    - `src/cli/commands/target-directory-tracker.ts` can persist completed-entry score/comment metadata for regrade rows.
    - `src/cli/commands/tests/regrade.test.ts` covers end-to-end multi-file success-path regrade execution.
  key_links:
    - The directory loop marks one entry `in-progress`, invokes the runner once per test, persists the completed-entry metadata, and then continues to the next pending test.
    - Completed tracker rows reflect the same score result the runner appended into `generatedTests` history.
---

<objective>
Turn the Phase 1 bootstrap command into a real sequential batch regrade loop that writes completed tracker results using the reusable runner from Plan `02-01`.

Purpose: Phase 2 must prove that directory-loop regrade can finish the full queued workset and persist both state history and tracker-visible outcomes.
Output: Sequential success-path regrade execution plus tracker completion metadata and command tests that prove all queued tests are processed.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-sequential-regrade-loop-and-history-persistence/02-RESEARCH.md
@.planning/phases/02-sequential-regrade-loop-and-history-persistence/02-VALIDATION.md
@.planning/phases/02-sequential-regrade-loop-and-history-persistence/02-01-SUMMARY.md
@src/cli/commands/regrade.ts
@src/cli/commands/regrade-runner.ts
@src/cli/commands/target-directory-tracker.ts
@src/cli/commands/target.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend tracker rows for completed regrade results</name>
  <files>src/cli/commands/target-directory-tracker.ts, src/cli/commands/tests/target-directory-tracker.test.ts</files>
  <read_first>src/cli/commands/target-directory-tracker.ts, src/cli/commands/tests/target-directory-tracker.test.ts, .planning/phases/02-sequential-regrade-loop-and-history-persistence/02-RESEARCH.md</read_first>
  <action>Extend the canonical tracker schema so completed regrade rows can persist the updated score threshold and follow-up comments in addition to the current stored score threshold added in Phase 1. Preserve repo-relative rendering, atomic writes, Markdown round-tripping, and the existing target-loop compatibility guarantees. Add any narrow helper needed to update a completed row with structured metadata rather than overloading the existing status-only update API.</action>
  <verify>
    <automated>pnpm exec vitest run src/cli/commands/tests/target-directory-tracker.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/cli/commands/target-directory-tracker.ts` can represent an updated score threshold for completed regrade rows.
    - `src/cli/commands/target-directory-tracker.ts` can render and parse follow-up comments for completed regrade rows.
    - Existing target-loop rows still round-trip without requiring the new completion metadata.
  </acceptance_criteria>
  <done>The tracker format can persist the user-visible outcome of each completed regrade.</done>
</task>

<task type="auto">
  <name>Task 2: Wire sequential success-path regrade execution into the directory loop</name>
  <files>src/cli/commands/regrade.ts</files>
  <read_first>src/cli/commands/regrade.ts, src/cli/commands/target.ts, src/cli/commands/regrade-runner.ts, src/cli/commands/target-directory-tracker.ts</read_first>
  <action>Replace the current bootstrap-only behavior in `src/cli/commands/regrade.ts` with a sequential success-path loop that mirrors the structure of `target --directory-loop`: read or rebuild the tracker, choose the active pending row, mark it `in-progress`, invoke the reusable runner once for that test file, persist the completed row with the new score threshold and follow-up comments, and continue until no pending entries remain. Keep Phase 2 focused on successful processing of the full queued workset; leave hardened interruption and resume policy to Phase 3.</action>
  <verify>
    <automated>pnpm exec vitest run src/cli/commands/tests/regrade.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/cli/commands/regrade.ts` iterates through queued test files sequentially rather than stopping after tracker bootstrap.
    - `src/cli/commands/regrade.ts` uses the runner from Plan `02-01` instead of duplicating scoring/state persistence logic inline.
    - Successful runs leave no pending rows when all tests complete.
  </acceptance_criteria>
  <done>The directory command can successfully regrade the full workset end-to-end on the happy path.</done>
</task>

<task type="auto">
  <name>Task 3: Add end-to-end tests for completed rows, score deltas, and explicit follow-up comments</name>
  <files>src/cli/commands/tests/regrade.test.ts, src/cli/commands/tests/target-directory-tracker.test.ts</files>
  <read_first>src/cli/commands/tests/regrade.test.ts, src/cli/commands/tests/target-directory-tracker.test.ts, src/cli/commands/regrade.ts</read_first>
  <action>Expand the command-level tests so they prove: a successful mixed directory loop regrades all discovered tests in order, the tracker ends with completed rows containing both the previous/current and updated score information, and a result that still requires manual review records explicit follow-up comments in the tracker output instead of hiding that state. Keep failure-stop and resume-hardening assertions out of this plan; those belong to Phase 3.</action>
  <verify>
    <automated>pnpm exec vitest run src/cli/commands/tests/regrade.test.ts src/cli/commands/tests/target-directory-tracker.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/cli/commands/tests/regrade.test.ts` proves a successful directory loop completes every queued test.
    - `src/cli/commands/tests/regrade.test.ts` contains an assertion that completed tracker rows show the updated score threshold.
    - `src/cli/commands/tests/regrade.test.ts` or `target-directory-tracker.test.ts` contains an assertion that follow-up comments are rendered for a manual-review result.
  </acceptance_criteria>
  <done>The sequential directory loop is protected by end-to-end tests for the exact tracker output Phase 2 promises.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm exec vitest run src/cli/commands/tests/regrade.test.ts` exits 0
- [ ] `pnpm exec vitest run src/cli/commands/tests/target-directory-tracker.test.ts` exits 0
- [ ] A command-level test proves all queued regrade rows finish as `completed` on the success path
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The regrade directory loop completes the queued workset sequentially and writes completed tracker rows with explicit result metadata
</success_criteria>

<output>
After completion, create `.planning/phases/02-sequential-regrade-loop-and-history-persistence/02-02-SUMMARY.md`
</output>
