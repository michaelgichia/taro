---
phase: 19-marker-coverage-audit-reporting
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/score.ts
  - src/core/scorer.ts
  - src/cli/commands/generate.ts
  - src/core/scorer.test.ts
  - src/cli/commands/generate.test.ts
autonomous: true
requirements:
  - QUAL-01

must_haves:
  truths:
    - "Every JS generation run derives marker coverage from one canonical run-level summary: detected semantic markers, emitted marker-derived assertions, and unresolved markers"
    - "Coverage totals are computed before output rendering and are reused by both CLI reporting and score/gate evaluation without recomputing with divergent rules"
    - "Coverage output remains run-level totals only for Phase 19; per-scenario exports and machine-readable coverage reports remain out of scope"
  artifacts:
    - path: "src/types/score.ts"
      provides: "Score contract fields for marker coverage and marker quality-gate state"
      contains: "ScoreResult"
    - path: "src/core/scorer.ts"
      provides: "Deterministic scoring integration point that accepts marker coverage context"
      contains: "scoreGeneratedTest"
    - path: "src/cli/commands/generate.ts"
      provides: "Runtime marker-coverage aggregation across analyzed recording and planned scenario marker state"
      contains: "createGenerateCommand"
    - path: "src/core/scorer.test.ts"
      provides: "Regression coverage for marker coverage signals flowing into score results"
      contains: "scoreGeneratedTest"
    - path: "src/cli/commands/generate.test.ts"
      provides: "Public-flow coverage proving marker coverage totals are surfaced from real generate paths"
      contains: "createGenerateCommand"
  key_links:
    - from: "src/cli/commands/generate.ts"
      to: "src/core/suite-planner.ts"
      via: "planned markerAssertions and unresolvedMarkerAssertions become emitted/unresolved coverage totals"
      pattern: "markerAssertions"
    - from: "src/cli/commands/generate.ts"
      to: "src/core/scorer.ts"
      via: "single marker-coverage payload passed into score evaluation"
      pattern: "scoreGeneratedTest"
    - from: "src/core/scorer.test.ts"
      to: "src/core/scorer.ts"
      via: "score-level regression lock for marker coverage signals"
      pattern: "scoreGeneratedTest"
---

<objective>
Establish a canonical marker-coverage accounting contract for Phase 19.

Purpose: make marker conversion coverage measurable and reusable so every downstream gate/report decision is based on stable totals instead of ad-hoc counting.
Output: marker-coverage fields in score contracts, scorer support for marker-coverage context, and generate-path aggregation wired to those contracts.
</objective>

<execution_context>
@/Users/michaelgichia/.codex/taro/workflows/execute-plan.md
@/Users/michaelgichia/.codex/taro/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-marker-coverage-audit-reporting/19-CONTEXT.md
@src/types/score.ts
@src/core/scorer.ts
@src/cli/commands/generate.ts
@src/core/suite-planner.ts
@src/types/recording.ts
@src/core/scorer.test.ts
@src/cli/commands/generate.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add marker coverage and marker quality-gate fields to scoring contracts</name>
  <files>src/types/score.ts, src/core/scorer.ts, src/core/scorer.test.ts</files>
  <action>
Extend scoring contracts so marker coverage is first-class and deterministic.

Required outcomes:
1. Add explicit score-contract fields for marker coverage totals (`detected`, `emitted`, `unresolved`) and marker gate state metadata that downstream CLI output can read without parsing free-form reasons.
2. Update `scoreGeneratedTest` input/output contracts so callers can provide marker coverage context and receive normalized marker gate state back.
3. Keep backward compatibility for non-JS and non-marker runs by defaulting marker coverage to zeroed values and a non-failing gate state.
4. Add focused scorer tests for marker-context present vs absent flows, proving deterministic totals and gate metadata are returned.
5. Keep QUAL-02 enforcement logic minimal in this plan: wire the data path and stable signal shape, but reserve final exit semantics and banner wording for follow-up plans.
  </action>
  <verify>
    <automated>npm run build && npm run test:run -- src/core/scorer.test.ts</automated>
  </verify>
  <done>
    Score contracts expose normalized marker coverage and marker-gate state that can be consumed directly by CLI reporting and quality-gate enforcement.
  </done>
</task>

<task type="auto">
  <name>Task 2: Compute run-level marker coverage once in generate and pass it through scoring</name>
  <files>src/cli/commands/generate.ts, src/cli/commands/generate.test.ts</files>
  <action>
Implement canonical coverage aggregation in the public generate path.

Required outcomes:
1. Add a single helper in `generate.ts` that computes run-level marker coverage using analyzed recording diagnostics plus hydrated suite-plan marker state (for emitted/unresolved parity with generation input).
2. Define detected markers as all semantic markers recognized in the run (resolved + unresolved), with unresolved count aligned to unresolved marker assertion metadata when available.
3. Feed the canonical marker coverage payload into `scoreGeneratedTest` rather than allowing multiple ad-hoc counting paths.
4. Preserve existing cleanup summary output while preparing structured coverage data for the dedicated Phase 19 reporting section.
5. Add or update generate-command tests to assert marker coverage totals are present and stable for representative marker flows.
6. Keep this plan scoped to data plumbing and parity; dedicated PASS/FAIL banner formatting and exit behavior are handled by later plans.
  </action>
  <verify>
    <automated>npm run build && npm run test:run -- src/core/scorer.test.ts src/cli/commands/generate.test.ts</automated>
  </verify>
  <done>
    The public generate flow now produces one canonical marker-coverage summary that score and reporting layers can trust.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds.
2. `npm run test:run -- src/core/scorer.test.ts src/cli/commands/generate.test.ts` succeeds.
3. Score results include normalized marker coverage and marker gate state for JS marker runs.
4. Generate computes marker coverage once and uses the same payload for scoring/report layers.
5. Non-marker runs retain stable behavior with zeroed marker coverage and no false failures.
</verification>

<success_criteria>
- QUAL-01 counting has an explicit shared contract
- Marker coverage totals are deterministic and reusable
- Score and CLI layers consume the same marker-coverage payload
- Coverage remains run-level totals only, matching locked Phase 19 scope
</success_criteria>

<output>
After completion, create `.planning/phases/19-marker-coverage-audit-reporting/19-01-SUMMARY.md` documenting marker coverage contract additions and generate/scorer data-flow wiring.
</output>
