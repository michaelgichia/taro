---
phase: 19-marker-coverage-audit-reporting
plan: "03"
type: execute
wave: 3
depends_on:
  - 19-01
  - 19-02
files_modified:
  - src/cli/commands/generate.ts
  - src/cli/commands/generate.test.ts
  - src/core/suite-planner.test.ts
  - src/core/generator.test.ts
autonomous: true
requirements:
  - QUAL-01
  - QUAL-03

must_haves:
  truths:
    - "Every unresolved marker emits one warning line with traceable fields: marker step id, recorder line context (or line: unknown), unresolved reason code, and human-readable guidance"
    - "Unresolved marker warnings are derived from planner unresolved marker state and remain visible even when marker coverage gate passes for other markers"
    - "Warning output preserves truthful guardrails: unresolved markers remain explicit warnings and never silently convert into fabricated assertions"
  artifacts:
    - path: "src/cli/commands/generate.ts"
      provides: "Stable unresolved-marker warning formatter and emission flow with line fallback behavior"
      contains: "createGenerateCommand"
    - path: "src/cli/commands/generate.test.ts"
      provides: "Public-flow regressions for per-marker warning lines and line fallback coverage"
      contains: "createGenerateCommand"
    - path: "src/core/suite-planner.test.ts"
      provides: "Planner-level guardrails proving unresolved marker metadata carries marker id/reason/line context needed by CLI reporting"
      contains: "planJsSuite"
    - path: "src/core/generator.test.ts"
      provides: "Generation-path regression ensuring unresolved markers stay non-emitted while warning traceability remains externalized"
      contains: "generateTestFromGroups"
  key_links:
    - from: "src/cli/commands/generate.ts"
      to: "src/core/suite-planner.ts"
      via: "CLI warning lines sourced from scenario unresolvedMarkerAssertions metadata"
      pattern: "unresolvedMarkerAssertions"
    - from: "src/cli/commands/generate.test.ts"
      to: "src/cli/commands/generate.ts"
      via: "regression lock for warning line shape, reason code inclusion, and line fallback"
      pattern: "createGenerateCommand"
    - from: "src/core/suite-planner.test.ts"
      to: "src/cli/commands/generate.test.ts"
      via: "end-to-end trust that unresolved marker metadata exported by planner can be rendered in CLI reporting"
      pattern: "unresolvedMarkerAssertions"
---

<objective>
Ship QUAL-03 unresolved-marker reporting with recorder line traceability.

Purpose: make unresolved marker repair actionable by emitting one clear warning per unresolved marker, including stable reason code and recorder-line context for source correction.
Output: deterministic unresolved-marker warning formatting in CLI and regression coverage for line-known and line-unknown reporting behavior.
</objective>

<execution_context>
@/Users/michaelgichia/.codex/taro/workflows/execute-plan.md
@/Users/michaelgichia/.codex/taro/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-marker-coverage-audit-reporting/19-CONTEXT.md
@.planning/phases/19-marker-coverage-audit-reporting/19-01-PLAN.md
@.planning/phases/19-marker-coverage-audit-reporting/19-02-PLAN.md
@src/cli/commands/generate.ts
@src/cli/commands/generate.test.ts
@src/core/suite-planner.ts
@src/core/suite-planner.test.ts
@src/core/generator.test.ts
@src/types/recording.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add structured unresolved-marker warning emission with recorder-line fallback</name>
  <files>src/cli/commands/generate.ts</files>
  <action>
Emit repair-oriented unresolved marker warnings directly in the public generate output.

Required outcomes:
1. Add a warning formatter that outputs one line per unresolved marker using stable fields: marker step id, reason code, human-readable reason text, target/proof hint, and recorder line context.
2. Use `line: unknown` when recorder line metadata is absent while still emitting marker step id and reason code for traceability.
3. Source unresolved marker warnings from hydrated scenario unresolved marker metadata so output matches actual planning/generation state.
4. Keep warning emission independent from QUAL-02 gate status: unresolved warnings should appear whenever unresolved markers exist, including mixed resolved/unresolved runs.
5. Keep warning format terminal- and CI-friendly (single-line entries, deterministic field order, no multiline blobs).
  </action>
  <verify>
    <automated>npm run build && npm run test:run -- src/cli/commands/generate.test.ts</automated>
  </verify>
  <done>
    The CLI now emits deterministic, per-marker unresolved warnings with line traceability and a stable `line: unknown` fallback.
  </done>
</task>

<task type="auto">
  <name>Task 2: Lock unresolved warning traceability with planner/generator-aware regressions</name>
  <files>src/cli/commands/generate.test.ts, src/core/suite-planner.test.ts, src/core/generator.test.ts</files>
  <action>
Add regression coverage that keeps QUAL-03 reporting truthful and actionable.

Required outcomes:
1. Add generate-command tests asserting one warning per unresolved marker and presence of marker step id, reason code, and recorder line field.
2. Add explicit test coverage for missing line metadata to prove `line: unknown` fallback behavior.
3. Add/adjust suite-planner and generator tests to confirm unresolved marker metadata remains available for CLI reporting while unresolved markers still do not emit fabricated assertions.
4. Keep coverage focused on unresolved-marker reporting and traceability, not on reopening Phase 18 conversion policy or adding new marker gestures.
5. Ensure mixed runs (some emitted assertions, some unresolved markers) still show unresolved warning lines and accurate run-level totals.
  </action>
  <verify>
    <automated>npm run build && npm run test:run -- src/core/suite-planner.test.ts src/core/generator.test.ts src/cli/commands/generate.test.ts</automated>
  </verify>
  <done>
    QUAL-03 reporting is regression-locked across planner metadata, generator behavior, and CLI output.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds.
2. `npm run test:run -- src/core/suite-planner.test.ts src/core/generator.test.ts src/cli/commands/generate.test.ts` succeeds.
3. Unresolved markers emit one warning line each with marker step id, reason code, human-readable reason, and recorder line context.
4. Runs without recorder line metadata emit `line: unknown` without dropping warning traceability.
5. Mixed resolved/unresolved runs keep unresolved warnings visible while maintaining truthful non-emission of unresolved assertions.
</verification>

<success_criteria>
- QUAL-03 warnings are actionable and traceable to source recording steps
- Warning format is stable for both local terminal and CI logs
- Missing line metadata degrades gracefully with `line: unknown`
- Unresolved markers remain explicit warnings, never fabricated assertions
</success_criteria>

<output>
After completion, create `.planning/phases/19-marker-coverage-audit-reporting/19-03-SUMMARY.md` documenting unresolved warning format, line fallback behavior, and regression coverage.
</output>
