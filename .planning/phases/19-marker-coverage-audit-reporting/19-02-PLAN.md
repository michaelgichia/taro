---
phase: 19-marker-coverage-audit-reporting
plan: "02"
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - src/types/score.ts
  - src/core/scorer.ts
  - src/cli/commands/generate.ts
  - src/core/scorer.test.ts
  - src/cli/commands/generate.test.ts
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02

must_haves:
  truths:
    - "When semantic markers are detected but emitted marker-derived assertions are zero, marker quality gate status is explicit FAIL (QUAL-02), never implicit or advisory-only"
    - "Marker gate status is printed in a dedicated marker-coverage section with deterministic detected/emitted/unresolved counts and explicit PASS/FAIL wording"
    - "Write mode preserves generated output for repair workflows while still failing the command with exit code 1; dry-run mode also fails explicitly under the same zero-conversion condition"
  artifacts:
    - path: "src/core/scorer.ts"
      provides: "Marker gate rule evaluation for zero-conversion failure"
      contains: "scoreGeneratedTest"
    - path: "src/types/score.ts"
      provides: "Typed marker gate status and reason fields consumed by CLI formatting"
      contains: "ScoreResult"
    - path: "src/cli/commands/generate.ts"
      provides: "Dedicated marker-coverage CLI section and QUAL-02 exit enforcement in dry-run/write flows"
      contains: "createGenerateCommand"
    - path: "src/core/scorer.test.ts"
      provides: "Unit coverage proving marker gate failure triggers under zero-conversion conditions"
      contains: "scoreGeneratedTest"
    - path: "src/cli/commands/generate.test.ts"
      provides: "Public-flow regression coverage for PASS/FAIL marker gate output and write/dry-run exit semantics"
      contains: "createGenerateCommand"
  key_links:
    - from: "src/core/scorer.ts"
      to: "src/types/score.ts"
      via: "normalized marker gate reason and pass/fail state for QUAL-02"
      pattern: "ScoreResult"
    - from: "src/cli/commands/generate.ts"
      to: "src/core/scorer.ts"
      via: "gate decision drives dedicated marker-coverage section and exit semantics"
      pattern: "scoreGeneratedTest"
    - from: "src/cli/commands/generate.test.ts"
      to: "src/cli/commands/generate.ts"
      via: "CLI regression lock for zero-conversion fail behavior in dry-run and write mode"
      pattern: "createGenerateCommand"
---

<objective>
Enforce QUAL-02 with explicit marker gate semantics in the public CLI flow.

Purpose: convert marker coverage from informative counts into an enforceable quality gate that fails zero-conversion runs while preserving repair-friendly output behavior.
Output: scorer-level gate evaluation, dedicated marker-coverage PASS/FAIL reporting, and deterministic dry-run/write exit behavior for zero-conversion failures.
</objective>

<execution_context>
@/Users/michaelgichia/.codex/get-shit-done/workflows/execute-plan.md
@/Users/michaelgichia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-marker-coverage-audit-reporting/19-CONTEXT.md
@.planning/phases/19-marker-coverage-audit-reporting/19-01-PLAN.md
@src/types/score.ts
@src/core/scorer.ts
@src/cli/commands/generate.ts
@src/core/scorer.test.ts
@src/cli/commands/generate.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement explicit QUAL-02 zero-conversion gate evaluation in scoring output</name>
  <files>src/types/score.ts, src/core/scorer.ts, src/core/scorer.test.ts</files>
  <action>
Convert marker gate state from passive metadata into explicit pass/fail evaluation logic.

Required outcomes:
1. Encode a clear QUAL-02 failure condition in scorer logic: marker gate fails only when detected markers are greater than zero and emitted marker-derived assertions are zero.
2. Ensure the scorer exposes both a stable machine-readable reason code and a human-readable reason message for marker gate failures.
3. Integrate the marker gate failure into score reasons/blockers in a way that remains additive to existing boundary/query/assertion diagnostics.
4. Keep non-marker and successful-conversion runs explicitly marked as gate PASS, not omitted/ambiguous.
5. Add scorer regressions that cover gate PASS and FAIL boundaries, including zero-detected and non-zero-emitted cases.
  </action>
  <verify>
    <automated>npm run build && npm run test:run -- src/core/scorer.test.ts</automated>
  </verify>
  <done>
    Scoring now returns explicit QUAL-02 PASS/FAIL marker gate state with stable reason semantics.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add dedicated marker coverage PASS/FAIL section and enforce fail-fast exit semantics</name>
  <files>src/cli/commands/generate.ts, src/cli/commands/generate.test.ts</files>
  <action>
Surface marker gate outcomes clearly to users and CI.

Required outcomes:
1. Emit a dedicated marker-coverage section near scoring output that always prints detected/emitted/unresolved totals and explicit gate status (PASS or FAIL).
2. Include a QUAL-02-labeled failure message when gate status is FAIL, instead of relying only on generic score hints.
3. In `--dry-run`, set explicit failure outcome (exit code 1) under QUAL-02 fail conditions after preview output is printed.
4. In write mode, keep writing/verifying the generated file first, then fail explicitly with exit code 1 under QUAL-02 fail conditions.
5. Preserve current successful-run UX when gate status is PASS.
6. Add generate-command regressions for both dry-run and write-mode zero-conversion failures, including assertion that generated output remains available in write mode.
  </action>
  <verify>
    <automated>npm run build && npm run test:run -- src/core/scorer.test.ts src/cli/commands/generate.test.ts</automated>
  </verify>
  <done>
    QUAL-02 zero-conversion runs now fail explicitly with a dedicated marker-gate section, while write mode still preserves generated output for manual repair.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds.
2. `npm run test:run -- src/core/scorer.test.ts src/cli/commands/generate.test.ts` succeeds.
3. CLI always prints a dedicated marker-coverage section with detected/emitted/unresolved totals and explicit PASS/FAIL state.
4. Zero-conversion runs fail with exit code 1 in both dry-run and write mode.
5. Write mode still emits and verifies output before failing on QUAL-02.
</verification>

<success_criteria>
- QUAL-02 is enforced as an explicit gate, not advisory text
- PASS/FAIL marker status is unambiguous in terminal and CI logs
- Zero-conversion failure behavior is consistent across dry-run and write flows
- Existing non-gate diagnostics continue to work alongside marker gate reporting
</success_criteria>

<output>
After completion, create `.planning/phases/19-marker-coverage-audit-reporting/19-02-SUMMARY.md` documenting marker gate rule enforcement, CLI PASS/FAIL reporting, and dry-run/write failure semantics.
</output>
