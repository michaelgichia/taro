---
phase: 03-query-test-design-intelligence
task: 0
total_tasks: 7
status: pending_execution
last_updated: 2026-03-06T18:20:00.000Z
---

<current_state>
Phase 3: Query & Test Design Intelligence - Gap Closure Ready

The phase was fully planned (Plans 01-06) and executed. VERIFICATION.md found 1 gap:
- TEST-03: selectMatcher() exists but NOT wired into generation pipeline

Gap closure Plan 07 was just created to wire selectMatcher() into the pipeline.
Plan 07 is ready for execution.
</current_state>

<completed_work>

- Phase 3 Plans 01-06: All completed (type contracts, js-parser, resolver, scanner, templates, CLI wiring)
- VERIFICATION.md: Created, found 1 gap (TEST-03)
- Gap closure Plan 07: Created (not yet executed)
  - Task 1: Wire selectMatcher() in generate.ts
  - Task 2: Extend QueryResult type with matcher field
  - Task 3: Update stepTemplate to accept matcher parameter
  - Task 4: Pass matcher from generator to stepTemplate
</completed_work>

<remaining_work>

- Execute Plan 07 (gap closure):
  - Task 1: Import selectMatcher in generate.ts, call after buildQuery(), store in queryResults
  - Task 2: Add optional matcher field to QueryResult in src/types/recording.ts
  - Task 3: Modify stepTemplate() to accept matcher parameter, use in assert case
  - Task 4: Pass matcher from queryResults through generator to stepTemplate
- Verify: Run build, test generation to confirm matchers appear in output
- Update VERIFICATION.md to mark TEST-03 as resolved
</remaining_work>

<decisions_made>

- Gap closure approach: Wire existing selectMatcher() function instead of rewriting
- Implementation order: generate.ts → types → templates → generator (dependency-aware)
</decisions_made>

<blockers>
- None - Plan 07 created and ready for execution
</blockers>

<context>
After Phase 3 Plan 06 was marked complete, VERIFICATION.md identified that selectMatcher() exists in resolver.ts (lines 103-126) with full implementation for toHaveValue, toBeChecked, toHaveTextContent, toBeVisible, but it's NEVER called in the generation pipeline. The test-template.ts also uses hardcoded toBeInTheDocument() for assert actions.

Gap closure plan 07 wires this up: generate.ts calls selectMatcher, stores in queryResults, templates accept matcher param, generator passes it through.
</context>

<next_action>
Execute Plan 07: Run /gsd/execute-phase 3 (or specifically execute plan 07)

The plan will:
1. Import and call selectMatcher(info, step.action) in generate.ts loop
2. Add matcher?: string to QueryResult interface
3. Update stepTemplate to use matcher param for assert case
4. Pass matcher from generator to stepTemplate

After execution, verify with npm run build and test generation.
</next_action>
