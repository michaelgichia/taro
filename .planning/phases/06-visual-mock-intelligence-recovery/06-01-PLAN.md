---
phase: 06-visual-mock-intelligence-recovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/recording.ts
  - src/core/resolver.ts
  - src/core/resolver.test.ts
autonomous: true
requirements:
  - VIS-01

must_haves:
  truths:
    - "Playwright inspection can capture a visual state artifact when DOM-only inspection is insufficient"
    - "Visual inspection returns structured state data rather than only accessibility metadata"
    - "Screenshot/state capture remains intentional and testable, not unconditional"
  artifacts:
    - path: "src/core/resolver.ts"
      provides: "Visual-state capture helpers on top of the existing Playwright resolver"
      exports: ["inspectElements", "captureVisualState"]
    - path: "src/core/resolver.test.ts"
      provides: "Automated verification for screenshot/state capture behavior"
  key_links:
    - from: "src/core/resolver.ts"
      to: "src/types/recording.ts"
      via: "Visual snapshot/state types"
      pattern: "VisualState|DialogState"
---

<objective>
Extend the Playwright resolver from DOM inspection into visual-state capture.

Purpose: Phase 6 visual intelligence starts with a stronger resolver contract. Tayo already knows how to inspect DOM nodes, but VIS-01 requires it to capture richer UI state when needed, including screenshot-backed state artifacts and structured visual metadata.
</objective>

<execution_context>
@/Users/michaelgichia/.codex/taro/workflows/execute-plan.md
@/Users/michaelgichia/.codex/taro/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-visual-mock-intelligence-recovery/06-RESEARCH.md
@.planning/phases/06-visual-mock-intelligence-recovery/06-VALIDATION.md
@src/core/resolver.ts
@src/core/resolver.test.ts
@src/types/recording.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add visual-state types for resolver output</name>
  <files>src/types/recording.ts</files>
  <behavior>
    - resolver can return a structured visual snapshot/state object
    - screenshot metadata is represented explicitly rather than as loose strings
    - dialog or container state can be described alongside element metadata
  </behavior>
  <action>
Extend `src/types/recording.ts` with the minimum visual-intelligence types needed for resolver output.

Add explicit types for:
- visual state capture metadata
- optional screenshot path or capture token
- dialog/container state descriptors

Keep the existing resolver call sites compatible; these should be additive types that Phase 6 can consume without destabilizing earlier phases.
  </action>
  <verify>
    <automated>cd /Users/michaelgichia/workspace/tayo && npm run build</automated>
  </verify>
  <done>
Visual-state types exist and compile cleanly with the existing recording and resolver contracts.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add captureVisualState() to resolver.ts</name>
  <files>src/core/resolver.ts</files>
  <behavior>
    - Playwright can capture a screenshot-backed state artifact when requested
    - resolver returns visual state metadata in a deterministic format
    - capture remains opt-in or condition-based rather than always-on
  </behavior>
  <action>
Extend `src/core/resolver.ts` with a `captureVisualState()` helper and any supporting internal helpers needed for VIS-01.

Requirements:
- reuse the existing Playwright browser flow where practical
- return structured state data, not only a screenshot filepath
- keep capture explicit so generation can decide when to invoke it
- avoid changing existing query-resolution behavior unless necessary
  </action>
  <verify>
    <automated>cd /Users/michaelgichia/workspace/tayo && npm run test:run -- src/core/resolver.test.ts && npm run build</automated>
  </verify>
  <done>
Resolver exposes a tested visual-state capture path on top of the existing DOM inspection behavior.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend resolver tests for visual capture</name>
  <files>src/core/resolver.test.ts</files>
  <behavior>
    - tests prove screenshot/state capture is returned in a structured shape
    - tests prove the visual-capture path stays deterministic under mocked Playwright
    - existing resolver behavior remains green
  </behavior>
  <action>
Expand `src/core/resolver.test.ts` to cover VIS-01 directly.

Add tests for:
- successful visual-state capture
- screenshot/state metadata shape
- graceful behavior when capture fails

Use the existing mocked Playwright strategy; do not require a live browser for unit verification.
  </action>
  <verify>
    <automated>cd /Users/michaelgichia/workspace/tayo && npm run test:run -- src/core/resolver.test.ts && npm run build</automated>
  </verify>
  <done>
Resolver tests now verify the visual-intelligence foundation without regressing the Phase 3 query-resolution behavior.
  </done>
</task>

</tasks>

<verification>
1. `npm run test:run -- src/core/resolver.test.ts` passes
2. `npm run build` passes
3. Visual capture remains opt-in and returns typed state artifacts
</verification>

<success_criteria>
Tayo has a tested visual-state capture primitive on top of the existing Playwright resolver, giving later plans a reliable VIS-01 integration seam.
</success_criteria>

<output>
After completion, create `.planning/phases/06-visual-mock-intelligence-recovery/06-01-SUMMARY.md`
</output>
