---
phase: 06-visual-mock-intelligence-recovery
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/core/recording-intelligence.ts
  - src/core/resolver.ts
  - src/cli/commands/generate.ts
  - src/core/resolver.test.ts
autonomous: true
requirements:
  - VIS-01
  - VIS-02

must_haves:
  truths:
    - "Dialog-heavy or ambiguous intent groups can trigger visual-state capture intentionally"
    - "Tayo understands multi-step dialog flows as dialog-state sequences instead of flat clicks and assertions"
    - "Generation consumes dialog-aware visual summaries without regressing the Phase 5 cleanup pipeline"
  artifacts:
    - path: "src/core/resolver.ts"
      provides: "Dialog-aware visual-state capture helpers"
      exports: ["captureVisualState", "extractDialogState"]
    - path: "src/cli/commands/generate.ts"
      provides: "Visual-intelligence integration before generation"
      contains: "captureVisualState"
  key_links:
    - from: "src/core/recording-intelligence.ts"
      to: "src/core/resolver.ts"
      via: "Intent-group aware dialog/state triggers"
      pattern: "intentGroups|dialog"
    - from: "src/cli/commands/generate.ts"
      to: "src/core/resolver.ts"
      via: "Visual capture integration for dialog or ambiguous UI states"
      pattern: "captureVisualState"
---

<objective>
Add dialog-state understanding and wire visual intelligence into generation.

Purpose: VIS-01 is only useful if the pipeline knows when visual capture is warranted, and VIS-02 requires dialog flows to be treated as UI states instead of raw event bursts. This plan turns the visual foundation into generation-time behavior.
</objective>

<execution_context>
@/Users/michaelgichia/.codex/taro/workflows/execute-plan.md
@/Users/michaelgichia/.codex/taro/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-visual-mock-intelligence-recovery/06-RESEARCH.md
@.planning/phases/06-visual-mock-intelligence-recovery/06-VALIDATION.md
@.planning/phases/06-visual-mock-intelligence-recovery/06-01-PLAN.md
@src/core/recording-intelligence.ts
@src/core/resolver.ts
@src/cli/commands/generate.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Model dialog-aware visual states</name>
  <files>src/core/recording-intelligence.ts, src/core/resolver.ts</files>
  <behavior>
    - dialog open/confirm flows can be identified from intent groups and resolver output
    - visual intelligence can describe dialog state transitions in a deterministic way
    - ambiguous UI states have a clear trigger for screenshot/state capture
  </behavior>
  <action>
Build a dialog-state layer that connects Phase 5 intent groups to the visual resolver.

Requirements:
- identify dialog-relevant intent groups
- extract dialog-facing state information from resolver output
- keep heuristics deterministic and testable
- avoid duplicating Phase 5 intent grouping logic from scratch
  </action>
  <verify>
    <automated>cd /Users/michaelgichia/workspace/tayo && npm run test:run -- src/core/resolver.test.ts && npm run build</automated>
  </verify>
  <done>
Dialog-state understanding exists as an explicit, testable layer rather than an implicit side effect of clicks and assertions.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate visual intelligence into generate.ts</name>
  <files>src/cli/commands/generate.ts</files>
  <behavior>
    - generation can trigger visual-state capture for dialog-heavy or ambiguous states
    - visual summaries are surfaced without disrupting recording cleanup, scoring, or verification order
    - dry-run and write flows remain intact
  </behavior>
  <action>
Update `src/cli/commands/generate.ts` to invoke the visual-intelligence path when appropriate.

Requirements:
- use conservative triggering rules
- keep visual intelligence advisory; do not block writes on missing screenshots
- preserve the existing order of cleanup → generation → score → write → verify
- keep console output concise and phase-consistent
  </action>
  <verify>
    <automated>cd /Users/michaelgichia/workspace/tayo && npm run build</automated>
  </verify>
  <done>
The generate pipeline can now reason about dialog-heavy or ambiguous UI states with explicit visual-intelligence support.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend tests for dialog-state integration</name>
  <files>src/core/resolver.test.ts</files>
  <behavior>
    - tests prove dialog-state extraction and visual-trigger logic
    - visual integration remains deterministic under mocked Playwright
    - earlier resolver tests remain green
  </behavior>
  <action>
Expand `src/core/resolver.test.ts` or adjacent targeted tests to prove VIS-02 directly.

Add coverage for:
- multi-step dialog state extraction
- capture-trigger behavior for ambiguous/dialog flows
- non-dialog flows avoiding unnecessary visual capture
  </action>
  <verify>
    <automated>cd /Users/michaelgichia/workspace/tayo && npm run test:run -- src/core/resolver.test.ts && npm run build</automated>
  </verify>
  <done>
The visual-intelligence integration is covered by deterministic tests and ready for phase-level verification.
  </done>
</task>

</tasks>

<verification>
1. `npm run test:run -- src/core/resolver.test.ts` passes
2. `npm run build` passes
3. Dialog-aware visual capture only triggers when the pipeline identifies a meaningful UI-state need
</verification>

<success_criteria>
Visual intelligence is now active in the pipeline, with dialog-aware state understanding and explicit screenshot/state capture support for VIS-01 and VIS-02.
</success_criteria>

<output>
After completion, create `.planning/phases/06-visual-mock-intelligence-recovery/06-02-SUMMARY.md`
</output>
