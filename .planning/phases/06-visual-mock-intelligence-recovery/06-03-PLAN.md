---
phase: 06-visual-mock-intelligence-recovery
plan: 03
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - src/types/conventions.ts
  - src/core/scanner.ts
  - src/core/mock-intelligence.ts
  - src/core/mock-intelligence.test.ts
autonomous: true
requirements:
  - MOCK-01
  - MOCK-02

must_haves:
  truths:
    - "Tayo can detect repeated mock targets across the codebase instead of only the majority mock style"
    - "Mock intelligence can recommend inline vs extract decisions deterministically"
    - "Mock analysis is implemented as a dedicated module, not hidden inside convention scanning"
  artifacts:
    - path: "src/core/mock-intelligence.ts"
      provides: "Repeated target detection and inline/extract recommendations"
      exports: ["analyzeMocks", "scanMockTargets", "deriveMockRecommendations"]
    - path: "src/core/mock-intelligence.test.ts"
      provides: "Automated coverage for MOCK-01 and MOCK-02 heuristics"
  key_links:
    - from: "src/core/mock-intelligence.ts"
      to: "src/core/scanner.ts"
      via: "Consumes discovered test files and mock conventions"
      pattern: "findTestFiles|readConventions"
    - from: "src/core/mock-intelligence.ts"
      to: "src/types/conventions.ts"
      via: "Mock analysis metadata or conventions extension"
      pattern: "mock"
---

<objective>
Build the mock-intelligence analysis foundation.

Purpose: The current scanner can detect whether the project prefers `vi.mock` or `jest.mock`, but it cannot produce actionable mock recommendations. This plan introduces a dedicated mock-intelligence module for repeated target detection and inline vs extract decisions.
</objective>

<execution_context>
@/Users/michaelgichia/.codex/taro/workflows/execute-plan.md
@/Users/michaelgichia/.codex/taro/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-visual-mock-intelligence-recovery/06-RESEARCH.md
@.planning/phases/06-visual-mock-intelligence-recovery/06-VALIDATION.md
@src/types/conventions.ts
@src/core/scanner.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define mock-intelligence output types and data model</name>
  <files>src/types/conventions.ts, src/core/mock-intelligence.ts</files>
  <behavior>
    - repeated mock targets can be represented explicitly
    - inline vs extract recommendations have a typed, deterministic output shape
    - the scanner and generator can consume the same mock-analysis result
  </behavior>
  <action>
Add the types needed for mock-intelligence output.

Prefer a dedicated `src/core/mock-intelligence.ts` module, and only extend `src/types/conventions.ts` if shared types truly belong there. The result model should represent:
- repeated mock targets
- mock recommendation kind (inline vs extract)
- supporting evidence such as reuse count or detected files
  </action>
  <verify>
    <automated>cd /Users/michaelgichia/workspace/tayo && npm run build</automated>
  </verify>
  <done>
Mock-intelligence output is explicitly typed and ready for scanner-backed analysis.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement repeated-target detection and recommendation heuristics</name>
  <files>src/core/mock-intelligence.ts, src/core/mock-intelligence.test.ts</files>
  <behavior>
    - repeated mock targets are detected across discovered test files
    - recommendations distinguish inline vs extract using deterministic heuristics
    - behavior is testable without a live codebase dependency
  </behavior>
  <action>
Create `src/core/mock-intelligence.ts` and `src/core/mock-intelligence.test.ts`.

Requirements:
- scan existing test content for repeated mock targets
- derive inline vs extract decisions from repeat count and codebase patterns
- keep the module separate from `scanner.ts` except for data acquisition/reuse
- favor deterministic string/AST-light analysis consistent with the rest of the repo
  </action>
  <verify>
    <automated>cd /Users/michaelgichia/workspace/tayo && npm run test:run -- src/core/mock-intelligence.test.ts && npm run build</automated>
  </verify>
  <done>
Mock intelligence can now detect repeated targets and produce actionable inline/extract recommendations with automated test coverage.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reuse scanner outputs without bloating scanner responsibility</name>
  <files>src/core/scanner.ts, src/core/mock-intelligence.ts</files>
  <behavior>
    - mock intelligence can reuse discovered test files and conventions
    - scanner remains a coarse convention tool, not the primary mock analyzer
    - integration points are explicit and stable
  </behavior>
  <action>
Add only the minimal scanner integration needed for mock intelligence.

Possible examples:
- export or reuse discovered test file lists
- expose a helper that lets mock intelligence inspect test content efficiently

Avoid stuffing repeated-target or decision heuristics directly into `scanner.ts`.
  </action>
  <verify>
    <automated>cd /Users/michaelgichia/workspace/tayo && npm run test:run -- src/core/mock-intelligence.test.ts src/core/scanner.test.ts && npm run build</automated>
  </verify>
  <done>
Mock intelligence reuses scanner data cleanly without blurring module boundaries.
  </done>
</task>

</tasks>

<verification>
1. `npm run test:run -- src/core/mock-intelligence.test.ts src/core/scanner.test.ts` passes
2. `npm run build` passes
3. Repeated-target detection and inline/extract recommendations are deterministic and typed
</verification>

<success_criteria>
Tayo now has a dedicated mock-intelligence analyzer that can detect repeated mock targets and recommend whether mocks should be inline or extracted.
</success_criteria>

<output>
After completion, create `.planning/phases/06-visual-mock-intelligence-recovery/06-03-SUMMARY.md`
</output>
