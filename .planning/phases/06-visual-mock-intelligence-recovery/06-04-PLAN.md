---
phase: 06-visual-mock-intelligence-recovery
plan: 04
type: execute
wave: 4
depends_on:
  - 06-03
files_modified:
  - src/core/mock-intelligence.ts
  - src/core/mock-intelligence.test.ts
  - src/cli/commands/generate.ts
autonomous: false
requirements:
  - MOCK-01
  - MOCK-02
  - MOCK-03
  - MOCK-04

must_haves:
  truths:
    - "Mock intelligence identifies mutation lifecycle patterns in existing tests"
    - "Mock intelligence warns about unstable mock-instance patterns"
    - "Generate integrates mock-analysis summaries without regressing existing output, scoring, or verification behavior"
  artifacts:
    - path: "src/core/mock-intelligence.ts"
      provides: "Mutation lifecycle and stability analysis"
      exports: ["analyzeMutationLifecycle", "detectMockInstability", "analyzeMocks"]
    - path: "src/cli/commands/generate.ts"
      provides: "Mock-aware generation integration"
      contains: "analyzeMocks"
  key_links:
    - from: "src/cli/commands/generate.ts"
      to: "src/core/mock-intelligence.ts"
      via: "Pre-generation mock analysis"
      pattern: "analyzeMocks"
    - from: "src/core/mock-intelligence.ts"
      to: "src/core/mock-intelligence.test.ts"
      via: "Mutation/stability test coverage"
      pattern: "mutation|stability"
---

<objective>
Integrate mock intelligence into generation and complete lifecycle/stability reasoning.

Purpose: Phase 6 only closes the mock gap when generation can reason about mock strategy before writing output. This plan finishes the mock analyzer with lifecycle/stability heuristics and wires it into the real CLI pipeline.
</objective>

<execution_context>
@/Users/michaelgichia/.codex/get-shit-done/workflows/execute-plan.md
@/Users/michaelgichia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-visual-mock-intelligence-recovery/06-RESEARCH.md
@.planning/phases/06-visual-mock-intelligence-recovery/06-VALIDATION.md
@.planning/phases/06-visual-mock-intelligence-recovery/06-03-PLAN.md
@src/core/mock-intelligence.ts
@src/cli/commands/generate.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add mutation lifecycle and stability heuristics</name>
  <files>src/core/mock-intelligence.ts, src/core/mock-intelligence.test.ts</files>
  <behavior>
    - mutation lifecycle reimplementation patterns are detected from existing tests
    - unstable mock-instance patterns are surfaced as explicit warnings
    - analysis remains deterministic and easy to verify
  </behavior>
  <action>
Extend `src/core/mock-intelligence.ts` with MOCK-03 and MOCK-04 heuristics.

Requirements:
- detect loading/success/error mock-flow patterns around mutations
- identify likely unstable mock instance patterns (recreated factories, per-test churn, or similar deterministic evidence)
- add focused test coverage in `src/core/mock-intelligence.test.ts`
  </action>
  <verify>
    <automated>cd /Users/michaelgichia/workspace/taro && npm run test:run -- src/core/mock-intelligence.test.ts && npm run build</automated>
  </verify>
  <done>
Mock intelligence now covers mutation lifecycle and stability analysis in addition to repeated-target detection.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate mock intelligence into generate.ts</name>
  <files>src/cli/commands/generate.ts</files>
  <behavior>
    - generation runs mock analysis before output
    - mock-aware summaries or hints are surfaced without disrupting visual intelligence, cleanup, scoring, or verification order
    - the pipeline remains advisory rather than blocking on missing mocks
  </behavior>
  <action>
Update `src/cli/commands/generate.ts` to call the mock analyzer and surface the result.

Requirements:
- run mock analysis before or alongside generation preparation
- keep the integration concise and phase-consistent
- preserve the existing ordering of cleanup, generation, score logging, write, and post-write verification
- avoid generating speculative mock code beyond what the deterministic analyzer can justify
  </action>
  <verify>
    <automated>cd /Users/michaelgichia/workspace/taro && npm run build</automated>
  </verify>
  <done>
The generate pipeline now includes mock-aware reasoning before test output is finalized.
  </done>
</task>

<task type="manual">
  <name>Task 3: Verify visual/mock-aware generation end to end</name>
  <files>src/cli/commands/generate.ts</files>
  <action>
Run one end-to-end verification against a dialog-heavy and mock-heavy fixture/project context.

Confirm:
- visual-intelligence logging or state capture appears when expected
- mock recommendations reflect repeated targets and mutation/stability findings
- generation still succeeds
- post-write verification still runs
  </action>
  <verify>
    <manual>Run `npm run build`, then execute `taro generate` against fixtures that exercise dialog state and repeated mocks; inspect console output and generated file behavior.</manual>
  </verify>
  <done>
Manual verification confirms visual/mock-aware generation works in the real CLI pipeline without regressing existing output guarantees.
  </done>
</task>

</tasks>

<verification>
1. `npm run test:run -- src/core/mock-intelligence.test.ts` passes
2. `npm run build` passes
3. Manual CLI verification confirms visual/mock summaries and post-write verification coexist
</verification>

<success_criteria>
Taro can now reason about visual UI state and mock strategy before generation, covering the remaining Phase 6 visual/mock intelligence gaps.
</success_criteria>

<output>
After completion, create `.planning/phases/06-visual-mock-intelligence-recovery/06-04-SUMMARY.md`
</output>
