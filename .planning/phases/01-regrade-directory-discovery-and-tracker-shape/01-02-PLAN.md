---
phase: 01-regrade-directory-discovery-and-tracker-shape
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/index.ts
  - src/cli/commands/regrade.ts
  - src/cli/commands/tests/regrade.test.ts
autonomous: true
requirements:
  - RGDIR-01
  - RGDIR-02
  - RGDIR-03
user_setup: []
must_haves:
  truths:
    - The regrade command surface accepts a directory target only when `--directory-loop` is present.
    - Regrade directory discovery includes `*.test.*` and `*.spec.*` files and excludes non-test files.
    - Running regrade directory mode materializes a canonical tracker for discovered test files using the Phase 1 tracker shape and records the current stored score threshold when that value exists.
  artifacts:
    - src/cli/commands/regrade.ts contains the internal regrade command surface for directory-loop discovery and tracker bootstrap.
    - src/cli/commands/tests/regrade.test.ts covers directory validation and test-file discovery.
    - src/index.ts wires the regrade command entrypoint into the internal CLI dispatch table.
  key_links:
    - The regrade command calls the tracker module created in Plan 01 to persist discovered test entries under `.taro/directory-loop/`.
    - Directory validation and discovery messages in tests match the implemented regrade command behavior.
---

<objective>
Add the Phase 1 regrade directory entry surface that validates inputs, discovers eligible test files, and writes the tracker bootstrap for later batch execution work.

Purpose: Phase 1 must make `regrade --directory-loop` concrete enough to discover the workset and persist the canonical tracker before Phase 2 adds sequential scoring and history writes.
Output: An internal regrade command implementation, CLI wiring, and tests that cover directory-only validation and eligible test discovery.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-regrade-directory-discovery-and-tracker-shape/01-RESEARCH.md
@.planning/phases/01-regrade-directory-discovery-and-tracker-shape/01-01-SUMMARY.md
@src/index.ts
@src/cli/commands/target.ts
@src/cli/commands/target-directory-tracker.ts
@.codex/skills/@taro-test/rtl-regrade/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the internal regrade command surface with a verifying test harness</name>
  <files>src/index.ts, src/cli/commands/regrade.ts, src/cli/commands/tests/regrade.test.ts</files>
  <read_first>src/index.ts, src/cli/commands/target.ts, src/cli/commands/tests/target.test.ts, .codex/skills/@taro-test/rtl-regrade/SKILL.md, .planning/phases/01-regrade-directory-discovery-and-tracker-shape/01-RESEARCH.md</read_first>
  <action>Create `src/cli/commands/regrade.ts` with a `createRegradeCommand()` internal command surface that mirrors the style of `createTargetCommand()` for argument parsing and error handling. Wire it into `src/index.ts` under an internal `__regrade` dispatch branch. In the same task, create `src/cli/commands/tests/regrade.test.ts` using the `target.test.ts` sandbox/command-runner pattern so the new command can be verified immediately. Add a required target path argument plus a `--directory-loop` boolean option. Reject directory input without `--directory-loop`, and reject `--directory-loop` when the target path is a single file. Keep this phase focused on discovery and tracker bootstrap; do not implement the Phase 2 sequential scoring loop here.</action>
  <verify>
    <automated>pnpm test -- --run src/cli/commands/tests/regrade.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/index.ts` contains `__regrade` and calls `createRegradeCommand()`.
    - `src/cli/commands/regrade.ts` contains `.option("--directory-loop"` or the Commander equivalent for the new flag.
    - `src/cli/commands/tests/regrade.test.ts` exists and contains a rejection test for directory input without `--directory-loop`.
    - `src/cli/commands/regrade.ts` contains error text for both `Directory input requires --directory-loop` and `--directory-loop is only valid when the target path is a directory`.
  </acceptance_criteria>
  <done>The repo has a concrete internal regrade command path with the correct Phase 1 flag validation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement eligible test discovery and tracker bootstrap</name>
  <files>src/cli/commands/regrade.ts</files>
  <read_first>src/cli/commands/regrade.ts, src/cli/commands/target.ts, src/cli/commands/target-directory-tracker.ts, src/core/state.ts</read_first>
  <action>Implement directory scanning in `src/cli/commands/regrade.ts` so `--directory-loop` collects only `*.test.*` and `*.spec.*` files under the target directory, skips non-test files, and materializes a tracker via the Plan 01 tracker module under `.taro/directory-loop/`. While bootstrapping the tracker, read the latest stored generated-test score for each discovered test when one exists and write that current stored score threshold into the tracker row. Log the tracker path and a pending-work summary in the same `[taro]` logging style used by `target.ts`. Leave per-file scoring execution and `appendGeneratedTestRecord()` writes for Phase 2.</action>
  <verify>
    <automated>pnpm test -- --run src/cli/commands/tests/regrade.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/cli/commands/regrade.ts` contains pattern checks for `.test.` and `.spec.` files.
    - `src/cli/commands/regrade.ts` reads the latest stored score for a discovered test and writes that current threshold into the tracker when available.
    - `src/cli/commands/regrade.ts` writes or logs a tracker path under `.taro/directory-loop/`.
    - `src/cli/commands/regrade.ts` does not call `appendGeneratedTestRecord(` in this phase.
  </acceptance_criteria>
  <done>Directory-loop regrade can discover its workset, persist the canonical tracker, and capture the current stored score threshold without prematurely implementing Phase 2 scoring behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Expand regrade tests for discovery rows and stored-threshold coverage</name>
  <files>src/cli/commands/tests/regrade.test.ts</files>
  <read_first>src/cli/commands/tests/target.test.ts, src/cli/commands/tests/target-directory-tracker.test.ts, src/cli/commands/regrade.ts</read_first>
  <action>Expand `src/cli/commands/tests/regrade.test.ts` so it proves: a mixed directory queues only `*.test.*` and `*.spec.*` files, a successful bootstrap run logs the tracker path plus writes tracker rows for the discovered tests, and a seeded generated-test record causes the current stored score threshold to appear in the tracker output. Keep the directory-validation assertions added in Task 1 and add the new discovery/threshold assertions here rather than moving them into a separate Wave 0 dependency.</action>
  <verify>
    <automated>pnpm test -- --run src/cli/commands/tests/regrade.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/cli/commands/tests/regrade.test.ts` contains a rejection test for `--directory-loop` on a single file.
    - `src/cli/commands/tests/regrade.test.ts` contains an assertion that non-test files are excluded from the tracker rows.
    - `src/cli/commands/tests/regrade.test.ts` contains an assertion that a seeded test’s current stored score threshold appears in the tracker output.
  </acceptance_criteria>
  <done>The Phase 1 command surface is protected by focused tests for validation and discovery behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test -- --run src/cli/commands/tests/regrade.test.ts` exits 0
- [ ] `pnpm test -- --run src/cli/commands/tests/target-directory-tracker.test.ts` exits 0
- [ ] A directory-loop regrade test proves only `*.test.*` and `*.spec.*` files appear in the tracker
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Regrade directory mode validates inputs, discovers only eligible test files, and persists a canonical tracker bootstrap for later phases
</success_criteria>

<output>
After completion, create `.planning/phases/01-regrade-directory-discovery-and-tracker-shape/01-02-SUMMARY.md`
</output>
