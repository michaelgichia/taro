---
phase: 08-readme-documentation
plan: "02"
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - README.md
autonomous: true
requirements:
  - DOCS-04
  - DOCS-05

must_haves:
  truths:
    - "Developer can read a worked example that shows a real Chrome recording input and the corresponding RTL test output"
    - "Developer can read a guide for invoking Taro as a Claude Code skill or agent tool and configure it without additional help"
  artifacts:
    - path: "README.md"
      provides: "Complete public documentation including worked example and Claude skill guide"
      contains: "Worked Example and Claude Code Skill sections"
  key_links:
    - from: "README.md Worked Example"
      to: "generated RTL test output"
      via: "fenced code block showing input recording and output test file"
      pattern: "describe\\|it\\(|userEvent|screen\\."
    - from: "README.md Claude Code Skill section"
      to: "taro generate invocation"
      via: "agent tool configuration example"
      pattern: "taro generate|@tayo/rtl"
---

<objective>
Extend README.md with two remaining sections: a Worked Example that shows a real Chrome recording going in and a generated RTL test coming out, and a Claude Code Skill guide that explains how to invoke Taro as an agent tool.

Purpose: Developers need to see concrete input/output before they trust a code generation tool. The Claude skill guide ensures AI-assisted developers can wire Taro into their Claude Code workflows without additional help.
Output: README.md completed with Worked Example and Claude Code Skill sections, making the document self-contained for all target audiences.
</objective>

<execution_context>
@/Users/michaelgichia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/michaelgichia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-readme-documentation/08-01-SUMMARY.md

<interfaces>
<!-- Key types and behaviors needed to write accurate examples -->

From src/types/recording.ts:
- Input: ChromeRecorderExport with title + steps (click, fill, navigate, etc.)
- Steps contain: type, selectors, value, target (URL), assertedEvents

From src/cli/commands/generate.ts:
- JS format detection: file ends with .js OR contains @jest-environment-options
- Generates: describe block with title from recording, it() blocks per intent group
- Convention-aware: reads .taro/conventions.json for import style, mock pattern, folder pattern
- Score output: "[taro] Score: {N}/100 ({grade}) — query: {N}, assertions: {N}, structure: {N}"
- Post-write: "[taro] ✓ post-write verified"

Realistic Chrome Recorder JSON structure (for worked example input):
```json
{
  "title": "login flow",
  "steps": [
    { "type": "navigate", "url": "http://localhost:3000/login" },
    { "type": "click", "selectors": [["aria/Email address"]] },
    { "type": "change", "value": "user@example.com", "selectors": [["#email"]] },
    { "type": "click", "selectors": [["aria/Password"]] },
    { "type": "change", "value": "secret123", "selectors": [["#password"]] },
    { "type": "click", "selectors": [["aria/Sign in[role=\"button\"]"]] },
    { "type": "waitForElement", "selectors": [["aria/Welcome back"]] }
  ]
}
```

Realistic generated RTL test output:
```typescript
import { render, screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { LoginPage } from '../LoginPage'

describe('login flow', () => {
  it('should complete login flow', async () => {
    const user = userEvent.setup()
    render(<LoginPage />)

    await user.click(screen.getByRole('textbox', { name: /email address/i }))
    await user.type(screen.getByRole('textbox', { name: /email address/i }), 'user@example.com')
    await user.click(screen.getByRole('textbox', { name: /password/i }))
    await user.type(screen.getByRole('textbox', { name: /password/i }), 'secret123')
    await user.click(screen.getByRole('button', { name: /sign in/i }))

    expect(screen.getByText(/welcome back/i)).toBeInTheDocument()
  })
})
```

Claude Code skill invocation pattern (standard for CLI tools):
- Place recording file in project
- Invoke: `npx @tayo/rtl generate ./recording.js`
- Or configure as a Claude Code skill in .claude/skills/
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Worked Example section (DOCS-04)</name>
  <files>README.md</files>
  <action>
    Append a `## Worked Example` section to README.md. This section must show a real Chrome Recorder recording going in and the generated RTL test coming out. Do not use placeholder text or "...".

    **Section structure:**

    ### Input: Chrome Recorder export (`login-flow.json`)

    Introduce with 1 sentence: "Here is a typical Chrome Recorder JSON export capturing a login flow."

    Show the full JSON in a fenced `json` code block:
    ```json
    {
      "title": "login flow",
      "steps": [
        { "type": "navigate", "url": "http://localhost:3000/login" },
        { "type": "click", "selectors": [["aria/Email address"]] },
        { "type": "change", "value": "user@example.com", "selectors": [["#email"]] },
        { "type": "click", "selectors": [["aria/Password"]] },
        { "type": "change", "value": "secret123", "selectors": [["#password"]] },
        { "type": "click", "selectors": [["aria/Sign in[role=\"button\"]"]] },
        { "type": "waitForElement", "selectors": [["aria/Welcome back"]] }
      ]
    }
    ```

    ### Command

    Show the command in a `bash` code block:
    ```bash
    taro generate ./login-flow.json
    ```

    ### Terminal output

    Show expected terminal output in a fenced code block (no syntax highlighting):
    ```
    Parsed: login flow — 7 steps
    [taro] Score: 82/100 (B) — query: 90, assertions: 75, structure: 80
    Created: login-flow.test.tsx
    [taro] ✓ post-write verified
    ```

    ### Output: Generated test (`login-flow.test.tsx`)

    Introduce with 1 sentence: "Taro generates a convention-aware RTL test with accessible queries:"

    Show the full generated test in a fenced `typescript` code block:
    ```typescript
    import { render, screen } from '@testing-library/react'
    import userEvent from '@testing-library/user-event'
    import { LoginPage } from '../LoginPage'

    describe('login flow', () => {
      it('should complete login flow', async () => {
        const user = userEvent.setup()
        render(<LoginPage />)

        await user.click(screen.getByRole('textbox', { name: /email address/i }))
        await user.type(screen.getByRole('textbox', { name: /email address/i }), 'user@example.com')
        await user.click(screen.getByRole('textbox', { name: /password/i }))
        await user.type(screen.getByRole('textbox', { name: /password/i }), 'secret123')
        await user.click(screen.getByRole('button', { name: /sign in/i }))

        expect(screen.getByText(/welcome back/i)).toBeInTheDocument()
      })
    })
    ```

    ### What Taro did here

    Brief bullet list (4-5 items):
    - Parsed the navigate step and inferred the component under test
    - Upgraded CSS selectors (`#email`, `#password`) to accessible `getByRole` queries using aria attributes from the recording
    - Inferred `userEvent.type()` from change steps and `userEvent.click()` from click steps
    - Mapped the `waitForElement` step to a `toBeInTheDocument()` assertion
    - Scored the output (82/100) and emitted no blocking errors

    Note: The component import path (`../LoginPage`) is a placeholder. Taro generates a comment indicating where to update it. Keep this note in the section.
  </action>
  <verify>
    <automated>grep -c "login-flow.json\|login flow\|login-flow.test.tsx" README.md</automated>
  </verify>
  <done>README.md Worked Example section exists with a complete JSON input recording, the generate command, expected terminal output, and the full generated TypeScript test. A developer can see exactly what goes in and what comes out.</done>
</task>

<task type="auto">
  <name>Task 2: Write Claude Code Skill guide (DOCS-05)</name>
  <files>README.md</files>
  <action>
    Append a `## Using Taro as a Claude Code Skill` section to README.md. This section must enable a developer using Claude Code to invoke Taro as an agent tool without additional help.

    **Section structure:**

    ### Overview

    1-2 sentences: Taro works naturally as a Claude Code skill. You can instruct Claude to run `taro generate` on a recording file and it will generate the test, report the score, and surface any quality hints — all in a single agent turn.

    ### Option A: Direct invocation (no setup required)

    Explain that Claude Code can invoke Taro directly using the Bash tool. Show a prompt example in a fenced `markdown` code block:

    ```
    Run: npx @tayo/rtl generate ./recordings/checkout-flow.js
    Then report the score and the path of the generated file.
    ```

    This requires no skill configuration — Claude calls npx inline.

    ### Option B: Register as a Claude Code skill

    Explain that registering Taro as a skill lets Claude invoke it by name without knowing the full command.

    **Step 1** — Create the skill file at `.claude/skills/taro/SKILL.md` in your project. Show the file contents in a fenced `markdown` code block:

    ```markdown
    # Taro — RTL Test Generator

    ## Purpose
    Generate a React Testing Library test from a Chrome Recorder export.

    ## Invocation
    Run: taro generate <recording-file>

    ## Flags
    - `--dry-run` (-d): Preview the generated test without writing to disk
    - `--output <path>` (-o): Override the output file path
    - `--force` (-f): Overwrite an existing test file

    ## Output
    Writes `{recording-name}.test.tsx` next to the recording file.
    Reports score (0-100) and any quality hints.
    ```

    **Step 2** — Ensure Taro is installed in the project:
    ```bash
    npm install --save-dev @tayo/rtl
    ```

    **Step 3** — Ask Claude to use the skill:
    ```
    Use the taro skill to generate a test from ./recordings/login-flow.js
    ```

    ### Tips for agent use

    Brief bullet list:
    - Use `--dry-run` first to preview output before committing generated files
    - If you record multiple flows, run Taro on each to build up convention state in `.taro/conventions.json` — later runs benefit from earlier ones
    - Pass `--force` when re-recording an updated flow to overwrite the old test
    - The `.taro/` directory should be committed to your repo so convention learning persists across team members

    ### Notes

    - Taro does not require network access at generation time (DOM inspection via Playwright is optional and only runs when a live URL is in the recording)
    - All state is local to `.taro/` — no external service is contacted
  </action>
  <verify>
    <automated>grep -c "Claude Code\|SKILL.md\|\.claude/skills" README.md</automated>
  </verify>
  <done>README.md Claude Code Skill section exists with direct invocation example, skill file registration steps, and agent use tips. A developer using Claude Code can configure Taro as a skill without additional help.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `README.md` contains all five requirement sections:
   - `grep -c "Introduction\|Quick Start\|CLI Reference\|Worked Example\|Claude Code" README.md` returns 5 or more
2. Worked example has all four parts: input JSON, command, terminal output, generated test
   - `grep -c "login-flow.json\|taro generate\|login-flow.test.tsx\|getByRole" README.md` returns 4 or more
3. Claude skill guide has skill file contents and both invocation options
   - `grep -c "SKILL.md\|\.claude/skills\|npx @tayo/rtl" README.md` returns 3 or more
4. README.md is a complete standalone document covering all five DOCS requirements
</verification>

<success_criteria>
- README.md contains a Worked Example section with real input/output (not placeholders)
- README.md contains a Claude Code Skill section with skill file template, install step, and usage instructions
- All five DOCS requirements (DOCS-01 through DOCS-05) are addressed across the two plans
- README.md reads as a single coherent document, not five disconnected sections
</success_criteria>

<output>
After completion, create `.planning/phases/08-readme-documentation/08-02-SUMMARY.md`
</output>
