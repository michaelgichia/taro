---
phase: 08-readme-documentation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03

must_haves:
  truths:
    - "Developer reads the README and understands what Tayo is, who it is for, and the problem it solves without reading source code"
    - "Developer can follow the Quick Start section and run their first generate command from a clean install"
    - "Developer can look up every `tayo generate` flag and its behavior from the README alone"
  artifacts:
    - path: "README.md"
      provides: "Public-facing documentation root"
      contains: "Introduction, Quick Start, CLI Reference sections"
      min_lines: 100
  key_links:
    - from: "README.md Quick Start"
      to: "npm install @tayo/rtl"
      via: "install command block"
      pattern: "npm install.*@tayo/rtl|npx @tayo/rtl"
    - from: "README.md CLI Reference"
      to: "generate command"
      via: "flags table"
      pattern: "--output|--dry-run|--force"
---

<objective>
Create README.md with the first three content sections: Introduction (what/who/why), Quick Start (install through first generated test), and CLI Reference (all generate flags documented).

Purpose: A developer who lands on the GitHub repo must immediately understand what Tayo does, be able to run it from a fresh machine in under 5 minutes, and look up any flag without guessing.
Output: README.md with Introduction, Quick Start, and CLI Reference sections written and accurate.
</objective>

<execution_context>
@/Users/michaelgichia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/michaelgichia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

<interfaces>
<!-- CLI entry point and generate command flags extracted from src/ -->

From src/index.ts:
- CLI name: `tayo`
- Version flag: `-v, --version`
- Help flag: `-h, --help`
- Subcommand: `generate`

From src/cli/commands/generate.ts:
```typescript
export interface GenerateOptions {
  output?: string    // -o, --output <path>    Output file path for the generated test
  dryRun?: boolean   // -d, --dry-run          Preview without writing to disk (default: false)
  force?: boolean    // -f, --force            Overwrite existing test file (default: false)
}

// Positional argument: <file>   Path to the Chrome Recorder JSON export file
// Description: "Generate RTL test from Chrome Recorder export"
```

Input formats:
- Chrome Recorder JSON export (`.json` file from Chrome DevTools Recorder)
- Testing Library Recorder JS export (`.js` file, detected by `.js` extension or `@jest-environment-options` header)

Output: auto-derived as `{input-basename}.test.tsx` in same directory, or overridden with `--output`

npm package name: `@tayo/rtl`
npx invocation: `npx @tayo/rtl generate ./recording.js`
local invocation: `tayo generate ./recording.js`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Introduction section (DOCS-01)</name>
  <files>README.md</files>
  <action>
    Create README.md. Write the Introduction section covering:

    1. **Project badge line** — a single line at top: "Generate React Testing Library tests from Chrome Recorder recordings — automatically."
    2. **What is Tayo** — 2-3 sentence prose: Tayo is a CLI tool that reads Chrome DevTools Recorder exports (JSON) and Testing Library Recorder JS files and generates RTL test files. It scores its own output, learns your project's test conventions from existing files, and stores per-project state in a local `.tayo/` directory. No server, no cloud — just files.
    3. **Who it is for** — bullet list: React developers who write tests with `@testing-library/react`, developers who use Chrome DevTools Recorder to capture user flows, teams that want test coverage without spending hours writing boilerplate.
    4. **The problem it solves** — 2-3 sentences: Recording a user flow in Chrome takes 30 seconds. Translating that recording into a well-structured RTL test takes 20–40 minutes and requires knowing which queries to use, how to assert, and how to match your project's test conventions. Tayo closes that gap.
    5. **How it works** — 4-step numbered list (no code blocks yet, just prose): (1) Record a user flow in Chrome DevTools → Recorder panel. (2) Export via the Testing Library Recorder extension (`.js`) or as native Chrome Recorder JSON (`.json`). (3) Run `tayo generate ./recording.js`. (4) Tayo writes a `.test.tsx` file next to your recording, scored and convention-aware.

    Use standard GitHub Markdown. No emoji. Heading hierarchy: `# Tayo` (h1), `## Introduction` or just start with the badge line followed by sections using `##` headings. Keep each section focused and scannable.
  </action>
  <verify>
    <automated>grep -c "Chrome Recorder\|React Testing Library\|tayo generate" README.md</automated>
  </verify>
  <done>README.md exists. Introduction section clearly explains what Tayo is, who it is for, and what problem it solves. Mentions Chrome Recorder, RTL, and `tayo generate`. No source code reading required to understand the tool.</done>
</task>

<task type="auto">
  <name>Task 2: Write Quick Start section (DOCS-02)</name>
  <files>README.md</files>
  <action>
    Append a `## Quick Start` section to README.md. It must be completable end-to-end in under 5 minutes from a clean install.

    Structure the section as:

    **Prerequisites** (brief list):
    - Node.js 18 or later
    - A React project using `@testing-library/react`
    - Chrome DevTools Recorder (built into Chrome — no extension needed for JSON exports)

    **Step 1 — Install** (code block):
    ```
    npm install --save-dev @tayo/rtl
    # or use npx to skip install entirely
    npx @tayo/rtl generate ./my-recording.js
    ```

    **Step 2 — Record a user flow** (prose + sub-steps):
    Open Chrome DevTools → Recorder panel → click "Start new recording" → perform your user flow (clicks, form fills, navigation) → click "End recording". Then either:
    - Export as JSON: click the export button → "JSON" → save as `recording.json`
    - Export via Testing Library Recorder extension: install the extension, click its export button, save as `recording.js`

    **Step 3 — Generate the test** (code blocks):
    ```
    # Local install
    npx tayo generate ./recording.js

    # Or if installed globally
    tayo generate ./recording.js
    ```
    Expected output:
    ```
    Parsed: my user flow — 8 steps
    [tayo] Score: 78/100 (B) — query: 80, assertions: 70, structure: 85
    Created: src/components/MyComponent.test.tsx
    [tayo] ✓ post-write verified
    ```

    **What happens next** (brief prose):
    Tayo writes a `.test.tsx` file. On subsequent runs in the same project, it reads `.tayo/conventions.json` to match your test style (import style, mock pattern, folder structure) automatically.

    Do not invent or fabricate CLI flags or output formats. Use only what is confirmed from the source code.
  </action>
  <verify>
    <automated>grep -c "npm install\|npx.*tayo\|Step 1\|Step 2\|Step 3" README.md</automated>
  </verify>
  <done>README.md Quick Start section exists with install command, record instructions, generate command, and expected terminal output. A developer can follow it from scratch in under 5 minutes.</done>
</task>

<task type="auto">
  <name>Task 3: Write CLI Reference section (DOCS-03)</name>
  <files>README.md</files>
  <action>
    Append a `## CLI Reference` section to README.md. Document every flag and argument for `tayo generate`.

    **Section structure:**

    ### `tayo generate <file>`

    Generates a React Testing Library test from a Chrome Recorder export.

    **Arguments:**

    | Argument | Description |
    |----------|-------------|
    | `<file>` | Path to the recording file. Accepts Chrome Recorder JSON exports (`.json`) or Testing Library Recorder JS files (`.js`). |

    **Options:**

    | Flag | Short | Default | Description |
    |------|-------|---------|-------------|
    | `--output <path>` | `-o` | Same directory as input, `{name}.test.tsx` | Override the output file path for the generated test. |
    | `--dry-run` | `-d` | `false` | Print the generated test to stdout and show the score without writing to disk. Useful for previewing output before committing. |
    | `--force` | `-f` | `false` | Overwrite an existing test file. Without this flag, Tayo exits with an error if the output file already exists. |
    | `--version` | `-v` | — | Print the installed version and exit. |
    | `--help` | `-h` | — | Display command help and exit. |

    **Examples:**

    ```bash
    # Generate and write a test next to the recording
    tayo generate ./recordings/checkout-flow.json

    # Preview without writing (dry run)
    tayo generate --dry-run ./recordings/checkout-flow.json

    # Write to a specific path
    tayo generate --output src/__tests__/checkout.test.tsx ./recordings/checkout-flow.json

    # Overwrite an existing test
    tayo generate --force ./recordings/checkout-flow.json
    ```

    **Output file naming:**
    If `--output` is not provided, Tayo derives the output path from the input file: `{input-dir}/{input-basename}.test.tsx`. For example, `./recordings/login.json` → `./recordings/login.test.tsx`.

    **Supported input formats:**
    - Chrome Recorder JSON (`.json`) — exported directly from Chrome DevTools Recorder
    - Testing Library Recorder JS (`.js`) — exported via the Testing Library Recorder Chrome extension; detected by `.js` extension or `@jest-environment-options` header

    Document only what the source code confirms. Do not add flags that do not exist in `src/cli/commands/generate.ts`.
  </action>
  <verify>
    <automated>grep -c "\-\-output\|\-\-dry-run\|\-\-force" README.md</automated>
  </verify>
  <done>README.md CLI Reference section exists with a complete flags table for `tayo generate`, covering all three options (--output, --dry-run, --force), the positional argument, and usage examples. Every documented flag matches what is in src/cli/commands/generate.ts.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `README.md` exists in the project root
2. `grep -c "Introduction\|Quick Start\|CLI Reference" README.md` returns 3
3. All three option flags are present: `grep -c "\-\-output\|\-\-dry-run\|\-\-force" README.md` returns 3 or more
4. Install command is present: `grep "npm install.*@tayo/rtl\|npx.*@tayo/rtl" README.md`
5. No invented flags or behaviors — everything matches src/cli/commands/generate.ts
</verification>

<success_criteria>
- README.md exists with Introduction, Quick Start, and CLI Reference sections
- Introduction answers: what Tayo is, who it is for, what problem it solves
- Quick Start gives step-by-step instructions completable in under 5 minutes from clean install
- CLI Reference documents every flag for `tayo generate` accurately
- All content is accurate to the actual codebase (no fabricated features)
</success_criteria>

<output>
After completion, create `.planning/phases/08-readme-documentation/08-01-SUMMARY.md`
</output>
