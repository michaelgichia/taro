---
phase: 11-runtime-targets-asset-delivery
plan: "03"
type: execute
wave: 2
depends_on:
  - "11-01"
files_modified:
  - assets/codex/
  - src/install/runtimes/codex.ts
  - src/install/codex-runtime.test.ts
autonomous: true
requirements:
  - RUNT-04

must_haves:
  truths:
    - "Codex installs skills under `skills/@tayo-dev/rtl-*`, not prompt files"
    - "Codex exposes the expected `$@tayo-dev/rtl-help` skill entrypoint"
    - "Codex gets a broader skill surface than the prompt-based runtimes"
  artifacts:
    - path: "assets/codex/"
      provides: "Packaged Codex skill directories"
      contains: "SKILL.md"
    - path: "src/install/runtimes/codex.ts"
      provides: "Codex-specific install operation builder"
      contains: "buildCodexOperations"
    - path: "src/install/codex-runtime.test.ts"
      provides: "Codex temp-directory skill install coverage"
      contains: "@tayo-dev/rtl-help"
  key_links:
    - from: "src/install/runtimes/codex.ts"
      to: "assets/codex/"
      via: "skill directory copy mapping"
      pattern: "SKILL.md"
    - from: "src/install/codex-runtime.test.ts"
      to: "src/install/runtimes/codex.ts"
      via: "temp-directory skill assertions"
      pattern: "buildCodexOperations"
---

<objective>
Deliver the packaged Codex skill suite and prove that Codex installs are skill-first, not prompt-based.

Purpose: satisfy the Codex runtime requirement with a distinct one-folder-per-skill delivery model.
Output: packaged Codex skills, Codex runtime install mapping, and automated temp-directory coverage.
</objective>

<execution_context>
@/Users/michaelgichia/.codex/taro/workflows/execute-plan.md
@/Users/michaelgichia/.codex/taro/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-runtime-targets-asset-delivery/11-CONTEXT.md
@.planning/phases/11-runtime-targets-asset-delivery/11-RESEARCH.md
@src/install/types.ts
@src/install/registry.ts
@src/install/manifest.ts
@src/install/assets.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create packaged Codex skills and runtime module</name>
  <files>assets/codex/, src/install/runtimes/codex.ts</files>
  <action>
Implement the Codex runtime delivery layer.

Required outcomes:
1. Add a broader Codex skill suite as packaged skill directories, with one `skills/@tayo-dev/rtl-*` folder per skill.
2. Ensure one of those skills exposes the help entrypoint expected by `$@tayo-dev/rtl-help`.
3. Build a Codex runtime module that maps packaged skill directories into the current global/local Codex destinations.
4. Keep Codex implementation distinct from the prompt-runtime surface and modules.

Do not fold Codex into a generic prompt-runtime helper in this plan.
  </action>
  <verify>
    <automated>rg -n "SKILL.md|@tayo-dev/rtl-help|buildCodexOperations" assets/codex src/install/runtimes/codex.ts</automated>
  </verify>
  <done>
    Codex packaged skills and runtime mapping exist as a separate skill-first runtime family.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Codex temp-directory skill install tests</name>
  <files>src/install/codex-runtime.test.ts</files>
  <action>
Add automated tests that prove Codex skill directories install correctly into temp directories.

Required outcomes:
1. Cover the global Codex skill path.
2. Cover the local Codex skill path under `./.codex`.
3. Assert that multiple namespaced skill directories are emitted.
4. Assert that the help skill exposes the expected naming for `$@tayo-dev/rtl-help`.
5. Keep the tests filesystem-local and deterministic.
  </action>
  <verify>
    <automated>npm run test:run -- src/install/codex-runtime.test.ts</automated>
  </verify>
  <done>
    Codex skill delivery is covered by automated temp-directory tests.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds.
2. `npm run test:run -- src/install/codex-runtime.test.ts` succeeds.
3. `find assets/codex -name SKILL.md | wc -l` shows multiple namespaced skill payloads.
</verification>

<success_criteria>
- Codex installs skill directories, not prompt files
- `$@tayo-dev/rtl-help` is represented in the packaged skill set
- Codex remains a distinct runtime family with one-folder-per-skill layout
</success_criteria>

<output>
After completion, create `.planning/phases/11-runtime-targets-asset-delivery/11-03-SUMMARY.md` documenting the Codex skill suite and temp-directory coverage.
</output>
