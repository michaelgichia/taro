---
phase: 11-runtime-targets-asset-delivery
plan: "04"
type: execute
wave: 3
depends_on:
  - "11-01"
  - "11-02"
  - "11-03"
files_modified:
  - src/install/writer.ts
  - src/install/executor.ts
  - src/install/planner.ts
  - src/install/summary.ts
  - src/cli/commands/install.ts
  - src/install/write-execution.test.ts
  - src/cli/commands/install.test.ts
autonomous: true
requirements:
  - RUNT-01
  - RUNT-02
  - RUNT-03
  - RUNT-04
  - RUNT-05

must_haves:
  truths:
    - "The installer can execute real write operations for all selected runtimes after confirmation"
    - "`--all` installs all supported runtimes in one run and reports what was written"
    - "Existing `@tayo-dev/rtl` assets are handled conservatively with visible conflict behavior"
    - "A visible ownership marker or manifest is written wherever the installer owns files"
  artifacts:
    - path: "src/install/writer.ts"
      provides: "Concrete filesystem write engine and conflict handling"
      contains: "writeInstallPlan"
    - path: "src/install/executor.ts"
      provides: "Install execution/result aggregation across runtime families"
      contains: "executeInstallPlan"
    - path: "src/cli/commands/install.ts"
      provides: "Phase 11 install execution wired into the existing confirmation flow"
      contains: "executeInstallPlan"
  key_links:
    - from: "src/cli/commands/install.ts"
      to: "src/install/executor.ts"
      via: "post-confirmation write execution"
      pattern: "executeInstallPlan"
    - from: "src/install/executor.ts"
      to: "src/install/writer.ts"
      via: "runtime operation write pipeline"
      pattern: "writeInstallPlan"
---

<objective>
Turn the Phase 10 preview flow and Phase 11 runtime assets into a real multi-runtime install execution path.

Purpose: execute writes after confirmation, handle namespaced conflicts conservatively, and report actual runtime-specific install results.
Output: writer/executor modules, CLI integration, and tests that prove `--all` writes all supported runtimes in one run.
</objective>

<execution_context>
@/Users/michaelgichia/.codex/taro/workflows/execute-plan.md
@/Users/michaelgichia/.codex/taro/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-runtime-targets-asset-delivery/11-CONTEXT.md
@.planning/phases/11-runtime-targets-asset-delivery/11-RESEARCH.md
@src/install/planner.ts
@src/install/summary.ts
@src/cli/commands/install.ts
@src/install/registry.ts
@src/install/manifest.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement install execution, manifest writes, and conflict handling</name>
  <files>src/install/writer.ts, src/install/executor.ts, src/install/planner.ts, src/install/summary.ts</files>
  <action>
Implement the concrete Phase 11 write path.

Required outcomes:
1. Extend the install plan or execution layer so post-confirmation writes can be executed deterministically.
2. Write namespaced assets for all selected runtimes using the packaged runtime payloads created in earlier plans.
3. Create the visible ownership marker/manifest alongside owned assets.
4. Detect existing `@tayo-dev/rtl` assets and apply the locked conflict contract explicitly:
   - installer-owned asset with no user edits -> require replace confirmation before overwriting
   - installer-owned asset with user edits detected -> protect it and stop that write path without overwriting
   - non-Tayo file colliding with a namespaced target -> block with a clear conflict result instead of mutating shared runtime files
5. Produce structured install results that can report what was written per runtime.

Keep broader rerun/update repair behavior out of scope for this plan.
  </action>
  <verify>
    <automated>rg -n "writeInstallPlan|executeInstallPlan|manifest|conflict" src/install/writer.ts src/install/executor.ts src/install/planner.ts src/install/summary.ts</automated>
  </verify>
  <done>
    The installer has a real write path with manifest support and conservative conflict handling.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire install execution into the CLI and prove `--all` reporting</name>
  <files>src/cli/commands/install.ts, src/install/write-execution.test.ts, src/cli/commands/install.test.ts</files>
  <action>
Integrate Phase 11 runtime delivery into the CLI flow and test the all-runtime path.

Required outcomes:
1. After confirmation, the install command should execute actual writes instead of stopping at the Phase 10 prewrite checkpoint.
2. `--all` should install all supported runtimes in one run.
3. Final output should report what was written per runtime and keep the runtime-specific help commands visible.
4. Add automated coverage for the multi-runtime execution path, replace-confirmation behavior, and protected-manual-edit behavior.
5. Preserve `tayo generate` behavior while the installer becomes write-capable.
  </action>
  <verify>
    <automated>npm run test:run -- src/install/write-execution.test.ts src/cli/commands/install.test.ts</automated>
  </verify>
  <done>
    CLI execution, all-runtime reporting, and runtime-specific result output are covered by automated tests.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds.
2. `npm run test:run -- src/install/write-execution.test.ts src/cli/commands/install.test.ts` succeeds.
3. A non-interactive temp-directory install using `--all --global` writes assets for Claude Code, OpenCode, Gemini CLI, and Codex and reports the expected verification commands.
4. Automated tests prove the conflict contract:
   - replace confirmation is required for existing installer-owned assets
   - user-edited installer assets are protected instead of overwritten
   - colliding non-Tayo files are reported as conflicts
</verification>

<success_criteria>
- Runtime assets are actually written after confirmation
- `--all` installs all supported runtimes in one run
- Results reporting reflects real writes, not only planned writes
- Manifest/ownership marker output exists for later Phase 12 repair work
- The write path explicitly enforces the locked replace/protect conflict rules
</success_criteria>

<output>
After completion, create `.planning/phases/11-runtime-targets-asset-delivery/11-04-SUMMARY.md` documenting write execution, conflict behavior, and all-runtime reporting.
</output>
