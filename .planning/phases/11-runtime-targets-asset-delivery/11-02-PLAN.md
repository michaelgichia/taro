---
phase: 11-runtime-targets-asset-delivery
plan: "02"
type: execute
wave: 2
depends_on:
  - "11-01"
files_modified:
  - assets/claude/
  - assets/gemini/
  - assets/opencode/
  - src/install/runtimes/prompt-runtimes.ts
  - src/install/runtimes/claude.ts
  - src/install/runtimes/gemini.ts
  - src/install/runtimes/opencode.ts
  - src/install/prompt-runtimes.test.ts
autonomous: true
requirements:
  - RUNT-01
  - RUNT-02
  - RUNT-03

must_haves:
  truths:
    - "Claude Code installations can emit `/@tayo-dev/rtl:help` from packaged prompt/command assets"
    - "Gemini CLI installations can emit `/@tayo-dev/rtl:help` from packaged prompt/command assets"
    - "OpenCode installations can emit `/@tayo-dev/rtl-help` and use the locked local `./.opencode` target"
    - "Prompt-based runtimes stay mostly aligned in vocabulary while remaining fully namespaced to `@tayo-dev/rtl`"
  artifacts:
    - path: "assets/claude/"
      provides: "Packaged Claude Code runtime assets"
      contains: "@tayo-dev/rtl"
    - path: "assets/gemini/"
      provides: "Packaged Gemini CLI runtime assets"
      contains: "@tayo-dev/rtl"
    - path: "assets/opencode/"
      provides: "Packaged OpenCode runtime assets"
      contains: "@tayo-dev/rtl"
    - path: "src/install/runtimes/prompt-runtimes.ts"
      provides: "Shared prompt-runtime install operation builder"
      contains: "buildPromptRuntimeOperations"
  key_links:
    - from: "src/install/runtimes/opencode.ts"
      to: "src/install/registry.ts"
      via: "runtime-specific destination mapping"
      pattern: ".opencode"
    - from: "src/install/prompt-runtimes.test.ts"
      to: "assets/claude/"
      via: "temp-directory install assertions"
      pattern: "@tayo-dev/rtl"
---

<objective>
Deliver the packaged runtime assets and temp-directory install behavior for Claude Code, Gemini CLI, and OpenCode.

Purpose: satisfy the prompt-based runtime requirements with a minimal but real `@tayo-dev/rtl` runtime surface.
Output: prompt-runtime asset files, runtime-specific mapping modules, and tests that prove the assets land in the right directories.
</objective>

<execution_context>
@/Users/michaelgichia/.codex/get-shit-done/workflows/execute-plan.md
@/Users/michaelgichia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-runtime-targets-asset-delivery/11-CONTEXT.md
@.planning/phases/11-runtime-targets-asset-delivery/11-RESEARCH.md
@src/install/types.ts
@src/install/registry.ts
@src/install/manifest.ts
@src/install/assets.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create prompt-based runtime asset payloads and runtime modules</name>
  <files>assets/claude/, assets/gemini/, assets/opencode/, src/install/runtimes/prompt-runtimes.ts, src/install/runtimes/claude.ts, src/install/runtimes/gemini.ts, src/install/runtimes/opencode.ts</files>
  <action>
Implement the prompt-runtime delivery layer for Claude Code, Gemini CLI, and OpenCode.

Required outcomes:
1. Add packaged assets for each prompt-based runtime using fully namespaced `@tayo-dev/rtl` file/folder naming.
2. Keep the installed surface minimal: help plus the core runtime command(s) needed to use Tayo.
3. Build runtime modules that map the shared prompt-runtime surface into Claude, Gemini, and OpenCode destination layouts.
4. Preserve the locked OpenCode local target of `./.opencode`.
5. Keep shared command vocabulary aligned across these runtimes wherever their target format allows it.

Do not implement Codex skills in this plan.
  </action>
  <verify>
    <automated>rg -n "@tayo-dev/rtl:help|@tayo-dev/rtl-help|\\.opencode|buildPromptRuntimeOperations" assets/claude assets/gemini assets/opencode src/install/runtimes</automated>
  </verify>
  <done>
    Prompt-runtime assets and runtime modules exist for Claude Code, Gemini CLI, and OpenCode.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add prompt-runtime temp-directory tests</name>
  <files>src/install/prompt-runtimes.test.ts</files>
  <action>
Add automated tests that prove the prompt-based runtime assets install correctly into temp directories.

Required outcomes:
1. Cover at least one global/local temp-directory path for Claude Code.
2. Cover at least one global/local temp-directory path for Gemini CLI.
3. Cover at least one global/local temp-directory path for OpenCode, including the local `./.opencode` shape.
4. Assert that expected namespaced help assets are present after the install operation builder runs.
5. Keep the tests filesystem-local and deterministic.
  </action>
  <verify>
    <automated>npm run test:run -- src/install/prompt-runtimes.test.ts</automated>
  </verify>
  <done>
    Prompt-runtime install behavior is covered by automated temp-directory tests.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds.
2. `npm run test:run -- src/install/prompt-runtimes.test.ts` succeeds.
3. `rg -n "@tayo-dev/rtl:help|@tayo-dev/rtl-help" assets/claude assets/gemini assets/opencode` confirms the expected runtime-native help entrypoints are present.
</verification>

<success_criteria>
- Claude Code assets support `/@tayo-dev/rtl:help`
- Gemini CLI assets support `/@tayo-dev/rtl:help`
- OpenCode assets support `/@tayo-dev/rtl-help`
- Prompt-based runtimes remain minimal, aligned, and fully namespaced
</success_criteria>

<output>
After completion, create `.planning/phases/11-runtime-targets-asset-delivery/11-02-SUMMARY.md` documenting prompt-runtime asset delivery and temp-directory coverage.
</output>
