---
phase: 11-runtime-targets-asset-delivery
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/install/types.ts
  - src/install/registry.ts
  - src/install/manifest.ts
  - src/install/assets.ts
  - src/install/registry.test.ts
  - src/install/manifest.test.ts
autonomous: true
requirements:
  - RUNT-05

must_haves:
  truths:
    - "Phase 11 has a dedicated runtime registry richer than the Phase 10 path metadata"
    - "The package can ship runtime asset files in npm output instead of relying on in-memory strings only"
    - "Installer ownership can be represented explicitly before write execution begins"
  artifacts:
    - path: "src/install/registry.ts"
      provides: "Runtime registry with asset metadata and namespaced container definitions"
      contains: "RUNTIME_REGISTRY"
    - path: "src/install/manifest.ts"
      provides: "Owned-file manifest and conflict classification primitives"
      contains: "InstallOwnershipManifest"
    - path: "src/install/assets.ts"
      provides: "Packaged asset root/source resolution helpers"
      contains: "resolveAssetSource"
  key_links:
    - from: "src/install/registry.ts"
      to: "src/install/types.ts"
      via: "runtime and asset operation types"
      pattern: "RuntimeDefinition"
    - from: "package.json"
      to: "assets/"
      via: "published package file inclusion"
      pattern: "assets"
---

<objective>
Create the packaged-asset and ownership foundation that every Phase 11 runtime delivery plan will build on.

Purpose: lock the runtime registry, manifest model, and asset source resolution before any runtime-specific files are written.
Output: shared installer foundation modules plus package manifest updates that allow runtime assets to ship with `@tayo-dev/rtl`.
</objective>

<execution_context>
@/Users/michaelgichia/.codex/get-shit-done/workflows/execute-plan.md
@/Users/michaelgichia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-runtime-targets-asset-delivery/11-CONTEXT.md
@.planning/phases/11-runtime-targets-asset-delivery/11-RESEARCH.md
@.planning/phases/11-runtime-targets-asset-delivery/11-VALIDATION.md
@package.json
@src/install/types.ts
@src/install/resolver.ts
@src/install/planner.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create runtime registry and ownership foundation</name>
  <files>src/install/types.ts, src/install/registry.ts, src/install/manifest.ts, src/install/assets.ts, src/install/registry.test.ts, src/install/manifest.test.ts</files>
  <action>
Extend the installer foundation for runtime delivery.

Required outcomes:
1. Introduce a dedicated runtime registry module that models:
   - runtime id and display name
   - global/local install containers
   - runtime asset kind (`prompt`, `command`, `skill`, `manifest`)
   - runtime verification command
   - any runtime-specific namespaced grouping rules
2. Add an ownership/manifest module that can represent installer-owned files and classify collisions before writes occur.
3. Add packaged asset source resolution helpers so the later writer can locate runtime payloads from the published package.
4. Keep Codex modeled as a separate skill-first runtime family rather than a prompt-runtime variant.
5. Add the foundation tests referenced by `11-VALIDATION.md` so registry and manifest behavior can be checked immediately.

Do not implement actual file writes in this plan.
  </action>
  <verify>
    <automated>rg -n "RUNTIME_REGISTRY|InstallOwnershipManifest|resolveAssetSource|assetKind" src/install/types.ts src/install/registry.ts src/install/manifest.ts src/install/assets.ts</automated>
  </verify>
  <done>
    Shared runtime and ownership primitives exist and future write plans can consume them without rediscovering layout rules.
  </done>
</task>

<task type="auto">
  <name>Task 2: Make runtime assets publishable with the package</name>
  <files>package.json</files>
  <action>
Update package publishing configuration so runtime asset files can ship with the npm package.

Required outcomes:
1. Create a clear package-level path for runtime assets (for example under `assets/`) that Phase 11 runtime plans can populate.
2. Update `package.json` file inclusion so the published tarball includes runtime assets alongside `dist`.
3. Keep the change minimal: no remote asset fetching, no separate asset package, no new build system unless absolutely needed.

Do not add the actual runtime asset files yet; only make their future location publishable and explicit.
  </action>
  <verify>
    <automated>rg -n "\"files\"|assets" package.json</automated>
  </verify>
  <done>
    The package manifest is ready to ship packaged runtime assets added in later plans.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds after the new installer foundation modules are added.
2. `npm run test:run -- src/install/registry.test.ts src/install/manifest.test.ts` succeeds if the plan adds those tests; otherwise create the files needed by `11-VALIDATION.md`.
3. `rg -n "\"files\"|assets" package.json` confirms runtime asset packaging is explicit.
</verification>

<success_criteria>
- Runtime registry and manifest primitives exist
- Packaged asset source resolution exists
- Package publishing is ready to include runtime assets
- Later runtime delivery plans can work from shared installer contracts instead of ad hoc path logic
</success_criteria>

<output>
After completion, create `.planning/phases/11-runtime-targets-asset-delivery/11-01-SUMMARY.md` documenting the runtime registry, manifest, and packaging foundation.
</output>
