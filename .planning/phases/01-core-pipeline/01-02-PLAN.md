---
phase: 01-core-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true

must_haves:
  truths:
    - "User can run 'tayo generate ./recording.json' from project root"
    - "CLI accepts recording file as argument"
    - "CLI shows helpful usage information"
  artifacts:
    - path: "src/index.ts"
      provides: "CLI entry point with commander setup"
    - path: "src/cli/commands/generate.ts"
      provides: "Generate command with file argument handling"
  key_links:
    - from: "src/index.ts"
      to: "src/cli/commands/generate.ts"
      via: "commander .command('generate')"
---

<objective>
Implement the CLI interface with commander - setup commands, arguments, and usage information.
</objective>

<context>
@~/.opencode/taro/references/ui-brand.md
</context>

<tasks>

<task type="auto">
  <name>Set up commander CLI in index.ts</name>
  <files>src/index.ts</files>
  <action>
    Create src/index.ts with:
    - Import commander and picocolors
    - Create program with name, description, version
    - Register generate command
    - Add --help and --version support
    - Export for use as CLI entry point
  </action>
  <verify>Run `npx ts-node src/index.ts --help` shows usage information</verify>
  <done>CLI shows usage with generate command available</done>
</task>

<task type="auto">
  <name>Create generate command with file argument</name>
  <files>src/cli/commands/generate.ts</files>
  <action>
    Create src/cli/commands/generate.ts with:
    - Command name: 'generate <file>'
    - Description: 'Generate RTL test from Chrome Recorder export'
    - Options: --output, --dry-run
    - Action handler that validates file exists and is readable
    - Stub call to pipeline (will be implemented in later plans)
  </action>
  <verify>Run `npx ts-node src/index.ts generate --help` shows generate command options</verify>
  <done>Generate command accepts file argument and shows options</done>
</task>

</tasks>

<verification>
- CLI --help shows available commands
- Generate command --help shows options
- Running generate with valid file shows "File found" or similar
</verification>

<success_criteria>
- CLI invoked as 'tayo' works (via npm bin link or direct node)
- Generate command accepts file path argument
- Error handling for missing files
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-pipeline/01-02-SUMMARY.md`
</output>
