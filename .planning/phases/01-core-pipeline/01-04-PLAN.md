---
phase: 01-core-pipeline
plan: 04
type: execute
wave: 2
depends_on: [01-01, 01-02]
files_modified: []
autonomous: true

must_haves:
  truths:
    - "Invalid JSON files produce clear, actionable error messages"
    - "Schema validation catches missing required fields"
    - "Validation errors include file location and issue description"
  artifacts:
    - path: "src/core/validator.ts"
      provides: "Input schema validation using Zod"
  key_links:
    - from: "src/cli/commands/generate.ts"
      to: "src/core/validator.ts"
      via: "import { validateRecording }"
---

<objective>
Implement input schema validation using Zod - validate Chrome Recorder JSON structure and provide clear error messages.
</objective>

<context>
@~/.opencode/taro/references/ui-brand.md
</context>

<tasks>

<task type="auto">
  <name>Create Zod schema for Chrome Recorder export</name>
  <files>src/core/validator.ts</files>
  <action>
    Create src/core/validator.ts with:
    - Import zod
    - Define ChromeRecorderSchema:
      - title: string (optional)
      - steps: array of step objects
      - each step has: type (string), selector (optional), value (optional), key (optional)
    - Validation function: validateRecording(data: unknown): ValidationResult
    - Returns { valid: true, data } or { valid: false, errors: [] }
    - Error messages include JSON path to issue
  </action>
  <verify>Run validator against valid and invalid JSON, verify error messages</verify>
  <done>Validator correctly identifies valid and invalid recordings</done>
</task>

<task type="auto">
  <name>Integrate validation into generate command</name>
  <files>src/cli/commands/generate.ts</files>
  <action>
    Update src/cli/commands/generate.ts to:
    - Import validateRecording from validator
    - Call validation after parsing JSON
    - Handle validation errors with clear messages
    - Exit with error code if validation fails
  </action>
  <verify>Run generate command with invalid JSON, see clear error message</verify>
  <done>CLI shows validation errors with actionable messages</done>
</task>

</tasks>

<verification>
- Test with valid Chrome Recorder JSON - passes
- Test with missing 'steps' field - shows error
- Test with invalid step type - shows error
- Error messages include line/position if possible
</verification>

<success_criteria>
- Zod schema validates Chrome Recorder format
- Clear error messages for all validation failures
- Integration with generate command works
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-pipeline/01-04-SUMMARY.md`
</output>
