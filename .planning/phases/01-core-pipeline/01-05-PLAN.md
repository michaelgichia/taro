---
phase: 01-core-pipeline
plan: 05
type: execute
wave: 3
depends_on: [01-03, 01-04]
files_modified: []
autonomous: true

must_haves:
  truths:
    - "Generated tests use getByRole, getByText, getByLabelText queries"
    - "Generated tests use proper describe/it block structure"
    - "Generated tests include necessary imports"
    - "Output is valid TypeScript/JavaScript that runs with Jest or Vitest"
  artifacts:
    - path: "src/core/generator.ts"
      provides: "RTL test code generation from normalized steps"
    - path: "src/templates/test-template.ts"
      provides: "Code templates for test structure"
  key_links:
    - from: "src/cli/commands/generate.ts"
      to: "src/core/generator.ts"
      via: "import { generateTest }"
    - from: "src/core/generator.ts"
      to: "src/templates/test-template.ts"
      via: "import templates"
---

<objective>
Implement RTL test code generation - convert normalized steps to valid React Testing Library test code.
</objective>

<context>
@~/.opencode/get-shit-done/references/ui-brand.md
</context>

<tasks>

<task type="auto">
  <name>Create test code templates</name>
  <files>src/templates/test-template.ts</files>
  <action>
    Create src/templates/test-template.ts with:
    - Import template: imports for React, Testing Library, describe, it, expect
    - Describe block template with name
    - Test case template for each action type:
      - click → userEvent.click(screen.getByRole(...))
      - fill → userEvent.type(screen.getByLabelText(...), ...)
      - select → selectOptions(screen.getByRole(...), ...)
      - scroll → window.scrollTo(...) or element.scrollIntoView()
      - assert → expect(screen.getBy...).toBeInTheDocument()
    - Use @testing-library/jest-dom for assertions
    - Use user-event for interactions (not fireEvent)
  </action>
  <verify>Templates produce valid JavaScript when filled</verify>
  <done>Templates defined for all action types</done>
</task>

<task type="auto">
  <name>Implement generator.ts</name>
  <files>src/core/generator.ts</files>
  <action>
    Create src/core/generator.ts with:
    - generateTest(normalizedSteps: NormalizedStep[], options: GenerateOptions): string
    - Options: { testName, outputPath, includeImports }
    - Query selection strategy:
      - Prefer getByRole with name (accessible)
      - Fall back to getByLabelText, getByText, getByPlaceholderText
      - Last resort: getByTestId (if user specified)
    - Convert Chrome selectors to RTL queries
    - Build complete test file content
    - Return as string for writing
  </action>
  <verify>Run generator on sample normalized steps, produce valid test code</verify>
  <done>Generator produces valid RTL test code</done>
</task>

</tasks>

<verification>
- Generate test from sample recording
- Verify output has proper imports
- Verify getByRole, getByText, getByLabelText queries used
- Verify Jest/Vitest compatible structure
</verification>

<success_criteria>
- Generated tests use getByRole primarily
- Tests are runnable with jest or vitest
- Proper describe/it block nesting
- User-event for interactions
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-pipeline/01-05-SUMMARY.md`
</output>
