---
phase: 01-core-pipeline
plan: 03
type: execute
wave: 2
depends_on: [01-01, 01-02]
files_modified: []
autonomous: true

must_haves:
  truths:
    - "Tayo parses Chrome Recorder JSON exports correctly"
    - "All step types are recognized (click, fill, select, scroll, assert)"
    - "Steps are normalized to internal representation"
  artifacts:
    - path: "src/core/parser.ts"
      provides: "Chrome Recorder JSON parsing and normalization"
    - path: "src/types/recording.ts"
      provides: "TypeScript types for recording and steps"
  key_links:
    - from: "src/cli/commands/generate.ts"
      to: "src/core/parser.ts"
      via: "import { parseRecording }"
    - from: "src/core/parser.ts"
      to: "src/types/recording.ts"
      via: "import types"
---

<objective>
Implement Chrome Recorder JSON parsing - handle all step types and normalize to internal format.
</objective>

<context>
@~/.opencode/taro/references/ui-brand.md
</context>

<tasks>

<task type="auto">
  <name>Create recording types</name>
  <files>src/types/recording.ts</files>
  <action>
    Create src/types/recording.ts with:
    - ChromeRecorderExport interface (title, steps, settings)
    - RecordingStep union type for step types: click, fill, select, scroll, assert, waitForSelector, doubleClick, keyDown
    - NormalizedStep interface (action, target, value, selector)
    - StepType enum mapping Chrome Recorder types
  </action>
  <verify>Types compile with TypeScript, no errors</verify>
  <done>TypeScript types for recording structure defined</done>
</task>

<task type="auto">
  <name>Implement parser.ts with step normalization</name>
  <files>src/core/parser.ts</files>
  <action>
    Create src/core/parser.ts with:
    - parseRecording(filePath: string): Promise<NormalizedRecording>
    - normalizeStep(chromeStep: ChromeStep): NormalizedStep
    - Supported step types mapping:
      - click → { action: 'click', target: selector }
      - fill → { action: 'fill', target: selector, value: value }
      - select → { action: 'select', target: selector, value: value }
      - scroll → { action: 'scroll', target: selector }
      - assert → { action: 'assert', target: selector, value: expected }
    - Error handling for unsupported step types
  </action>
  <verify>Run parser with sample Chrome Recorder JSON, output normalized steps</verify>
  <done>Parser correctly converts Chrome Recorder steps to normalized format</done>
</task>

</tasks>

<verification>
- Create sample Chrome Recorder JSON fixture
- Run parser on fixture
- Verify all step types are recognized and normalized
</verification>

<success_criteria>
- Parser handles click, fill, select, scroll, assert step types
- Unknown step types are logged as warnings, not errors
- Output is typed NormalizedRecording
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-pipeline/01-03-SUMMARY.md`
</output>
