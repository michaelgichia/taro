---
phase: 01-core-pipeline
plan: 06
type: execute
wave: 3
depends_on: [01-05]
files_modified: []
autonomous: true

must_haves:
  truths:
    - "Test files are created in the filesystem"
    - "Files have proper .test.tsx or .test.ts extension"
    - "Directory structure is created if it doesn't exist"
    - "Existing files can be overwritten with --force flag"
  artifacts:
    - path: "src/core/writer.ts"
      provides: "Test file filesystem writing"
  key_links:
    - from: "src/cli/commands/generate.ts"
      to: "src/core/writer.ts"
      via: "import { writeTestFile }"
---

<objective>
Implement test file writing - write generated tests to the filesystem with proper naming and location.
</objective>

<context>
@~/.opencode/taro/references/ui-brand.md
</context>

<tasks>

<task type="auto">
  <name>Implement writer.ts with file operations</name>
  <files>src/core/writer.ts</files>
  <action>
    Create src/core/writer.ts with:
    - writeTestFile(content: string, outputPath: string, options?: WriteOptions): Promise<void>
    - Options: { force?: boolean, createDir?: boolean }
    - Validate output path (must end in .test.ts or .test.tsx)
    - Check if file exists, prompt or error unless force
    - Create parent directories if needed
    - Write file with proper encoding
    - Return confirmation message with file path
  </action>
  <verify>Write test file to filesystem, verify file exists with correct content</verify>
  <done>Writer creates test files on filesystem</done>
</task>

<task type="auto">
  <name>Integrate writer into generate command</name>
  <files>src/cli/commands/generate.ts</files>
  <action>
    Update src/cli/commands/generate.ts to:
    - Import writeTestFile from writer
    - Connect parser -> validator -> generator -> writer pipeline
    - Add --output option for custom output path
    - Add --dry-run option to preview without writing
    - Handle success and error cases
    - Print success message with file path
  </action>
  <verify>Run complete pipeline: tayo generate ./recording.json</verify>
  <done>Full pipeline works: parse -> validate -> generate -> write</done>
</task>

</tasks>

<verification>
- Run tayo generate on sample recording
- Verify .test.ts file is created
- Verify file contains valid test code
- Test --output and --dry-run options
</verification>

<success_criteria>
- Test files written with correct extension
- Pipeline is complete and functional
- CLI provides clear feedback on success/failure
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-pipeline/01-06-SUMMARY.md`
</output>
