---
phase: 01-core-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true

must_haves:
  truths:
    - "User can run 'tayo generate ./recording.json' from project root without errors"
    - "Tayo recognizes and parses all Chrome Recorder step types (click, fill, select, scroll, assert)"
    - "Invalid JSON files produce clear, actionable error messages"
    - "Generated test files contain valid RTL queries (getByRole, getByText, getByLabelText)"
    - "Test files are written with proper imports and structure"
  artifacts:
    - path: "package.json"
      provides: "Project configuration and dependencies"
    - path: "src/index.ts"
      provides: "CLI entry point and commander setup"
    - path: "src/cli/commands/generate.ts"
      provides: "Generate command implementation"
    - path: "src/core/parser.ts"
      provides: "Chrome Recorder JSON parsing"
    - path: "src/core/validator.ts"
      provides: "Input schema validation"
    - path: "src/core/generator.ts"
      provides: "RTL test code generation"
    - path: "src/core/writer.ts"
      provides: "Test file filesystem writing"
  key_links:
    - from: "src/index.ts"
      to: "src/cli/commands/generate.ts"
      via: "commander command registration"
    - from: "src/cli/commands/generate.ts"
      to: "src/core/parser.ts"
      via: "import and function call"
    - from: "src/cli/commands/generate.ts"
      to: "src/core/validator.ts"
      via: "import and function call"
    - from: "src/cli/commands/generate.ts"
      to: "src/core/generator.ts"
      via: "import and function call"
    - from: "src/cli/commands/generate.ts"
      to: "src/core/writer.ts"
      via: "import and function call"
---

<objective>
Set up project structure with package.json, TypeScript config, and CLI entry point. Install all required dependencies.
</objective>

<context>
@~/.opencode/get-shit-done/references/ui-brand.md
</context>

<tasks>

<task type="auto">
  <name>Create package.json with dependencies</name>
  <files>package.json</files>
  <action>
    Create package.json with:
    - name: "tayo"
    - type: "module"
    - bin: "./dist/index.js"
    - scripts: {build: "tsc", start: "node dist/index.js", dev: "tsc && node dist/index.js"}
    - dependencies: {commander: "^12.0.0", zod: "^3.24.0", picocolors: "^1.0.0", "@babel/parser": "^7.29.0", "@babel/template": "^7.29.0", "@babel/traverse": "^7.29.0"}
    - devDependencies: {typescript: "^5.7.0", "@types/node": "^22.0.0", vitest: "^3.0.0}
  </action>
  <verify>package.json exists with correct structure and all dependencies listed</verify>
  <done>package.json created with all required dependencies</done>
</task>

<task type="auto">
  <name>Create TypeScript configuration</name>
  <files>tsconfig.json</files>
  <action>
    Create tsconfig.json with:
    - target: ES2022
    - module: ESNext
    - moduleResolution: bundler
    - outDir: ./dist
    - rootDir: ./src
    - strict: true
    - esModuleInterop: true
    - skipLibCheck: true
  </action>
  <verify>tsconfig.json exists with valid TypeScript configuration</verify>
  <done>tsconfig.json created with ES2022 target and ESNext modules</done>
</task>

<task type="auto">
  <name>Create source directory structure</name>
  <files>src/index.ts, src/core/parser.ts, src/core/validator.ts, src/core/generator.ts, src/core/writer.ts, src/cli/commands/generate.ts</files>
  <action>
    Create directory structure:
    - src/index.ts (main entry point with commander setup)
    - src/cli/commands/generate.ts (generate command)
    - src/core/parser.ts (Chrome Recorder parsing)
    - src/core/validator.ts (schema validation)
    - src/core/generator.ts (RTL test generation)
    - src/core/writer.ts (file writing)

    Each file should have basic TypeScript interfaces and stub functions for the pipeline.
  </action>
  <verify>All source files created with directory structure in place</verify>
  <done>Source directory structure created with stub implementations</done>
</task>

</tasks>

<verification>
- Run `npm install` to verify dependencies resolve
- Run `npx tsc --noEmit` to verify TypeScript configuration
</verification>

<success_criteria>
- package.json has all dependencies (commander, zod, babel packages)
- TypeScript compiles without errors
- Basic file structure in place for subsequent plans
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-pipeline/01-01-SUMMARY.md`
</output>
