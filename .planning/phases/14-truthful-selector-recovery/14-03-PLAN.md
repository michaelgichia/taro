---
phase: 14-truthful-selector-recovery
plan: "03"
type: execute
wave: 3
depends_on:
  - 14-01
  - 14-02
files_modified:
  - src/cli/commands/generate.test.ts
  - src/core/resolver.test.ts
  - sample/sample-rest-recordingextension-output.js
autonomous: true
requirements:
  - QUERY-02
  - QUERY-03
  - QUERY-04

must_haves:
  truths:
    - "Optional live DOM enrichment improves selectors when trustworthy evidence exists but generation stays truthful when that enrichment is unavailable"
    - "The Add Sale sample no longer produces fabricated selector fallbacks when selector strengthening fails"
    - "Dry-run output surfaces the exact warning/degradation states users need to see for offline or inspection-failure paths"
  artifacts:
    - path: "src/cli/commands/generate.test.ts"
      provides: "Sample-backed CLI coverage for URL-present, URL-missing, and inspection-failure selector flows"
      contains: "sample-rest-recordingextension-output"
    - path: "src/core/resolver.test.ts"
      provides: "Resolver regression tests for optional live DOM enrichment and failure handling"
      contains: "inspectElements"
    - path: "sample/sample-rest-recordingextension-output.js"
      provides: "Golden recorder fixture anchoring the Phase 14 regression path"
      contains: "document.querySelector"
  key_links:
    - from: "src/cli/commands/generate.test.ts"
      to: "sample/sample-rest-recordingextension-output.js"
      via: "golden recorder regression"
      pattern: "sample-rest-recordingextension-output"
    - from: "src/cli/commands/generate.test.ts"
      to: "src/cli/commands/generate.ts"
      via: "dry-run warning and checkpoint behavior"
      pattern: "createGenerateCommand"
---

<objective>
Prove the optional live DOM enrichment path and its truthful degradation modes.

Purpose: finish Phase 14 by demonstrating that selector strengthening works when evidence exists, and that URL-missing or inspection-failure paths remain explicit instead of silently degrading into fake queries.
Output: sample-backed regression coverage for the shipped JS generation flow across online and offline selector-recovery scenarios.
</objective>

<execution_context>
@/Users/michaelgichia/.codex/taro/workflows/execute-plan.md
@/Users/michaelgichia/.codex/taro/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-truthful-selector-recovery/14-RESEARCH.md
@.planning/phases/14-truthful-selector-recovery/14-VALIDATION.md
@src/cli/commands/generate.ts
@src/cli/commands/generate.test.ts
@src/core/resolver.test.ts
@sample/sample-rest-recordingextension-output.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add end-to-end JS generation coverage for selector recovery states</name>
  <files>src/cli/commands/generate.test.ts, src/core/resolver.test.ts</files>
  <action>
Expand the JS generation regression suite to cover the full Phase 14 selector matrix.

Required outcomes:
1. Add mocked tests for URL-present + accessible DOM evidence, URL-present + inaccessible DOM evidence, URL-missing, and inspection-failure behavior.
2. Assert the emitted warnings/checkpoints match the truthful degradation state instead of silently producing fake queries.
3. Keep browser-dependent behavior mocked so the regression suite remains fast and deterministic.

Do not expand into Phase 15 boundary or repo-aware suite planning concerns here.
  </action>
  <verify>
    <automated>npm run build && npm run test:run -- src/cli/commands/generate.test.ts src/core/resolver.test.ts</automated>
  </verify>
  <done>
    The shipped JS generation path has deterministic regression coverage for every important selector-recovery state Phase 14 owns.
  </done>
</task>

<task type="auto">
  <name>Task 2: Lock the Add Sale sample against fabricated selector fallbacks</name>
  <files>src/cli/commands/generate.test.ts, sample/sample-rest-recordingextension-output.js</files>
  <action>
Use the existing recorder JS sample as the gold regression fixture for Phase 14.

Required outcomes:
1. Add assertions that the Add Sale dry-run output no longer fabricates `getByTestId(...)` queries from CSS-only selector steps.
2. Add assertions that unresolved selectors produce explicit warnings/checkpoints carrying the original selector evidence.
3. Preserve the sample fixture as the golden input; only adjust it if the test harness truly requires a narrower fixture for deterministic coverage.

Do not rewrite the generated sample output file in this plan; this is a regression guard for Tayo's shipped generation path.
  </action>
  <verify>
    <automated>npm run build && npm run test:run -- src/cli/commands/generate.test.ts</automated>
  </verify>
  <done>
    The Add Sale sample proves Phase 14's truthful selector policy on the repo's real JS baseline fixture.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds.
2. `npm run test:run -- src/cli/commands/generate.test.ts src/core/resolver.test.ts` succeeds.
3. The Add Sale JS sample no longer yields fabricated CSS-to-testid fallback output.
4. URL-missing and inspection-failure paths emit explicit degradation signals without aborting generation.
</verification>

<success_criteria>
- Online and offline selector-recovery paths are both covered
- Add Sale sample guards against fabricated fallback regressions
- Dry-run warnings/checkpoints reflect real resolution state
- Browser-dependent behavior remains mocked and deterministic
</success_criteria>

<output>
After completion, create `.planning/phases/14-truthful-selector-recovery/14-03-SUMMARY.md` documenting optional live DOM enrichment behavior and sample-backed selector-recovery regression coverage.
</output>
