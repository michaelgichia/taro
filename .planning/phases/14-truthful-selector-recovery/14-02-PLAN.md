---
phase: 14-truthful-selector-recovery
plan: "02"
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - src/cli/commands/generate.ts
  - src/core/generator.ts
  - src/templates/test-template.ts
  - src/core/generator.test.ts
autonomous: true
requirements:
  - QUERY-02
  - QUERY-03

must_haves:
  truths:
    - "JS generation consumes truthful selector outcomes instead of assuming every selector can become a concrete RTL query"
    - "Unresolved selectors become explicit warnings/checkpoints in generated output rather than clean-looking fabricated code"
    - "Recorder-provided accessible query evidence still wins over weaker selector-derived guesses"
  artifacts:
    - path: "src/cli/commands/generate.ts"
      provides: "JS generation orchestration that threads resolved and unresolved selector outcomes into dry-run and write flows"
      contains: "resolveJsQueryResults"
    - path: "src/core/generator.ts"
      provides: "Truthful step rendering for unresolved selector checkpoints"
      contains: "generateTestFromGroups"
    - path: "src/templates/test-template.ts"
      provides: "Stable TODO/checkpoint rendering for unresolved JS selector steps"
      contains: "stepTemplate"
    - path: "src/core/generator.test.ts"
      provides: "Generation coverage for truthful unresolved selector output"
      contains: "generateTestFromGroups"
  key_links:
    - from: "src/cli/commands/generate.ts"
      to: "src/core/resolver.ts"
      via: "selector-resolution outcomes"
      pattern: "resolveSelector"
    - from: "src/cli/commands/generate.ts"
      to: "src/core/generator.ts"
      via: "JS generation handoff"
      pattern: "generateTestFromGroups"
    - from: "src/core/generator.ts"
      to: "src/templates/test-template.ts"
      via: "checkpoint rendering"
      pattern: "stepTemplate"
---

<objective>
Wire truthful selector outcomes into JS generation.

Purpose: ensure Phase 14 changes affect the shipped `tayo generate` output path by replacing fake selector fallbacks with explicit, reviewable checkpoints and warnings.
Output: generation orchestration and templates that preserve truthful unresolved selector states in both dry-run previews and written tests.
</objective>

<execution_context>
@/Users/michaelgichia/.codex/taro/workflows/execute-plan.md
@/Users/michaelgichia/.codex/taro/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-truthful-selector-recovery/14-RESEARCH.md
@.planning/phases/14-truthful-selector-recovery/14-VALIDATION.md
@src/cli/commands/generate.ts
@src/core/generator.ts
@src/templates/test-template.ts
@src/core/generator.test.ts
@src/core/resolver.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread truthful selector outcomes through JS generation</name>
  <files>src/cli/commands/generate.ts, src/core/generator.ts, src/templates/test-template.ts</files>
  <action>
Update the shipped generation path to consume the new resolver contract.

Required outcomes:
1. Refactor `resolveJsQueryResults()` or an equivalent seam so JS generation receives resolved queries plus unresolved selector outcomes, not just a flat `QueryResult[]`.
2. Preserve existing recorder query descriptors when they are already stronger than selector-derived evidence.
3. For unresolved selectors, emit explicit warning/checkpoint markers in generated code and CLI output instead of fake `getByTestId(...)` placeholders derived from CSS selectors.
4. Keep dry-run and write behavior aligned so truthful degradation is visible in both modes.

Do not solve score weighting here; leave machine-detectable markers in place for Phase 16.
  </action>
  <verify>
    <automated>npm run build && npm run test:run -- src/core/generator.test.ts</automated>
  </verify>
  <done>
    JS generation emits truthful selector output: resolved selectors become real queries, unresolved selectors become explicit checkpoints, and no fabricated fallback code is introduced.
  </done>
</task>

<task type="auto">
  <name>Task 2: Lock generator behavior with unresolved-selector tests</name>
  <files>src/core/generator.test.ts</files>
  <action>
Expand generator-level coverage around the new selector-resolution behavior.

Required outcomes:
1. Add tests proving unresolved selector states render explicit TODO/checkpoint comments rather than `screen.getByTestId(...)` placeholders.
2. Add tests proving preserved recorder query evidence still reconstructs accessible queries correctly.
3. Keep generated output syntactically valid so unresolved-selector markers do not break the emitted test file.

Do not duplicate full CLI coverage here; keep this plan focused on generator behavior.
  </action>
  <verify>
    <automated>npm run build && npm run test:run -- src/core/generator.test.ts</automated>
  </verify>
  <done>
    Generator tests prove Phase 14's truthful selector policy survives the code-emission layer.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds.
2. `npm run test:run -- src/core/generator.test.ts` succeeds.
3. Generated JS tests no longer contain fabricated CSS-to-testid fallbacks for unresolved selectors.
4. Unresolved selector checkpoints remain syntactically valid and reviewable in emitted code.
</verification>

<success_criteria>
- JS generation consumes selector-resolution outcomes
- Unresolved selectors are rendered explicitly
- Recorder query evidence still reconstructs correctly
- Generator coverage proves no fake fallback code is emitted
</success_criteria>

<output>
After completion, create `.planning/phases/14-truthful-selector-recovery/14-02-SUMMARY.md` documenting truthful JS generation output and unresolved-selector checkpoint rendering.
</output>
