---
phase: 14-truthful-selector-recovery
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/recording.ts
  - src/core/resolver.ts
  - src/core/resolver.test.ts
autonomous: true
requirements:
  - QUERY-02
  - QUERY-03

must_haves:
  truths:
    - "Selector-derived JS steps resolve through explicit outcome states instead of silently falling back to invented RTL queries"
    - "Inaccessible or failed-inspection selector states remain visible to later generation and scoring work"
    - "Phase 13 accessible query evidence remains reusable and is not overwritten by weaker selector heuristics"
  artifacts:
    - path: "src/types/recording.ts"
      provides: "Structured selector-resolution outcome types for JS-generated steps"
      contains: "SelectorResolutionResult"
    - path: "src/core/resolver.ts"
      provides: "Truthful selector resolution helpers that distinguish resolved and unresolved states"
      contains: "resolveSelector"
    - path: "src/core/resolver.test.ts"
      provides: "Unit coverage for accessible resolution and explicit unresolved outcomes"
      contains: "resolveSelector"
  key_links:
    - from: "src/core/resolver.ts"
      to: "src/types/recording.ts"
      via: "selector-resolution contract"
      pattern: "SelectorResolutionResult"
    - from: "src/core/resolver.ts"
      to: "src/core/resolver.test.ts"
      via: "truthful resolver behavior"
      pattern: "resolveSelector"
---

<objective>
Define the truthful selector-resolution contract for Phase 14.

Purpose: replace the current "always return some query" behavior with explicit resolved/unresolved selector outcomes so Tayo can strengthen selectors only when evidence supports it.
Output: selector-resolution types, resolver helpers, and tests that prove inaccessible DOM states no longer become invented `getByTestId(...)` output.
</objective>

<execution_context>
@/Users/michaelgichia/.codex/taro/workflows/execute-plan.md
@/Users/michaelgichia/.codex/taro/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-truthful-selector-recovery/14-RESEARCH.md
@.planning/phases/14-truthful-selector-recovery/14-VALIDATION.md
@src/types/recording.ts
@src/core/resolver.ts
@src/core/resolver.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add selector-resolution outcome types and resolver entrypoints</name>
  <files>src/types/recording.ts, src/core/resolver.ts</files>
  <action>
Extend the JS selector-resolution seam so it can represent truthful outcomes.

Required outcomes:
1. Add structured result types for selector resolution that distinguish at least: accessible-query resolved, no URL available, inspection failed, selector not found/inaccessible, and preserved recorder query evidence.
2. Refactor resolver helpers so accessible query derivation is separate from the policy decision about whether generation may use a query.
3. Keep Playwright inspection best-effort; resolver failures must surface as structured unresolved results rather than throwing or hiding the gap.
4. Preserve the original CSS selector and relevant reason metadata on unresolved outcomes so later generation can emit checkpoints.

Do not yet wire these results through the CLI generation flow in this plan.
  </action>
  <verify>
    <automated>rg -n "SelectorResolution|resolveSelector|unresolved" src/types/recording.ts src/core/resolver.ts</automated>
  </verify>
  <done>
    The repo has an explicit selector-resolution contract that can represent truthful success and failure states without inventing fallback queries.
  </done>
</task>

<task type="auto">
  <name>Task 2: Prove resolver truthfulness with focused unit coverage</name>
  <files>src/core/resolver.test.ts</files>
  <action>
Add resolver tests that lock the Phase 14 policy in place.

Required outcomes:
1. Cover successful selector strengthening when inspected DOM evidence supports an accessible query.
2. Cover unresolved/inaccessible selector states and verify they do not collapse into invented `getByTestId(...)` results.
3. Cover inspection failure and missing-URL-adjacent behavior using mocked Playwright responses rather than real browser launches.
4. Keep existing matcher/query tests that are still useful, but update them to reflect the new truthful result contract.

Do not add CLI or generator assertions here; keep this plan resolver-focused.
  </action>
  <verify>
    <automated>npm run build && npm run test:run -- src/core/resolver.test.ts</automated>
  </verify>
  <done>
    Resolver coverage proves Phase 14 can tell the difference between trustworthy selector recovery and unresolved selector evidence.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds.
2. `npm run test:run -- src/core/resolver.test.ts` succeeds.
3. The resolver no longer requires fake `getByTestId(...)` output to represent unresolved selector evidence.
4. Structured unresolved states preserve enough metadata for later plans to emit warnings and checkpoints.
</verification>

<success_criteria>
- Selector-resolution outcome types exist
- Resolver policy distinguishes resolved and unresolved states
- Inaccessible selectors no longer imply invented fallback queries
- Unit coverage locks the truthfulness policy in place
</success_criteria>

<output>
After completion, create `.planning/phases/14-truthful-selector-recovery/14-01-SUMMARY.md` documenting the selector-resolution contract and resolver truthfulness coverage.
</output>
