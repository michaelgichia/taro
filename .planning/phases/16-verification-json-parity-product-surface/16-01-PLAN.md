---
phase: 16-verification-json-parity-product-surface
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/scorer.ts
  - src/types/score.ts
  - src/cli/commands/generate.ts
  - src/core/scorer.test.ts
  - src/cli/commands/generate.test.ts
autonomous: true
requirements:
  - VERIFY-01

must_haves:
  truths:
    - "JS generation scores account explicitly for weak queries, weak assertions, placeholder output, and low-confidence generation states"
    - "The public score surface stays recognizable while adding deterministic reasons/signals and a stronger low-confidence trust banner"
    - "Low-confidence messaging remains advisory and surfaces the top blockers in both dry-run and write flows"
  artifacts:
    - path: "src/core/scorer.ts"
      provides: "Deterministic score dimensions plus explainable low-confidence signals/reasons"
      contains: "scoreGeneratedTest"
    - path: "src/types/score.ts"
      provides: "Public score contract for reasons/signals and low-confidence classification"
      contains: "ScoreResult"
    - path: "src/cli/commands/generate.ts"
      provides: "CLI trust messaging that shows a stronger banner and top blockers for low-confidence output"
      contains: "logScore"
    - path: "src/core/scorer.test.ts"
      provides: "Scoring regressions for draft-quality output and explainable deductions"
      contains: "calculateStructureScore"
  key_links:
    - from: "src/core/scorer.ts"
      to: "src/core/boundary-intelligence.ts"
      via: "boundary-derived blockers"
      pattern: "calculateBoundaryIsolationScore"
    - from: "src/cli/commands/generate.ts"
      to: "src/core/scorer.ts"
      via: "public score and trust banner emission"
      pattern: "scoreGeneratedTest"
---

<objective>
Strengthen the scoring and trust contract without changing generation scope.

Purpose: make low-confidence JS output explainable and visibly review-required while preserving the recognizable score line that Tayo already ships.
Output: richer score metadata, stronger draft-quality messaging, and regression coverage for the new trust contract.
</objective>

<execution_context>
@/Users/michaelgichia/.codex/taro/workflows/execute-plan.md
@/Users/michaelgichia/.codex/taro/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-verification-json-parity-product-surface/16-CONTEXT.md
@.planning/phases/16-verification-json-parity-product-surface/16-RESEARCH.md
@.planning/phases/16-verification-json-parity-product-surface/16-VALIDATION.md
@src/core/scorer.ts
@src/types/score.ts
@src/cli/commands/generate.ts
@src/core/scorer.test.ts
@src/cli/commands/generate.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the score contract with deterministic low-confidence reasons and signals</name>
  <files>src/core/scorer.ts, src/types/score.ts, src/core/scorer.test.ts</files>
  <action>
Upgrade the score layer so it explains low-confidence output instead of only emitting a thin aggregate.

Required outcomes:
1. Preserve the current recognizable public score shape (total, grade, dimensions) while adding deterministic reasons/signals derived from actual code evidence.
2. Make low-confidence states explicit for weak queries, weak assertions, placeholder render targets, and boundary-draft output.
3. Keep scoring advisory; do not convert low scores into write-blocking behavior.
4. Add focused scorer coverage proving the new reasons/signals are deterministic and that draft-quality output is scored/flagged consistently.

Do not redesign the entire scoring rubric from scratch in this task; Phase 16 extends the current contract rather than replacing it wholesale.
  </action>
  <verify>
    <automated>npm run build && npm run test:run -- src/core/scorer.test.ts</automated>
  </verify>
  <done>
    Tayo can explain why an output is low-confidence, not just assign it a number.
  </done>
</task>

<task type="auto">
  <name>Task 2: Emit the stronger low-confidence banner and top blockers in the CLI</name>
  <files>src/cli/commands/generate.ts, src/cli/commands/generate.test.ts</files>
  <action>
Wire the richer score contract into the public generate flow.

Required outcomes:
1. Add one strong summary banner in both dry-run and write mode when the score is `C` or below.
2. Surface the top two blockers in that banner using real score/boundary evidence rather than generic static text.
3. Preserve the existing score line and detailed tips so the CLI remains familiar.
4. Add CLI regressions proving the stronger banner is advisory, consistent across modes, and aligned with the score threshold.

Do not add new generator capabilities here; the task is about trust messaging on top of existing output.
  </action>
  <verify>
    <automated>npm run build && npm run test:run -- src/core/scorer.test.ts src/cli/commands/generate.test.ts</automated>
  </verify>
  <done>
    Users can tell immediately when generated output still needs manual review and why.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds.
2. `npm run test:run -- src/core/scorer.test.ts src/cli/commands/generate.test.ts` succeeds.
3. The public score line remains recognizable while low-confidence output gains deterministic reasons/signals.
4. `C`-or-below output triggers a stronger advisory banner with the top blockers named.
</verification>

<success_criteria>
- Score contract explains low-confidence states
- CLI shows stronger trust messaging without becoming blocking
- Draft-quality output is consistently flagged at the chosen threshold
- Regressions lock the trust contract in place
</success_criteria>

<output>
After completion, create `.planning/phases/16-verification-json-parity-product-surface/16-01-SUMMARY.md` documenting the score-contract and low-confidence messaging upgrade.
</output>
