---
phase: 16-verification-json-parity-product-surface
plan: "02"
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - sample/sample-json-recording-basic.json
  - sample/sample-json-recording-dialog.json
  - src/core/input-loader.test.ts
  - src/core/parser.test.ts
  - src/core/recording-intelligence.test.ts
  - src/cli/commands/generate.test.ts
autonomous: true
requirements:
  - VERIFY-02

must_haves:
  truths:
    - "Chrome Recorder JSON remains a supported public input path while JS fidelity improves"
    - "JSON parity proof is sample-backed and public-flow-oriented rather than only parser-internal"
    - "Behavioral regressions in the JSON generate contract are detectable without freezing every output byte"
  artifacts:
    - path: "sample/sample-json-recording-basic.json"
      provides: "Representative straightforward JSON fixture for parity proof"
      contains: "\"steps\""
    - path: "sample/sample-json-recording-dialog.json"
      provides: "Representative dialog/stateful JSON fixture for parity proof"
      contains: "\"steps\""
    - path: "src/cli/commands/generate.test.ts"
      provides: "Public generate-flow regression coverage for representative JSON inputs"
      contains: "createGenerateCommand"
    - path: "src/core/input-loader.test.ts"
      provides: "Shared input boundary proof for JSON fixture loading"
      contains: "loadInput"
  key_links:
    - from: "src/cli/commands/generate.test.ts"
      to: "src/core/input-loader.ts"
      via: "shared JSON input loading"
      pattern: "loadInput"
    - from: "src/cli/commands/generate.test.ts"
      to: "src/core/recording-intelligence.ts"
      via: "public-flow JSON behavior"
      pattern: "analyzeRecording"
---

<objective>
Prove that the JSON path still works through the public generate flow.

Purpose: add representative JSON fixtures and regression coverage that protect Chrome Recorder JSON behavior while Tayo's JS baseline path continues to evolve.
Output: sample-backed JSON parity proof at the shared input boundary and the public CLI boundary.
</objective>

<execution_context>
@/Users/michaelgichia/.codex/taro/workflows/execute-plan.md
@/Users/michaelgichia/.codex/taro/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-verification-json-parity-product-surface/16-CONTEXT.md
@.planning/phases/16-verification-json-parity-product-surface/16-RESEARCH.md
@.planning/phases/16-verification-json-parity-product-surface/16-VALIDATION.md
@src/core/input-loader.ts
@src/core/parser.ts
@src/core/recording-intelligence.ts
@src/cli/commands/generate.ts
@src/core/input-loader.test.ts
@src/core/parser.test.ts
@src/core/recording-intelligence.test.ts
@src/cli/commands/generate.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add representative JSON fixtures and shared-boundary regression coverage</name>
  <files>sample/sample-json-recording-basic.json, sample/sample-json-recording-dialog.json, src/core/input-loader.test.ts, src/core/parser.test.ts, src/core/recording-intelligence.test.ts</files>
  <action>
Create a small representative JSON proof set and use it to anchor shared-boundary regressions.

Required outcomes:
1. Add two representative Chrome Recorder JSON fixtures: one straightforward flow and one dialog/stateful flow.
2. Use those fixtures to prove the shared input boundary, parser behavior, and JSON recording analysis still behave as expected.
3. Keep the assertions behavioral and representative instead of freezing every generated line byte-for-byte.
4. Avoid expanding JSON support scope; this task protects the existing contract rather than adding new JSON-only features.
  </action>
  <verify>
    <automated>npm run build && npm run test:run -- src/core/input-loader.test.ts src/core/parser.test.ts src/core/recording-intelligence.test.ts</automated>
  </verify>
  <done>
    The repo has a believable JSON parity fixture set and regression coverage at the shared boundary.
  </done>
</task>

<task type="auto">
  <name>Task 2: Prove JSON parity through the public generate flow</name>
  <files>src/cli/commands/generate.test.ts, src/core/input-loader.test.ts</files>
  <action>
Extend the CLI regression surface so JSON parity is proven where users actually interact with Tayo.

Required outcomes:
1. Add CLI tests proving representative JSON inputs still work through `tayo generate` and `--dry-run`.
2. Keep the parity bar on behavioral regressions in the public contract rather than harmless output drift.
3. Align the JSON path with the updated Phase 16 trust contract where appropriate, without requiring JS-only selector-resolution behavior.
4. Preserve the shared output expectations that matter to users: parsing succeeds, output is generated, and the public score/trust surface remains coherent.

Do not update README/help here; Phase 16-03 owns the product-surface wording.
  </action>
  <verify>
    <automated>npm run build && npm run test:run -- src/cli/commands/generate.test.ts src/core/input-loader.test.ts</automated>
  </verify>
  <done>
    JSON non-regression is now proven at the public generate boundary rather than inferred from lower-level tests alone.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds.
2. `npm run test:run -- src/core/input-loader.test.ts src/core/parser.test.ts src/core/recording-intelligence.test.ts src/cli/commands/generate.test.ts` succeeds.
3. Two representative JSON fixtures prove the shared boundary and the public generate flow.
4. JSON parity failures are detectable as behavioral regressions in the public contract.
</verification>

<success_criteria>
- Representative JSON fixtures exist
- Shared-boundary JSON regressions are sample-backed
- Public generate-flow parity is tested
- JSON support remains stable without adding new scope
</success_criteria>

<output>
After completion, create `.planning/phases/16-verification-json-parity-product-surface/16-02-SUMMARY.md` documenting the representative JSON proof set and public-flow parity coverage.
</output>
